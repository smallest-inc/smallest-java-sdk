/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.callactions;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.MediaTypes;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.QueryStringMapper;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.callactions.requests.CreateCallActionRequest;
import ai.smallest.resources.atoms.callactions.requests.DeleteCallActionRequest;
import ai.smallest.resources.atoms.callactions.requests.GetCallActionRequest;
import ai.smallest.resources.atoms.callactions.requests.ListCallActionsRequest;
import ai.smallest.resources.atoms.callactions.requests.UpdateCallActionRequest;
import ai.smallest.resources.atoms.callactions.types.CreateCallActionResponse;
import ai.smallest.resources.atoms.callactions.types.DeleteCallActionResponse;
import ai.smallest.resources.atoms.callactions.types.GetCallActionResponse;
import ai.smallest.resources.atoms.callactions.types.ListCallActionsResponse;
import ai.smallest.resources.atoms.callactions.types.UpdateCallActionResponse;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.NotFoundError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawCallActionsClient {
  protected final ClientOptions clientOptions;

  public RawCallActionsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns a paginated list of call actions for the organization, filtered by agent. Optionally filter by category or provider.
   */
  public SmallestAIHttpResponse<ListCallActionsResponse> listCallActions(
      ListCallActionsRequest request) {
    return listCallActions(request,null);
  }

  /**
   * Returns a paginated list of call actions for the organization, filtered by agent. Optionally filter by category or provider.
   */
  public SmallestAIHttpResponse<ListCallActionsResponse> listCallActions(
      ListCallActionsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("call-actions");QueryStringMapper.addQueryParameter(httpUrl, "agentId", request.getAgentId(), false);
      if (request.getPage().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
      }
      if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (request.getCategory().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "category", request.getCategory().get(), false);
      }
      if (request.getProvider().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "provider", request.getProvider().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListCallActionsResponse.class), response);
        }
        try {
          switch (response.code()) {
            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (JsonProcessingException e) {
        throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
      }
      catch (IOException e) {
        throw new SmallestAIException("Network error executing HTTP request", e);
      }
    }

    /**
     * Creates a new call action for an agent. Call actions define automated behaviors that fire at specific points in a call lifecycle.
     * <ul>
     * <li><strong><code>trigger</code></strong> actions fire to initiate an outbound call and require <code>config.phoneNumberFieldName</code>.</li>
     * <li><strong><code>post-call</code></strong> actions fire after a call ends (e.g. to update a CRM record).</li>
     * </ul>
     */
    public SmallestAIHttpResponse<CreateCallActionResponse> createCallAction(
        CreateCallActionRequest request) {
      return createCallAction(request,null);
    }

    /**
     * Creates a new call action for an agent. Call actions define automated behaviors that fire at specific points in a call lifecycle.
     * <ul>
     * <li><strong><code>trigger</code></strong> actions fire to initiate an outbound call and require <code>config.phoneNumberFieldName</code>.</li>
     * <li><strong><code>post-call</code></strong> actions fire after a call ends (e.g. to update a CRM record).</li>
     * </ul>
     */
    public SmallestAIHttpResponse<CreateCallActionResponse> createCallAction(
        CreateCallActionRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("call-actions");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new SmallestAIException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateCallActionResponse.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (JsonProcessingException e) {
          throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
        }
        catch (IOException e) {
          throw new SmallestAIException("Network error executing HTTP request", e);
        }
      }

      /**
       * Returns a single call action by ID. Scoped to the authenticated organization.
       */
      public SmallestAIHttpResponse<GetCallActionResponse> getCallAction(String id) {
        return getCallAction(id,GetCallActionRequest.builder().build());
      }

      /**
       * Returns a single call action by ID. Scoped to the authenticated organization.
       */
      public SmallestAIHttpResponse<GetCallActionResponse> getCallAction(String id,
          RequestOptions requestOptions) {
        return getCallAction(id,GetCallActionRequest.builder().build(),requestOptions);
      }

      /**
       * Returns a single call action by ID. Scoped to the authenticated organization.
       */
      public SmallestAIHttpResponse<GetCallActionResponse> getCallAction(String id,
          GetCallActionRequest request) {
        return getCallAction(id,request,null);
      }

      /**
       * Returns a single call action by ID. Scoped to the authenticated organization.
       */
      public SmallestAIHttpResponse<GetCallActionResponse> getCallAction(String id,
          GetCallActionRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("call-actions")
          .addPathSegment(id);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetCallActionResponse.class), response);
            }
            try {
              switch (response.code()) {
                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (JsonProcessingException e) {
            throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
          }
          catch (IOException e) {
            throw new SmallestAIException("Network error executing HTTP request", e);
          }
        }

        /**
         * Updates an existing call action. All body fields are optional — only provided fields are updated.
         */
        public SmallestAIHttpResponse<UpdateCallActionResponse> updateCallAction(String id) {
          return updateCallAction(id,UpdateCallActionRequest.builder().build());
        }

        /**
         * Updates an existing call action. All body fields are optional — only provided fields are updated.
         */
        public SmallestAIHttpResponse<UpdateCallActionResponse> updateCallAction(String id,
            RequestOptions requestOptions) {
          return updateCallAction(id,UpdateCallActionRequest.builder().build(),requestOptions);
        }

        /**
         * Updates an existing call action. All body fields are optional — only provided fields are updated.
         */
        public SmallestAIHttpResponse<UpdateCallActionResponse> updateCallAction(String id,
            UpdateCallActionRequest request) {
          return updateCallAction(id,request,null);
        }

        /**
         * Updates an existing call action. All body fields are optional — only provided fields are updated.
         */
        public SmallestAIHttpResponse<UpdateCallActionResponse> updateCallAction(String id,
            UpdateCallActionRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("call-actions")
            .addPathSegment(id);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new SmallestAIException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("PUT", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateCallActionResponse.class), response);
              }
              try {
                switch (response.code()) {
                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (JsonProcessingException e) {
              throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
            }
            catch (IOException e) {
              throw new SmallestAIException("Network error executing HTTP request", e);
            }
          }

          /**
           * Permanently deletes a call action. Scoped to the authenticated organization.
           */
          public SmallestAIHttpResponse<DeleteCallActionResponse> deleteCallAction(String id) {
            return deleteCallAction(id,DeleteCallActionRequest.builder().build());
          }

          /**
           * Permanently deletes a call action. Scoped to the authenticated organization.
           */
          public SmallestAIHttpResponse<DeleteCallActionResponse> deleteCallAction(String id,
              RequestOptions requestOptions) {
            return deleteCallAction(id,DeleteCallActionRequest.builder().build(),requestOptions);
          }

          /**
           * Permanently deletes a call action. Scoped to the authenticated organization.
           */
          public SmallestAIHttpResponse<DeleteCallActionResponse> deleteCallAction(String id,
              DeleteCallActionRequest request) {
            return deleteCallAction(id,request,null);
          }

          /**
           * Permanently deletes a call action. Scoped to the authenticated organization.
           */
          public SmallestAIHttpResponse<DeleteCallActionResponse> deleteCallAction(String id,
              DeleteCallActionRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

              .addPathSegments("call-actions")
              .addPathSegment(id);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeleteCallActionResponse.class), response);
                }
                try {
                  switch (response.code()) {
                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (JsonProcessingException e) {
                throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
              }
              catch (IOException e) {
                throw new SmallestAIException("Network error executing HTTP request", e);
              }
            }
          }
