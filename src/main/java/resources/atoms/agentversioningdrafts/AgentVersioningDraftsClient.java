/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.agentversioningdrafts;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.RequestOptions;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.CreateDraftRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.DiscardDraftAgentVersioningDraftsRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.DraftConfigRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.GetAgentIdDraftsDraftIdDiffRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.GetAgentIdDraftsDraftIdRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.GetAgentIdDraftsRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.PostAgentIdDraftsDraftIdTestCallRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.PublishDraftRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.RenameDraftAgentVersioningDraftsRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.types.CreateDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.DiscardDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.GetAgentIdDraftsDraftIdDiffResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.GetAgentIdDraftsDraftIdResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.GetAgentIdDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.PostAgentIdDraftsDraftIdTestCallResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.PublishDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.RenameDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.UpdateDraftConfigAgentVersioningDraftsResponse;
import java.lang.String;

public class AgentVersioningDraftsClient {
  protected final ClientOptions clientOptions;

  private final RawAgentVersioningDraftsClient rawClient;

  public AgentVersioningDraftsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawAgentVersioningDraftsClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawAgentVersioningDraftsClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public GetAgentIdDraftsResponse listActiveDrafts(String id) {
    return this.rawClient.listActiveDrafts(id).body();
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public GetAgentIdDraftsResponse listActiveDrafts(String id, RequestOptions requestOptions) {
    return this.rawClient.listActiveDrafts(id, requestOptions).body();
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public GetAgentIdDraftsResponse listActiveDrafts(String id, GetAgentIdDraftsRequest request) {
    return this.rawClient.listActiveDrafts(id, request).body();
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public GetAgentIdDraftsResponse listActiveDrafts(String id, GetAgentIdDraftsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.listActiveDrafts(id, request, requestOptions).body();
  }

  /**
   * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
   */
  public CreateDraftAgentVersioningDraftsResponse createDraft(String id) {
    return this.rawClient.createDraft(id).body();
  }

  /**
   * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
   */
  public CreateDraftAgentVersioningDraftsResponse createDraft(String id,
      RequestOptions requestOptions) {
    return this.rawClient.createDraft(id, requestOptions).body();
  }

  /**
   * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
   */
  public CreateDraftAgentVersioningDraftsResponse createDraft(String id,
      CreateDraftRequest request) {
    return this.rawClient.createDraft(id, request).body();
  }

  /**
   * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
   */
  public CreateDraftAgentVersioningDraftsResponse createDraft(String id, CreateDraftRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.createDraft(id, request, requestOptions).body();
  }

  /**
   * Returns the latest revision of a draft along with its edit history.
   */
  public GetAgentIdDraftsDraftIdResponse getDraftDetail(String id, String draftId) {
    return this.rawClient.getDraftDetail(id, draftId).body();
  }

  /**
   * Returns the latest revision of a draft along with its edit history.
   */
  public GetAgentIdDraftsDraftIdResponse getDraftDetail(String id, String draftId,
      RequestOptions requestOptions) {
    return this.rawClient.getDraftDetail(id, draftId, requestOptions).body();
  }

  /**
   * Returns the latest revision of a draft along with its edit history.
   */
  public GetAgentIdDraftsDraftIdResponse getDraftDetail(String id, String draftId,
      GetAgentIdDraftsDraftIdRequest request) {
    return this.rawClient.getDraftDetail(id, draftId, request).body();
  }

  /**
   * Returns the latest revision of a draft along with its edit history.
   */
  public GetAgentIdDraftsDraftIdResponse getDraftDetail(String id, String draftId,
      GetAgentIdDraftsDraftIdRequest request, RequestOptions requestOptions) {
    return this.rawClient.getDraftDetail(id, draftId, request, requestOptions).body();
  }

  /**
   * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
   */
  public DiscardDraftAgentVersioningDraftsResponse discardDraft(String id, String draftId) {
    return this.rawClient.discardDraft(id, draftId).body();
  }

  /**
   * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
   */
  public DiscardDraftAgentVersioningDraftsResponse discardDraft(String id, String draftId,
      RequestOptions requestOptions) {
    return this.rawClient.discardDraft(id, draftId, requestOptions).body();
  }

  /**
   * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
   */
  public DiscardDraftAgentVersioningDraftsResponse discardDraft(String id, String draftId,
      DiscardDraftAgentVersioningDraftsRequest request) {
    return this.rawClient.discardDraft(id, draftId, request).body();
  }

  /**
   * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
   */
  public DiscardDraftAgentVersioningDraftsResponse discardDraft(String id, String draftId,
      DiscardDraftAgentVersioningDraftsRequest request, RequestOptions requestOptions) {
    return this.rawClient.discardDraft(id, draftId, request, requestOptions).body();
  }

  /**
   * Rename a draft. For config changes, use PATCH /agent/{id}/drafts/{draftId}/config instead.
   */
  public RenameDraftAgentVersioningDraftsResponse renameDraft(String id, String draftId,
      RenameDraftAgentVersioningDraftsRequest request) {
    return this.rawClient.renameDraft(id, draftId, request).body();
  }

  /**
   * Rename a draft. For config changes, use PATCH /agent/{id}/drafts/{draftId}/config instead.
   */
  public RenameDraftAgentVersioningDraftsResponse renameDraft(String id, String draftId,
      RenameDraftAgentVersioningDraftsRequest request, RequestOptions requestOptions) {
    return this.rawClient.renameDraft(id, draftId, request, requestOptions).body();
  }

  /**
   * Compare a draft against its source version or another specified version.
   */
  public GetAgentIdDraftsDraftIdDiffResponse getDraftDiff(String id, String draftId) {
    return this.rawClient.getDraftDiff(id, draftId).body();
  }

  /**
   * Compare a draft against its source version or another specified version.
   */
  public GetAgentIdDraftsDraftIdDiffResponse getDraftDiff(String id, String draftId,
      RequestOptions requestOptions) {
    return this.rawClient.getDraftDiff(id, draftId, requestOptions).body();
  }

  /**
   * Compare a draft against its source version or another specified version.
   */
  public GetAgentIdDraftsDraftIdDiffResponse getDraftDiff(String id, String draftId,
      GetAgentIdDraftsDraftIdDiffRequest request) {
    return this.rawClient.getDraftDiff(id, draftId, request).body();
  }

  /**
   * Compare a draft against its source version or another specified version.
   */
  public GetAgentIdDraftsDraftIdDiffResponse getDraftDiff(String id, String draftId,
      GetAgentIdDraftsDraftIdDiffRequest request, RequestOptions requestOptions) {
    return this.rawClient.getDraftDiff(id, draftId, request, requestOptions).body();
  }

  /**
   * Publish a draft as a new versioned release. Optionally activate it immediately.
   */
  public PublishDraftAgentVersioningDraftsResponse publishDraft(String id, String draftId) {
    return this.rawClient.publishDraft(id, draftId).body();
  }

  /**
   * Publish a draft as a new versioned release. Optionally activate it immediately.
   */
  public PublishDraftAgentVersioningDraftsResponse publishDraft(String id, String draftId,
      RequestOptions requestOptions) {
    return this.rawClient.publishDraft(id, draftId, requestOptions).body();
  }

  /**
   * Publish a draft as a new versioned release. Optionally activate it immediately.
   */
  public PublishDraftAgentVersioningDraftsResponse publishDraft(String id, String draftId,
      PublishDraftRequest request) {
    return this.rawClient.publishDraft(id, draftId, request).body();
  }

  /**
   * Publish a draft as a new versioned release. Optionally activate it immediately.
   */
  public PublishDraftAgentVersioningDraftsResponse publishDraft(String id, String draftId,
      PublishDraftRequest request, RequestOptions requestOptions) {
    return this.rawClient.publishDraft(id, draftId, request, requestOptions).body();
  }

  /**
   * Initiate a test call using the draft's resolved configuration.
   */
  public PostAgentIdDraftsDraftIdTestCallResponse testCallWithDraftConfig(String id, String draftId,
      PostAgentIdDraftsDraftIdTestCallRequest request) {
    return this.rawClient.testCallWithDraftConfig(id, draftId, request).body();
  }

  /**
   * Initiate a test call using the draft's resolved configuration.
   */
  public PostAgentIdDraftsDraftIdTestCallResponse testCallWithDraftConfig(String id, String draftId,
      PostAgentIdDraftsDraftIdTestCallRequest request, RequestOptions requestOptions) {
    return this.rawClient.testCallWithDraftConfig(id, draftId, request, requestOptions).body();
  }

  /**
   * Update the configuration of a draft. This single endpoint is how every
   * agent-level config field is changed: prompt, tools, voice, language,
   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
   * standalone post-call-analytics endpoint — it lives here as the
   * <code>postCallAnalyticsConfig</code> body field.
   * <h2>Post-Call Analytics</h2>
   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
   * automatically extracted from each completed call, along with the
   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
   * full Python walkthrough and disposition metric schema reference.</p>
   * <h2>Full payload</h2>
   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
   * fields:</p>
   * <ul>
   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
   * reasoning model flags.</li>
   * </ul>
   * <p>Each PATCH increments the draft's revision counter. Config is not
   * live until the draft is published and activated (see
   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
   */
  public UpdateDraftConfigAgentVersioningDraftsResponse updateDraftConfig(String id,
      String draftId) {
    return this.rawClient.updateDraftConfig(id, draftId).body();
  }

  /**
   * Update the configuration of a draft. This single endpoint is how every
   * agent-level config field is changed: prompt, tools, voice, language,
   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
   * standalone post-call-analytics endpoint — it lives here as the
   * <code>postCallAnalyticsConfig</code> body field.
   * <h2>Post-Call Analytics</h2>
   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
   * automatically extracted from each completed call, along with the
   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
   * full Python walkthrough and disposition metric schema reference.</p>
   * <h2>Full payload</h2>
   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
   * fields:</p>
   * <ul>
   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
   * reasoning model flags.</li>
   * </ul>
   * <p>Each PATCH increments the draft's revision counter. Config is not
   * live until the draft is published and activated (see
   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
   */
  public UpdateDraftConfigAgentVersioningDraftsResponse updateDraftConfig(String id, String draftId,
      RequestOptions requestOptions) {
    return this.rawClient.updateDraftConfig(id, draftId, requestOptions).body();
  }

  /**
   * Update the configuration of a draft. This single endpoint is how every
   * agent-level config field is changed: prompt, tools, voice, language,
   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
   * standalone post-call-analytics endpoint — it lives here as the
   * <code>postCallAnalyticsConfig</code> body field.
   * <h2>Post-Call Analytics</h2>
   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
   * automatically extracted from each completed call, along with the
   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
   * full Python walkthrough and disposition metric schema reference.</p>
   * <h2>Full payload</h2>
   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
   * fields:</p>
   * <ul>
   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
   * reasoning model flags.</li>
   * </ul>
   * <p>Each PATCH increments the draft's revision counter. Config is not
   * live until the draft is published and activated (see
   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
   */
  public UpdateDraftConfigAgentVersioningDraftsResponse updateDraftConfig(String id, String draftId,
      DraftConfigRequest request) {
    return this.rawClient.updateDraftConfig(id, draftId, request).body();
  }

  /**
   * Update the configuration of a draft. This single endpoint is how every
   * agent-level config field is changed: prompt, tools, voice, language,
   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
   * standalone post-call-analytics endpoint — it lives here as the
   * <code>postCallAnalyticsConfig</code> body field.
   * <h2>Post-Call Analytics</h2>
   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
   * automatically extracted from each completed call, along with the
   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
   * full Python walkthrough and disposition metric schema reference.</p>
   * <h2>Full payload</h2>
   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
   * fields:</p>
   * <ul>
   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
   * reasoning model flags.</li>
   * </ul>
   * <p>Each PATCH increments the draft's revision counter. Config is not
   * live until the draft is published and activated (see
   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
   */
  public UpdateDraftConfigAgentVersioningDraftsResponse updateDraftConfig(String id, String draftId,
      DraftConfigRequest request, RequestOptions requestOptions) {
    return this.rawClient.updateDraftConfig(id, draftId, request, requestOptions).body();
  }
}
