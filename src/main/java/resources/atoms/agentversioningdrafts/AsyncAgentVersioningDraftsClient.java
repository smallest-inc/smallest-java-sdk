/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.agentversioningdrafts;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.RequestOptions;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.CreateDraftRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.DiscardDraftAgentVersioningDraftsRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.DraftConfigRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.GetAgentIdDraftsDraftIdDiffRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.GetAgentIdDraftsDraftIdRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.GetAgentIdDraftsRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.PostAgentIdDraftsDraftIdTestCallRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.PublishDraftRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.RenameDraftAgentVersioningDraftsRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.types.CreateDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.DiscardDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.GetAgentIdDraftsDraftIdDiffResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.GetAgentIdDraftsDraftIdResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.GetAgentIdDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.PostAgentIdDraftsDraftIdTestCallResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.PublishDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.RenameDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.UpdateDraftConfigAgentVersioningDraftsResponse;
import java.lang.String;
import java.util.concurrent.CompletableFuture;

public class AsyncAgentVersioningDraftsClient {
  protected final ClientOptions clientOptions;

  private final AsyncRawAgentVersioningDraftsClient rawClient;

  public AsyncAgentVersioningDraftsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new AsyncRawAgentVersioningDraftsClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public AsyncRawAgentVersioningDraftsClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public CompletableFuture<GetAgentIdDraftsResponse> listActiveDrafts(String id) {
    return this.rawClient.listActiveDrafts(id).thenApply(response -> response.body());
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public CompletableFuture<GetAgentIdDraftsResponse> listActiveDrafts(String id,
      RequestOptions requestOptions) {
    return this.rawClient.listActiveDrafts(id, requestOptions).thenApply(response -> response.body());
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public CompletableFuture<GetAgentIdDraftsResponse> listActiveDrafts(String id,
      GetAgentIdDraftsRequest request) {
    return this.rawClient.listActiveDrafts(id, request).thenApply(response -> response.body());
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public CompletableFuture<GetAgentIdDraftsResponse> listActiveDrafts(String id,
      GetAgentIdDraftsRequest request, RequestOptions requestOptions) {
    return this.rawClient.listActiveDrafts(id, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
   */
  public CompletableFuture<CreateDraftAgentVersioningDraftsResponse> createDraft(String id) {
    return this.rawClient.createDraft(id).thenApply(response -> response.body());
  }

  /**
   * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
   */
  public CompletableFuture<CreateDraftAgentVersioningDraftsResponse> createDraft(String id,
      RequestOptions requestOptions) {
    return this.rawClient.createDraft(id, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
   */
  public CompletableFuture<CreateDraftAgentVersioningDraftsResponse> createDraft(String id,
      CreateDraftRequest request) {
    return this.rawClient.createDraft(id, request).thenApply(response -> response.body());
  }

  /**
   * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
   */
  public CompletableFuture<CreateDraftAgentVersioningDraftsResponse> createDraft(String id,
      CreateDraftRequest request, RequestOptions requestOptions) {
    return this.rawClient.createDraft(id, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the latest revision of a draft along with its edit history.
   */
  public CompletableFuture<GetAgentIdDraftsDraftIdResponse> getDraftDetail(String id,
      String draftId) {
    return this.rawClient.getDraftDetail(id, draftId).thenApply(response -> response.body());
  }

  /**
   * Returns the latest revision of a draft along with its edit history.
   */
  public CompletableFuture<GetAgentIdDraftsDraftIdResponse> getDraftDetail(String id,
      String draftId, RequestOptions requestOptions) {
    return this.rawClient.getDraftDetail(id, draftId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the latest revision of a draft along with its edit history.
   */
  public CompletableFuture<GetAgentIdDraftsDraftIdResponse> getDraftDetail(String id,
      String draftId, GetAgentIdDraftsDraftIdRequest request) {
    return this.rawClient.getDraftDetail(id, draftId, request).thenApply(response -> response.body());
  }

  /**
   * Returns the latest revision of a draft along with its edit history.
   */
  public CompletableFuture<GetAgentIdDraftsDraftIdResponse> getDraftDetail(String id,
      String draftId, GetAgentIdDraftsDraftIdRequest request, RequestOptions requestOptions) {
    return this.rawClient.getDraftDetail(id, draftId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
   */
  public CompletableFuture<DiscardDraftAgentVersioningDraftsResponse> discardDraft(String id,
      String draftId) {
    return this.rawClient.discardDraft(id, draftId).thenApply(response -> response.body());
  }

  /**
   * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
   */
  public CompletableFuture<DiscardDraftAgentVersioningDraftsResponse> discardDraft(String id,
      String draftId, RequestOptions requestOptions) {
    return this.rawClient.discardDraft(id, draftId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
   */
  public CompletableFuture<DiscardDraftAgentVersioningDraftsResponse> discardDraft(String id,
      String draftId, DiscardDraftAgentVersioningDraftsRequest request) {
    return this.rawClient.discardDraft(id, draftId, request).thenApply(response -> response.body());
  }

  /**
   * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
   */
  public CompletableFuture<DiscardDraftAgentVersioningDraftsResponse> discardDraft(String id,
      String draftId, DiscardDraftAgentVersioningDraftsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.discardDraft(id, draftId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Rename a draft. For config changes, use PATCH /agent/{id}/drafts/{draftId}/config instead.
   */
  public CompletableFuture<RenameDraftAgentVersioningDraftsResponse> renameDraft(String id,
      String draftId, RenameDraftAgentVersioningDraftsRequest request) {
    return this.rawClient.renameDraft(id, draftId, request).thenApply(response -> response.body());
  }

  /**
   * Rename a draft. For config changes, use PATCH /agent/{id}/drafts/{draftId}/config instead.
   */
  public CompletableFuture<RenameDraftAgentVersioningDraftsResponse> renameDraft(String id,
      String draftId, RenameDraftAgentVersioningDraftsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.renameDraft(id, draftId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Compare a draft against its source version or another specified version.
   */
  public CompletableFuture<GetAgentIdDraftsDraftIdDiffResponse> getDraftDiff(String id,
      String draftId) {
    return this.rawClient.getDraftDiff(id, draftId).thenApply(response -> response.body());
  }

  /**
   * Compare a draft against its source version or another specified version.
   */
  public CompletableFuture<GetAgentIdDraftsDraftIdDiffResponse> getDraftDiff(String id,
      String draftId, RequestOptions requestOptions) {
    return this.rawClient.getDraftDiff(id, draftId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Compare a draft against its source version or another specified version.
   */
  public CompletableFuture<GetAgentIdDraftsDraftIdDiffResponse> getDraftDiff(String id,
      String draftId, GetAgentIdDraftsDraftIdDiffRequest request) {
    return this.rawClient.getDraftDiff(id, draftId, request).thenApply(response -> response.body());
  }

  /**
   * Compare a draft against its source version or another specified version.
   */
  public CompletableFuture<GetAgentIdDraftsDraftIdDiffResponse> getDraftDiff(String id,
      String draftId, GetAgentIdDraftsDraftIdDiffRequest request, RequestOptions requestOptions) {
    return this.rawClient.getDraftDiff(id, draftId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Publish a draft as a new versioned release. Optionally activate it immediately.
   */
  public CompletableFuture<PublishDraftAgentVersioningDraftsResponse> publishDraft(String id,
      String draftId) {
    return this.rawClient.publishDraft(id, draftId).thenApply(response -> response.body());
  }

  /**
   * Publish a draft as a new versioned release. Optionally activate it immediately.
   */
  public CompletableFuture<PublishDraftAgentVersioningDraftsResponse> publishDraft(String id,
      String draftId, RequestOptions requestOptions) {
    return this.rawClient.publishDraft(id, draftId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Publish a draft as a new versioned release. Optionally activate it immediately.
   */
  public CompletableFuture<PublishDraftAgentVersioningDraftsResponse> publishDraft(String id,
      String draftId, PublishDraftRequest request) {
    return this.rawClient.publishDraft(id, draftId, request).thenApply(response -> response.body());
  }

  /**
   * Publish a draft as a new versioned release. Optionally activate it immediately.
   */
  public CompletableFuture<PublishDraftAgentVersioningDraftsResponse> publishDraft(String id,
      String draftId, PublishDraftRequest request, RequestOptions requestOptions) {
    return this.rawClient.publishDraft(id, draftId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Initiate a test call using the draft's resolved configuration.
   */
  public CompletableFuture<PostAgentIdDraftsDraftIdTestCallResponse> testCallWithDraftConfig(
      String id, String draftId, PostAgentIdDraftsDraftIdTestCallRequest request) {
    return this.rawClient.testCallWithDraftConfig(id, draftId, request).thenApply(response -> response.body());
  }

  /**
   * Initiate a test call using the draft's resolved configuration.
   */
  public CompletableFuture<PostAgentIdDraftsDraftIdTestCallResponse> testCallWithDraftConfig(
      String id, String draftId, PostAgentIdDraftsDraftIdTestCallRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.testCallWithDraftConfig(id, draftId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Update the configuration of a draft. This single endpoint is how every
   * agent-level config field is changed: prompt, tools, voice, language,
   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
   * standalone post-call-analytics endpoint — it lives here as the
   * <code>postCallAnalyticsConfig</code> body field.
   * <h2>Post-Call Analytics</h2>
   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
   * automatically extracted from each completed call, along with the
   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
   * full Python walkthrough and disposition metric schema reference.</p>
   * <h2>Full payload</h2>
   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
   * fields:</p>
   * <ul>
   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
   * reasoning model flags.</li>
   * </ul>
   * <p>Each PATCH increments the draft's revision counter. Config is not
   * live until the draft is published and activated (see
   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
   */
  public CompletableFuture<UpdateDraftConfigAgentVersioningDraftsResponse> updateDraftConfig(
      String id, String draftId) {
    return this.rawClient.updateDraftConfig(id, draftId).thenApply(response -> response.body());
  }

  /**
   * Update the configuration of a draft. This single endpoint is how every
   * agent-level config field is changed: prompt, tools, voice, language,
   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
   * standalone post-call-analytics endpoint — it lives here as the
   * <code>postCallAnalyticsConfig</code> body field.
   * <h2>Post-Call Analytics</h2>
   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
   * automatically extracted from each completed call, along with the
   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
   * full Python walkthrough and disposition metric schema reference.</p>
   * <h2>Full payload</h2>
   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
   * fields:</p>
   * <ul>
   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
   * reasoning model flags.</li>
   * </ul>
   * <p>Each PATCH increments the draft's revision counter. Config is not
   * live until the draft is published and activated (see
   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
   */
  public CompletableFuture<UpdateDraftConfigAgentVersioningDraftsResponse> updateDraftConfig(
      String id, String draftId, RequestOptions requestOptions) {
    return this.rawClient.updateDraftConfig(id, draftId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Update the configuration of a draft. This single endpoint is how every
   * agent-level config field is changed: prompt, tools, voice, language,
   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
   * standalone post-call-analytics endpoint — it lives here as the
   * <code>postCallAnalyticsConfig</code> body field.
   * <h2>Post-Call Analytics</h2>
   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
   * automatically extracted from each completed call, along with the
   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
   * full Python walkthrough and disposition metric schema reference.</p>
   * <h2>Full payload</h2>
   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
   * fields:</p>
   * <ul>
   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
   * reasoning model flags.</li>
   * </ul>
   * <p>Each PATCH increments the draft's revision counter. Config is not
   * live until the draft is published and activated (see
   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
   */
  public CompletableFuture<UpdateDraftConfigAgentVersioningDraftsResponse> updateDraftConfig(
      String id, String draftId, DraftConfigRequest request) {
    return this.rawClient.updateDraftConfig(id, draftId, request).thenApply(response -> response.body());
  }

  /**
   * Update the configuration of a draft. This single endpoint is how every
   * agent-level config field is changed: prompt, tools, voice, language,
   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
   * standalone post-call-analytics endpoint — it lives here as the
   * <code>postCallAnalyticsConfig</code> body field.
   * <h2>Post-Call Analytics</h2>
   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
   * automatically extracted from each completed call, along with the
   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
   * full Python walkthrough and disposition metric schema reference.</p>
   * <h2>Full payload</h2>
   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
   * fields:</p>
   * <ul>
   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
   * reasoning model flags.</li>
   * </ul>
   * <p>Each PATCH increments the draft's revision counter. Config is not
   * live until the draft is published and activated (see
   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
   */
  public CompletableFuture<UpdateDraftConfigAgentVersioningDraftsResponse> updateDraftConfig(
      String id, String draftId, DraftConfigRequest request, RequestOptions requestOptions) {
    return this.rawClient.updateDraftConfig(id, draftId, request, requestOptions).thenApply(response -> response.body());
  }
}
