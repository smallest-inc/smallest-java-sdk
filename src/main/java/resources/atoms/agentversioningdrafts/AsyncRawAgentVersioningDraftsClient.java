/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.agentversioningdrafts;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.MediaTypes;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.QueryStringMapper;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.CreateDraftRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.DiscardDraftAgentVersioningDraftsRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.DraftConfigRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.GetAgentIdDraftsDraftIdDiffRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.GetAgentIdDraftsDraftIdRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.GetAgentIdDraftsRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.PostAgentIdDraftsDraftIdTestCallRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.PublishDraftRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.requests.RenameDraftAgentVersioningDraftsRequest;
import ai.smallest.resources.atoms.agentversioningdrafts.types.CreateDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.DiscardDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.GetAgentIdDraftsDraftIdDiffResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.GetAgentIdDraftsDraftIdResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.GetAgentIdDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.PostAgentIdDraftsDraftIdTestCallResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.PublishDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.RenameDraftAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.agentversioningdrafts.types.UpdateDraftConfigAgentVersioningDraftsResponse;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.ForbiddenError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.NotFoundError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawAgentVersioningDraftsClient {
  protected final ClientOptions clientOptions;

  public AsyncRawAgentVersioningDraftsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsResponse>> listActiveDrafts(
      String id) {
    return listActiveDrafts(id,GetAgentIdDraftsRequest.builder().build());
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsResponse>> listActiveDrafts(
      String id, RequestOptions requestOptions) {
    return listActiveDrafts(id,GetAgentIdDraftsRequest.builder().build(),requestOptions);
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsResponse>> listActiveDrafts(
      String id, GetAgentIdDraftsRequest request) {
    return listActiveDrafts(id,request,null);
  }

  /**
   * List all active (non-discarded) drafts for an agent.
   */
  public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsResponse>> listActiveDrafts(
      String id, GetAgentIdDraftsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("agent")
      .addPathSegment(id)
      .addPathSegments("drafts");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentIdDraftsResponse.class), response));
              return;
            }
            try {
              switch (response.code()) {
                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (JsonProcessingException e) {
            future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
          }
          catch (IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
     */
    public CompletableFuture<SmallestAIHttpResponse<CreateDraftAgentVersioningDraftsResponse>> createDraft(
        String id) {
      return createDraft(id,CreateDraftRequest.builder().build());
    }

    /**
     * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
     */
    public CompletableFuture<SmallestAIHttpResponse<CreateDraftAgentVersioningDraftsResponse>> createDraft(
        String id, RequestOptions requestOptions) {
      return createDraft(id,CreateDraftRequest.builder().build(),requestOptions);
    }

    /**
     * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
     */
    public CompletableFuture<SmallestAIHttpResponse<CreateDraftAgentVersioningDraftsResponse>> createDraft(
        String id, CreateDraftRequest request) {
      return createDraft(id,request,null);
    }

    /**
     * Create a new draft from an existing published version or another draft. At least one of sourceVersionId or sourceDraftId is required (both may be sent simultaneously).
     */
    public CompletableFuture<SmallestAIHttpResponse<CreateDraftAgentVersioningDraftsResponse>> createDraft(
        String id, CreateDraftRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("agent")
        .addPathSegment(id)
        .addPathSegments("drafts");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new SmallestAIException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        CompletableFuture<SmallestAIHttpResponse<CreateDraftAgentVersioningDraftsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateDraftAgentVersioningDraftsResponse.class), response));
                return;
              }
              try {
                switch (response.code()) {
                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (JsonProcessingException e) {
              future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
            }
            catch (IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Returns the latest revision of a draft along with its edit history.
       */
      public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsDraftIdResponse>> getDraftDetail(
          String id, String draftId) {
        return getDraftDetail(id,draftId,GetAgentIdDraftsDraftIdRequest.builder().build());
      }

      /**
       * Returns the latest revision of a draft along with its edit history.
       */
      public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsDraftIdResponse>> getDraftDetail(
          String id, String draftId, RequestOptions requestOptions) {
        return getDraftDetail(id,draftId,GetAgentIdDraftsDraftIdRequest.builder().build(),requestOptions);
      }

      /**
       * Returns the latest revision of a draft along with its edit history.
       */
      public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsDraftIdResponse>> getDraftDetail(
          String id, String draftId, GetAgentIdDraftsDraftIdRequest request) {
        return getDraftDetail(id,draftId,request,null);
      }

      /**
       * Returns the latest revision of a draft along with its edit history.
       */
      public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsDraftIdResponse>> getDraftDetail(
          String id, String draftId, GetAgentIdDraftsDraftIdRequest request,
          RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("agent")
          .addPathSegment(id)
          .addPathSegments("drafts")
          .addPathSegment(draftId);if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
          }
          if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsDraftIdResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentIdDraftsDraftIdResponse.class), response));
                  return;
                }
                try {
                  switch (response.code()) {
                    case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (JsonProcessingException e) {
                future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
              }
              catch (IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
         */
        public CompletableFuture<SmallestAIHttpResponse<DiscardDraftAgentVersioningDraftsResponse>> discardDraft(
            String id, String draftId) {
          return discardDraft(id,draftId,DiscardDraftAgentVersioningDraftsRequest.builder().build());
        }

        /**
         * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
         */
        public CompletableFuture<SmallestAIHttpResponse<DiscardDraftAgentVersioningDraftsResponse>> discardDraft(
            String id, String draftId, RequestOptions requestOptions) {
          return discardDraft(id,draftId,DiscardDraftAgentVersioningDraftsRequest.builder().build(),requestOptions);
        }

        /**
         * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
         */
        public CompletableFuture<SmallestAIHttpResponse<DiscardDraftAgentVersioningDraftsResponse>> discardDraft(
            String id, String draftId, DiscardDraftAgentVersioningDraftsRequest request) {
          return discardDraft(id,draftId,request,null);
        }

        /**
         * Discard (soft-delete) a draft. Only the draft creator or an admin can discard.
         */
        public CompletableFuture<SmallestAIHttpResponse<DiscardDraftAgentVersioningDraftsResponse>> discardDraft(
            String id, String draftId, DiscardDraftAgentVersioningDraftsRequest request,
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("agent")
            .addPathSegment(id)
            .addPathSegments("drafts")
            .addPathSegment(draftId);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("DELETE", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            CompletableFuture<SmallestAIHttpResponse<DiscardDraftAgentVersioningDraftsResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DiscardDraftAgentVersioningDraftsResponse.class), response));
                    return;
                  }
                  try {
                    switch (response.code()) {
                      case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (JsonProcessingException e) {
                  future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                }
                catch (IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          /**
           * Rename a draft. For config changes, use PATCH /agent/{id}/drafts/{draftId}/config instead.
           */
          public CompletableFuture<SmallestAIHttpResponse<RenameDraftAgentVersioningDraftsResponse>> renameDraft(
              String id, String draftId, RenameDraftAgentVersioningDraftsRequest request) {
            return renameDraft(id,draftId,request,null);
          }

          /**
           * Rename a draft. For config changes, use PATCH /agent/{id}/drafts/{draftId}/config instead.
           */
          public CompletableFuture<SmallestAIHttpResponse<RenameDraftAgentVersioningDraftsResponse>> renameDraft(
              String id, String draftId, RenameDraftAgentVersioningDraftsRequest request,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

              .addPathSegments("agent")
              .addPathSegment(id)
              .addPathSegments("drafts")
              .addPathSegment(draftId);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              RequestBody body;
              try {
                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
              }
              catch(JsonProcessingException e) {
                throw new SmallestAIException("Failed to serialize request", e);
              }
              Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
              }
              CompletableFuture<SmallestAIHttpResponse<RenameDraftAgentVersioningDraftsResponse>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RenameDraftAgentVersioningDraftsResponse.class), response));
                      return;
                    }
                    try {
                      switch (response.code()) {
                        case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (JsonProcessingException e) {
                    future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              });
              return future;
            }

            /**
             * Compare a draft against its source version or another specified version.
             */
            public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsDraftIdDiffResponse>> getDraftDiff(
                String id, String draftId) {
              return getDraftDiff(id,draftId,GetAgentIdDraftsDraftIdDiffRequest.builder().build());
            }

            /**
             * Compare a draft against its source version or another specified version.
             */
            public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsDraftIdDiffResponse>> getDraftDiff(
                String id, String draftId, RequestOptions requestOptions) {
              return getDraftDiff(id,draftId,GetAgentIdDraftsDraftIdDiffRequest.builder().build(),requestOptions);
            }

            /**
             * Compare a draft against its source version or another specified version.
             */
            public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsDraftIdDiffResponse>> getDraftDiff(
                String id, String draftId, GetAgentIdDraftsDraftIdDiffRequest request) {
              return getDraftDiff(id,draftId,request,null);
            }

            /**
             * Compare a draft against its source version or another specified version.
             */
            public CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsDraftIdDiffResponse>> getDraftDiff(
                String id, String draftId, GetAgentIdDraftsDraftIdDiffRequest request,
                RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                .addPathSegments("agent")
                .addPathSegment(id)
                .addPathSegments("drafts")
                .addPathSegment(draftId)
                .addPathSegments("diff");if (request.getCompareTo().isPresent()) {
                  QueryStringMapper.addQueryParameter(httpUrl, "compareTo", request.getCompareTo().get(), false);
                }
                if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("GET", null)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Accept", "application/json");
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                  okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                }
                CompletableFuture<SmallestAIHttpResponse<GetAgentIdDraftsDraftIdDiffResponse>> future = new CompletableFuture<>();
                client.newCall(okhttpRequest).enqueue(new Callback() {
                  @Override
                  public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                    try (ResponseBody responseBody = response.body()) {
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentIdDraftsDraftIdDiffResponse.class), response));
                        return;
                      }
                      try {
                        switch (response.code()) {
                          case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                      return;
                    }
                    catch (JsonProcessingException e) {
                      future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                    }
                    catch (IOException e) {
                      future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                    }
                  }

                  @Override
                  public void onFailure(@NotNull Call call, @NotNull IOException e) {
                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                  }
                });
                return future;
              }

              /**
               * Publish a draft as a new versioned release. Optionally activate it immediately.
               */
              public CompletableFuture<SmallestAIHttpResponse<PublishDraftAgentVersioningDraftsResponse>> publishDraft(
                  String id, String draftId) {
                return publishDraft(id,draftId,PublishDraftRequest.builder().build());
              }

              /**
               * Publish a draft as a new versioned release. Optionally activate it immediately.
               */
              public CompletableFuture<SmallestAIHttpResponse<PublishDraftAgentVersioningDraftsResponse>> publishDraft(
                  String id, String draftId, RequestOptions requestOptions) {
                return publishDraft(id,draftId,PublishDraftRequest.builder().build(),requestOptions);
              }

              /**
               * Publish a draft as a new versioned release. Optionally activate it immediately.
               */
              public CompletableFuture<SmallestAIHttpResponse<PublishDraftAgentVersioningDraftsResponse>> publishDraft(
                  String id, String draftId, PublishDraftRequest request) {
                return publishDraft(id,draftId,request,null);
              }

              /**
               * Publish a draft as a new versioned release. Optionally activate it immediately.
               */
              public CompletableFuture<SmallestAIHttpResponse<PublishDraftAgentVersioningDraftsResponse>> publishDraft(
                  String id, String draftId, PublishDraftRequest request,
                  RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                  .addPathSegments("agent")
                  .addPathSegment(id)
                  .addPathSegments("drafts")
                  .addPathSegment(draftId)
                  .addPathSegments("publish");if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  RequestBody body;
                  try {
                    body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                  }
                  catch(JsonProcessingException e) {
                    throw new SmallestAIException("Failed to serialize request", e);
                  }
                  Request okhttpRequest = new Request.Builder()
                    .url(httpUrl.build())
                    .method("POST", body)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Content-Type", "application/json")
                    .addHeader("Accept", "application/json")
                    .build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                    okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                  }
                  CompletableFuture<SmallestAIHttpResponse<PublishDraftAgentVersioningDraftsResponse>> future = new CompletableFuture<>();
                  client.newCall(okhttpRequest).enqueue(new Callback() {
                    @Override
                    public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                      try (ResponseBody responseBody = response.body()) {
                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                        if (response.isSuccessful()) {
                          future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PublishDraftAgentVersioningDraftsResponse.class), response));
                          return;
                        }
                        try {
                          switch (response.code()) {
                            case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                            case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                            case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                            case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                            case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                          }
                        }
                        catch (JsonProcessingException ignored) {
                          // unable to map error response, throwing generic error
                        }
                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                        future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                        return;
                      }
                      catch (JsonProcessingException e) {
                        future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                      }
                      catch (IOException e) {
                        future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                      }
                    }

                    @Override
                    public void onFailure(@NotNull Call call, @NotNull IOException e) {
                      future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                    }
                  });
                  return future;
                }

                /**
                 * Initiate a test call using the draft's resolved configuration.
                 */
                public CompletableFuture<SmallestAIHttpResponse<PostAgentIdDraftsDraftIdTestCallResponse>> testCallWithDraftConfig(
                    String id, String draftId, PostAgentIdDraftsDraftIdTestCallRequest request) {
                  return testCallWithDraftConfig(id,draftId,request,null);
                }

                /**
                 * Initiate a test call using the draft's resolved configuration.
                 */
                public CompletableFuture<SmallestAIHttpResponse<PostAgentIdDraftsDraftIdTestCallResponse>> testCallWithDraftConfig(
                    String id, String draftId, PostAgentIdDraftsDraftIdTestCallRequest request,
                    RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                    .addPathSegments("agent")
                    .addPathSegment(id)
                    .addPathSegments("drafts")
                    .addPathSegment(draftId)
                    .addPathSegments("test-call");if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    RequestBody body;
                    try {
                      body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
                    }
                    catch(JsonProcessingException e) {
                      throw new SmallestAIException("Failed to serialize request", e);
                    }
                    Request okhttpRequest = new Request.Builder()
                      .url(httpUrl.build())
                      .method("POST", body)
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Content-Type", "application/json")
                      .addHeader("Accept", "application/json")
                      .build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                      okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                    }
                    CompletableFuture<SmallestAIHttpResponse<PostAgentIdDraftsDraftIdTestCallResponse>> future = new CompletableFuture<>();
                    client.newCall(okhttpRequest).enqueue(new Callback() {
                      @Override
                      public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                        try (ResponseBody responseBody = response.body()) {
                          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                          if (response.isSuccessful()) {
                            future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostAgentIdDraftsDraftIdTestCallResponse.class), response));
                            return;
                          }
                          try {
                            switch (response.code()) {
                              case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                            }
                          }
                          catch (JsonProcessingException ignored) {
                            // unable to map error response, throwing generic error
                          }
                          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                          future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                          return;
                        }
                        catch (JsonProcessingException e) {
                          future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                        }
                        catch (IOException e) {
                          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                        }
                      }

                      @Override
                      public void onFailure(@NotNull Call call, @NotNull IOException e) {
                        future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                      }
                    });
                    return future;
                  }

                  /**
                   * Update the configuration of a draft. This single endpoint is how every
                   * agent-level config field is changed: prompt, tools, voice, language,
                   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
                   * standalone post-call-analytics endpoint — it lives here as the
                   * <code>postCallAnalyticsConfig</code> body field.
                   * <h2>Post-Call Analytics</h2>
                   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
                   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
                   * automatically extracted from each completed call, along with the
                   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
                   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
                   * full Python walkthrough and disposition metric schema reference.</p>
                   * <h2>Full payload</h2>
                   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
                   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
                   * fields:</p>
                   * <ul>
                   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
                   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
                   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
                   * reasoning model flags.</li>
                   * </ul>
                   * <p>Each PATCH increments the draft's revision counter. Config is not
                   * live until the draft is published and activated (see
                   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
                   */
                  public CompletableFuture<SmallestAIHttpResponse<UpdateDraftConfigAgentVersioningDraftsResponse>> updateDraftConfig(
                      String id, String draftId) {
                    return updateDraftConfig(id,draftId,DraftConfigRequest.builder().build());
                  }

                  /**
                   * Update the configuration of a draft. This single endpoint is how every
                   * agent-level config field is changed: prompt, tools, voice, language,
                   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
                   * standalone post-call-analytics endpoint — it lives here as the
                   * <code>postCallAnalyticsConfig</code> body field.
                   * <h2>Post-Call Analytics</h2>
                   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
                   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
                   * automatically extracted from each completed call, along with the
                   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
                   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
                   * full Python walkthrough and disposition metric schema reference.</p>
                   * <h2>Full payload</h2>
                   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
                   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
                   * fields:</p>
                   * <ul>
                   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
                   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
                   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
                   * reasoning model flags.</li>
                   * </ul>
                   * <p>Each PATCH increments the draft's revision counter. Config is not
                   * live until the draft is published and activated (see
                   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
                   */
                  public CompletableFuture<SmallestAIHttpResponse<UpdateDraftConfigAgentVersioningDraftsResponse>> updateDraftConfig(
                      String id, String draftId, RequestOptions requestOptions) {
                    return updateDraftConfig(id,draftId,DraftConfigRequest.builder().build(),requestOptions);
                  }

                  /**
                   * Update the configuration of a draft. This single endpoint is how every
                   * agent-level config field is changed: prompt, tools, voice, language,
                   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
                   * standalone post-call-analytics endpoint — it lives here as the
                   * <code>postCallAnalyticsConfig</code> body field.
                   * <h2>Post-Call Analytics</h2>
                   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
                   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
                   * automatically extracted from each completed call, along with the
                   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
                   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
                   * full Python walkthrough and disposition metric schema reference.</p>
                   * <h2>Full payload</h2>
                   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
                   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
                   * fields:</p>
                   * <ul>
                   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
                   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
                   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
                   * reasoning model flags.</li>
                   * </ul>
                   * <p>Each PATCH increments the draft's revision counter. Config is not
                   * live until the draft is published and activated (see
                   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
                   */
                  public CompletableFuture<SmallestAIHttpResponse<UpdateDraftConfigAgentVersioningDraftsResponse>> updateDraftConfig(
                      String id, String draftId, DraftConfigRequest request) {
                    return updateDraftConfig(id,draftId,request,null);
                  }

                  /**
                   * Update the configuration of a draft. This single endpoint is how every
                   * agent-level config field is changed: prompt, tools, voice, language,
                   * <strong>post-call analytics (disposition metrics)</strong>, and more. There is no
                   * standalone post-call-analytics endpoint — it lives here as the
                   * <code>postCallAnalyticsConfig</code> body field.
                   * <h2>Post-Call Analytics</h2>
                   * <p>Pass a <code>postCallAnalyticsConfig</code> object to configure disposition
                   * metrics (STRING, BOOLEAN, INTEGER, ENUM, DATETIME) that are
                   * automatically extracted from each completed call, along with the
                   * <code>useInternalAnalyticsModel</code> and <code>useReasoningModel</code> flags. See the
                   * <a href="/atoms/atoms-platform/features/post-call-metrics">Post-Call Metrics guide</a> for a
                   * full Python walkthrough and disposition metric schema reference.</p>
                   * <h2>Full payload</h2>
                   * <p>Accepts the full agent-shaped config payload (language, synthesizer,
                   * slmModel, defaultVariables, preCallAPI, etc.) plus two draft-specific
                   * fields:</p>
                   * <ul>
                   * <li><code>singlePromptConfig</code> — prompt and tools (end_call, transfer_call,
                   * api_call, extract_dynamic_variables, knowledge_base_search).</li>
                   * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics/
                   * reasoning model flags.</li>
                   * </ul>
                   * <p>Each PATCH increments the draft's revision counter. Config is not
                   * live until the draft is published and activated (see
                   * <code>/drafts/{draftId}/publish</code> and <code>/versions/{versionId}/activate</code>).</p>
                   */
                  public CompletableFuture<SmallestAIHttpResponse<UpdateDraftConfigAgentVersioningDraftsResponse>> updateDraftConfig(
                      String id, String draftId, DraftConfigRequest request,
                      RequestOptions requestOptions) {
                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                      .addPathSegments("agent")
                      .addPathSegment(id)
                      .addPathSegments("drafts")
                      .addPathSegment(draftId)
                      .addPathSegments("config");if (requestOptions != null) {
                        requestOptions.getQueryParameters().forEach((_key, _value) -> {
                          httpUrl.addQueryParameter(_key, _value);
                        } );
                      }
                      RequestBody body;
                      try {
                        body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                      }
                      catch(JsonProcessingException e) {
                        throw new SmallestAIException("Failed to serialize request", e);
                      }
                      Request okhttpRequest = new Request.Builder()
                        .url(httpUrl.build())
                        .method("PATCH", body)
                        .headers(Headers.of(clientOptions.headers(requestOptions)))
                        .addHeader("Content-Type", "application/json")
                        .addHeader("Accept", "application/json")
                        .build();
                      OkHttpClient client = clientOptions.httpClient();
                      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                        client = clientOptions.httpClientWithTimeout(requestOptions);
                      }
                      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                      }
                      CompletableFuture<SmallestAIHttpResponse<UpdateDraftConfigAgentVersioningDraftsResponse>> future = new CompletableFuture<>();
                      client.newCall(okhttpRequest).enqueue(new Callback() {
                        @Override
                        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                          try (ResponseBody responseBody = response.body()) {
                            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                            if (response.isSuccessful()) {
                              future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateDraftConfigAgentVersioningDraftsResponse.class), response));
                              return;
                            }
                            try {
                              switch (response.code()) {
                                case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                              }
                            }
                            catch (JsonProcessingException ignored) {
                              // unable to map error response, throwing generic error
                            }
                            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                            future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                            return;
                          }
                          catch (JsonProcessingException e) {
                            future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                          }
                          catch (IOException e) {
                            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                          }
                        }

                        @Override
                        public void onFailure(@NotNull Call call, @NotNull IOException e) {
                          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                        }
                      });
                      return future;
                    }
                  }
