/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.webhooks;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.RequestOptions;
import ai.smallest.resources.atoms.webhooks.requests.CreateWebhooksRequest;
import ai.smallest.resources.atoms.webhooks.requests.DeleteAgentAgentIdWebhookSubscriptionsRequest;
import ai.smallest.resources.atoms.webhooks.requests.DeleteWebhooksRequest;
import ai.smallest.resources.atoms.webhooks.requests.GetAgentAgentIdWebhookSubscriptionsRequest;
import ai.smallest.resources.atoms.webhooks.requests.GetWebhookRequest;
import ai.smallest.resources.atoms.webhooks.requests.PostAgentAgentIdWebhookSubscriptionsRequest;
import ai.smallest.resources.atoms.webhooks.types.CreateWebhooksResponse;
import ai.smallest.resources.atoms.webhooks.types.DeleteAgentAgentIdWebhookSubscriptionsResponse;
import ai.smallest.resources.atoms.webhooks.types.DeleteWebhooksResponse;
import ai.smallest.resources.atoms.webhooks.types.GetAgentAgentIdWebhookSubscriptionsResponse;
import ai.smallest.resources.atoms.webhooks.types.GetWebhookResponse;
import ai.smallest.resources.atoms.webhooks.types.PostAgentAgentIdWebhookSubscriptionsResponse;
import java.lang.String;
import java.util.concurrent.CompletableFuture;

public class AsyncWebhooksClient {
  protected final ClientOptions clientOptions;

  private final AsyncRawWebhooksClient rawClient;

  public AsyncWebhooksClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new AsyncRawWebhooksClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public AsyncRawWebhooksClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Retrieve all webhooks for the organization or a specific webhook by ID
   */
  public CompletableFuture<GetWebhookResponse> getWebhooks() {
    return this.rawClient.getWebhooks().thenApply(response -> response.body());
  }

  /**
   * Retrieve all webhooks for the organization or a specific webhook by ID
   */
  public CompletableFuture<GetWebhookResponse> getWebhooks(RequestOptions requestOptions) {
    return this.rawClient.getWebhooks(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Retrieve all webhooks for the organization or a specific webhook by ID
   */
  public CompletableFuture<GetWebhookResponse> getWebhooks(GetWebhookRequest request) {
    return this.rawClient.getWebhooks(request).thenApply(response -> response.body());
  }

  /**
   * Retrieve all webhooks for the organization or a specific webhook by ID
   */
  public CompletableFuture<GetWebhookResponse> getWebhooks(GetWebhookRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getWebhooks(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Create a new webhook with subscriptions for specific agents and events.
   * <p><strong>400 is also returned when the endpoint URL is already registered:</strong>
   * <code>&quot;A webhook with this URL has already been registered&quot;</code></p>
   */
  public CompletableFuture<CreateWebhooksResponse> create(CreateWebhooksRequest request) {
    return this.rawClient.create(request).thenApply(response -> response.body());
  }

  /**
   * Create a new webhook with subscriptions for specific agents and events.
   * <p><strong>400 is also returned when the endpoint URL is already registered:</strong>
   * <code>&quot;A webhook with this URL has already been registered&quot;</code></p>
   */
  public CompletableFuture<CreateWebhooksResponse> create(CreateWebhooksRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.create(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Delete a webhook by its ID.
   * <p><strong>400 is returned when the webhook still has active agent subscriptions:</strong>
   * <code>&quot;Cannot delete webhook: It is currently assigned to one or more agents. Please remove all agent assignments first.&quot;</code>
   * Call <code>DELETE /agent/{agentId}/webhook-subscriptions</code> for each assigned agent before deleting.</p>
   * <p><strong>400 is also returned for an invalid webhook ID format:</strong>
   * <code>&quot;The provided Webhook ID is invalid.&quot;</code></p>
   */
  public CompletableFuture<DeleteWebhooksResponse> delete(String id) {
    return this.rawClient.delete(id).thenApply(response -> response.body());
  }

  /**
   * Delete a webhook by its ID.
   * <p><strong>400 is returned when the webhook still has active agent subscriptions:</strong>
   * <code>&quot;Cannot delete webhook: It is currently assigned to one or more agents. Please remove all agent assignments first.&quot;</code>
   * Call <code>DELETE /agent/{agentId}/webhook-subscriptions</code> for each assigned agent before deleting.</p>
   * <p><strong>400 is also returned for an invalid webhook ID format:</strong>
   * <code>&quot;The provided Webhook ID is invalid.&quot;</code></p>
   */
  public CompletableFuture<DeleteWebhooksResponse> delete(String id,
      RequestOptions requestOptions) {
    return this.rawClient.delete(id, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Delete a webhook by its ID.
   * <p><strong>400 is returned when the webhook still has active agent subscriptions:</strong>
   * <code>&quot;Cannot delete webhook: It is currently assigned to one or more agents. Please remove all agent assignments first.&quot;</code>
   * Call <code>DELETE /agent/{agentId}/webhook-subscriptions</code> for each assigned agent before deleting.</p>
   * <p><strong>400 is also returned for an invalid webhook ID format:</strong>
   * <code>&quot;The provided Webhook ID is invalid.&quot;</code></p>
   */
  public CompletableFuture<DeleteWebhooksResponse> delete(String id,
      DeleteWebhooksRequest request) {
    return this.rawClient.delete(id, request).thenApply(response -> response.body());
  }

  /**
   * Delete a webhook by its ID.
   * <p><strong>400 is returned when the webhook still has active agent subscriptions:</strong>
   * <code>&quot;Cannot delete webhook: It is currently assigned to one or more agents. Please remove all agent assignments first.&quot;</code>
   * Call <code>DELETE /agent/{agentId}/webhook-subscriptions</code> for each assigned agent before deleting.</p>
   * <p><strong>400 is also returned for an invalid webhook ID format:</strong>
   * <code>&quot;The provided Webhook ID is invalid.&quot;</code></p>
   */
  public CompletableFuture<DeleteWebhooksResponse> delete(String id, DeleteWebhooksRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.delete(id, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Retrieve webhook subscriptions for a given agent ID
   */
  public CompletableFuture<GetAgentAgentIdWebhookSubscriptionsResponse> getWebhookSubscriptionsForAnAgent(
      String agentId) {
    return this.rawClient.getWebhookSubscriptionsForAnAgent(agentId).thenApply(response -> response.body());
  }

  /**
   * Retrieve webhook subscriptions for a given agent ID
   */
  public CompletableFuture<GetAgentAgentIdWebhookSubscriptionsResponse> getWebhookSubscriptionsForAnAgent(
      String agentId, RequestOptions requestOptions) {
    return this.rawClient.getWebhookSubscriptionsForAnAgent(agentId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Retrieve webhook subscriptions for a given agent ID
   */
  public CompletableFuture<GetAgentAgentIdWebhookSubscriptionsResponse> getWebhookSubscriptionsForAnAgent(
      String agentId, GetAgentAgentIdWebhookSubscriptionsRequest request) {
    return this.rawClient.getWebhookSubscriptionsForAnAgent(agentId, request).thenApply(response -> response.body());
  }

  /**
   * Retrieve webhook subscriptions for a given agent ID
   */
  public CompletableFuture<GetAgentAgentIdWebhookSubscriptionsResponse> getWebhookSubscriptionsForAnAgent(
      String agentId, GetAgentAgentIdWebhookSubscriptionsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getWebhookSubscriptionsForAnAgent(agentId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * <strong>Replaces</strong> all existing webhook subscriptions for the agent with the provided event types.
   * Any previously configured subscriptions for this agent are deleted before the new ones are created.
   * To add subscriptions without removing existing ones, retrieve current subscriptions first and include them in the request.
   */
  public CompletableFuture<PostAgentAgentIdWebhookSubscriptionsResponse> replaceWebhookSubscriptionsForAnAgent(
      String agentId, PostAgentAgentIdWebhookSubscriptionsRequest request) {
    return this.rawClient.replaceWebhookSubscriptionsForAnAgent(agentId, request).thenApply(response -> response.body());
  }

  /**
   * <strong>Replaces</strong> all existing webhook subscriptions for the agent with the provided event types.
   * Any previously configured subscriptions for this agent are deleted before the new ones are created.
   * To add subscriptions without removing existing ones, retrieve current subscriptions first and include them in the request.
   */
  public CompletableFuture<PostAgentAgentIdWebhookSubscriptionsResponse> replaceWebhookSubscriptionsForAnAgent(
      String agentId, PostAgentAgentIdWebhookSubscriptionsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.replaceWebhookSubscriptionsForAnAgent(agentId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Deletes <strong>all</strong> webhook subscriptions for the agent, regardless of which webhook they belong to.
   * If the agent has subscriptions across multiple webhooks, all of them are removed in a single call.
   */
  public CompletableFuture<DeleteAgentAgentIdWebhookSubscriptionsResponse> deleteWebhookSubscriptionsForAnAgent(
      String agentId) {
    return this.rawClient.deleteWebhookSubscriptionsForAnAgent(agentId).thenApply(response -> response.body());
  }

  /**
   * Deletes <strong>all</strong> webhook subscriptions for the agent, regardless of which webhook they belong to.
   * If the agent has subscriptions across multiple webhooks, all of them are removed in a single call.
   */
  public CompletableFuture<DeleteAgentAgentIdWebhookSubscriptionsResponse> deleteWebhookSubscriptionsForAnAgent(
      String agentId, RequestOptions requestOptions) {
    return this.rawClient.deleteWebhookSubscriptionsForAnAgent(agentId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Deletes <strong>all</strong> webhook subscriptions for the agent, regardless of which webhook they belong to.
   * If the agent has subscriptions across multiple webhooks, all of them are removed in a single call.
   */
  public CompletableFuture<DeleteAgentAgentIdWebhookSubscriptionsResponse> deleteWebhookSubscriptionsForAnAgent(
      String agentId, DeleteAgentAgentIdWebhookSubscriptionsRequest request) {
    return this.rawClient.deleteWebhookSubscriptionsForAnAgent(agentId, request).thenApply(response -> response.body());
  }

  /**
   * Deletes <strong>all</strong> webhook subscriptions for the agent, regardless of which webhook they belong to.
   * If the agent has subscriptions across multiple webhooks, all of them are removed in a single call.
   */
  public CompletableFuture<DeleteAgentAgentIdWebhookSubscriptionsResponse> deleteWebhookSubscriptionsForAnAgent(
      String agentId, DeleteAgentAgentIdWebhookSubscriptionsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.deleteWebhookSubscriptionsForAnAgent(agentId, request, requestOptions).thenApply(response -> response.body());
  }
}
