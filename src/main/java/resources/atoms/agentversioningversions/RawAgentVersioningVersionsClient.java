/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.agentversioningversions;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.MediaTypes;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.QueryStringMapper;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.agentversioningversions.requests.ActivateVersionAgentVersioningVersionsRequest;
import ai.smallest.resources.atoms.agentversioningversions.requests.CompareVersionMetricsAgentVersioningVersionsRequest;
import ai.smallest.resources.atoms.agentversioningversions.requests.GetAgentIdVersionsDiffRequest;
import ai.smallest.resources.atoms.agentversioningversions.requests.GetAgentIdVersionsRequest;
import ai.smallest.resources.atoms.agentversioningversions.requests.GetAgentIdVersionsVersionIdRequest;
import ai.smallest.resources.atoms.agentversioningversions.requests.PostAgentIdVersionsVersionIdTestCallRequest;
import ai.smallest.resources.atoms.agentversioningversions.requests.UpdateVersionMetaRequest;
import ai.smallest.resources.atoms.agentversioningversions.types.ActivateVersionAgentVersioningVersionsResponse;
import ai.smallest.resources.atoms.agentversioningversions.types.CompareVersionMetricsAgentVersioningVersionsResponse;
import ai.smallest.resources.atoms.agentversioningversions.types.GetAgentIdVersionsDiffResponse;
import ai.smallest.resources.atoms.agentversioningversions.types.GetAgentIdVersionsResponse;
import ai.smallest.resources.atoms.agentversioningversions.types.GetAgentIdVersionsVersionIdResponse;
import ai.smallest.resources.atoms.agentversioningversions.types.PostAgentIdVersionsVersionIdTestCallResponse;
import ai.smallest.resources.atoms.agentversioningversions.types.UpdateVersionMetadataAgentVersioningVersionsResponse;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.ForbiddenError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.NotFoundError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawAgentVersioningVersionsClient {
  protected final ClientOptions clientOptions;

  public RawAgentVersioningVersionsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * List published versions for an agent with pagination and optional pin filter.
   * The <code>total</code> value currently represents the total number of published versions
   * for the agent, not necessarily the filtered count when <code>isPinned</code> is used.
   */
  public SmallestAIHttpResponse<GetAgentIdVersionsResponse> listPublishedVersions(String id) {
    return listPublishedVersions(id,GetAgentIdVersionsRequest.builder().build());
  }

  /**
   * List published versions for an agent with pagination and optional pin filter.
   * The <code>total</code> value currently represents the total number of published versions
   * for the agent, not necessarily the filtered count when <code>isPinned</code> is used.
   */
  public SmallestAIHttpResponse<GetAgentIdVersionsResponse> listPublishedVersions(String id,
      RequestOptions requestOptions) {
    return listPublishedVersions(id,GetAgentIdVersionsRequest.builder().build(),requestOptions);
  }

  /**
   * List published versions for an agent with pagination and optional pin filter.
   * The <code>total</code> value currently represents the total number of published versions
   * for the agent, not necessarily the filtered count when <code>isPinned</code> is used.
   */
  public SmallestAIHttpResponse<GetAgentIdVersionsResponse> listPublishedVersions(String id,
      GetAgentIdVersionsRequest request) {
    return listPublishedVersions(id,request,null);
  }

  /**
   * List published versions for an agent with pagination and optional pin filter.
   * The <code>total</code> value currently represents the total number of published versions
   * for the agent, not necessarily the filtered count when <code>isPinned</code> is used.
   */
  public SmallestAIHttpResponse<GetAgentIdVersionsResponse> listPublishedVersions(String id,
      GetAgentIdVersionsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("agent")
      .addPathSegment(id)
      .addPathSegments("versions");if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (request.getSkip().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "skip", request.getSkip().get(), false);
      }
      if (request.getIsPinned().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "isPinned", request.getIsPinned().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentIdVersionsResponse.class), response);
        }
        try {
          switch (response.code()) {
            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (JsonProcessingException e) {
        throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
      }
      catch (IOException e) {
        throw new SmallestAIException("Network error executing HTTP request", e);
      }
    }

    /**
     * Compare two version or draft revision records side-by-side by their IDs. The implementation tries published versions first and can fall back to the latest draft revision.
     */
    public SmallestAIHttpResponse<GetAgentIdVersionsDiffResponse> diffTwoVersions(String id,
        GetAgentIdVersionsDiffRequest request) {
      return diffTwoVersions(id,request,null);
    }

    /**
     * Compare two version or draft revision records side-by-side by their IDs. The implementation tries published versions first and can fall back to the latest draft revision.
     */
    public SmallestAIHttpResponse<GetAgentIdVersionsDiffResponse> diffTwoVersions(String id,
        GetAgentIdVersionsDiffRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("agent")
        .addPathSegment(id)
        .addPathSegments("versions")
        .addPathSegments("diff");QueryStringMapper.addQueryParameter(httpUrl, "versionA", request.getVersionA(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "versionB", request.getVersionB(), false);
        if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentIdVersionsDiffResponse.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (JsonProcessingException e) {
          throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
        }
        catch (IOException e) {
          throw new SmallestAIException("Network error executing HTTP request", e);
        }
      }

      /**
       * Compare analytics/call metrics between two published versions over an optional date range.
       */
      public SmallestAIHttpResponse<CompareVersionMetricsAgentVersioningVersionsResponse> compareVersionMetrics(
          String id, CompareVersionMetricsAgentVersioningVersionsRequest request) {
        return compareVersionMetrics(id,request,null);
      }

      /**
       * Compare analytics/call metrics between two published versions over an optional date range.
       */
      public SmallestAIHttpResponse<CompareVersionMetricsAgentVersioningVersionsResponse> compareVersionMetrics(
          String id, CompareVersionMetricsAgentVersioningVersionsRequest request,
          RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("agent")
          .addPathSegment(id)
          .addPathSegments("versions")
          .addPathSegments("compare-metrics");QueryStringMapper.addQueryParameter(httpUrl, "versionA", request.getVersionA(), false);
          QueryStringMapper.addQueryParameter(httpUrl, "versionB", request.getVersionB(), false);
          if (request.getDateFrom().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "dateFrom", request.getDateFrom().get(), false);
          }
          if (request.getDateTo().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "dateTo", request.getDateTo().get(), false);
          }
          if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CompareVersionMetricsAgentVersioningVersionsResponse.class), response);
            }
            try {
              switch (response.code()) {
                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (JsonProcessingException e) {
            throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
          }
          catch (IOException e) {
            throw new SmallestAIException("Network error executing HTTP request", e);
          }
        }

        /**
         * Returns the full detail of a specific published version (read-only).
         * Published versions are config-immutable — to modify config, create a draft from
         * this version and publish it as a new version.
         */
        public SmallestAIHttpResponse<GetAgentIdVersionsVersionIdResponse> getVersionDetail(
            String id, String versionId) {
          return getVersionDetail(id,versionId,GetAgentIdVersionsVersionIdRequest.builder().build());
        }

        /**
         * Returns the full detail of a specific published version (read-only).
         * Published versions are config-immutable — to modify config, create a draft from
         * this version and publish it as a new version.
         */
        public SmallestAIHttpResponse<GetAgentIdVersionsVersionIdResponse> getVersionDetail(
            String id, String versionId, RequestOptions requestOptions) {
          return getVersionDetail(id,versionId,GetAgentIdVersionsVersionIdRequest.builder().build(),requestOptions);
        }

        /**
         * Returns the full detail of a specific published version (read-only).
         * Published versions are config-immutable — to modify config, create a draft from
         * this version and publish it as a new version.
         */
        public SmallestAIHttpResponse<GetAgentIdVersionsVersionIdResponse> getVersionDetail(
            String id, String versionId, GetAgentIdVersionsVersionIdRequest request) {
          return getVersionDetail(id,versionId,request,null);
        }

        /**
         * Returns the full detail of a specific published version (read-only).
         * Published versions are config-immutable — to modify config, create a draft from
         * this version and publish it as a new version.
         */
        public SmallestAIHttpResponse<GetAgentIdVersionsVersionIdResponse> getVersionDetail(
            String id, String versionId, GetAgentIdVersionsVersionIdRequest request,
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("agent")
            .addPathSegment(id)
            .addPathSegments("versions")
            .addPathSegment(versionId);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("GET", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentIdVersionsVersionIdResponse.class), response);
              }
              try {
                switch (response.code()) {
                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (JsonProcessingException e) {
              throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
            }
            catch (IOException e) {
              throw new SmallestAIException("Network error executing HTTP request", e);
            }
          }

          /**
           * Update a published version's label, description, or pinned status. At least one field is required.
           * Published versions (both active and inactive) are config-immutable — their agent
           * configuration cannot be changed. To modify config, create a new draft from the version,
           * edit the draft, and publish it as a new version.
           */
          public SmallestAIHttpResponse<UpdateVersionMetadataAgentVersioningVersionsResponse> updateVersionMetadata(
              String id, String versionId) {
            return updateVersionMetadata(id,versionId,UpdateVersionMetaRequest.builder().build());
          }

          /**
           * Update a published version's label, description, or pinned status. At least one field is required.
           * Published versions (both active and inactive) are config-immutable — their agent
           * configuration cannot be changed. To modify config, create a new draft from the version,
           * edit the draft, and publish it as a new version.
           */
          public SmallestAIHttpResponse<UpdateVersionMetadataAgentVersioningVersionsResponse> updateVersionMetadata(
              String id, String versionId, RequestOptions requestOptions) {
            return updateVersionMetadata(id,versionId,UpdateVersionMetaRequest.builder().build(),requestOptions);
          }

          /**
           * Update a published version's label, description, or pinned status. At least one field is required.
           * Published versions (both active and inactive) are config-immutable — their agent
           * configuration cannot be changed. To modify config, create a new draft from the version,
           * edit the draft, and publish it as a new version.
           */
          public SmallestAIHttpResponse<UpdateVersionMetadataAgentVersioningVersionsResponse> updateVersionMetadata(
              String id, String versionId, UpdateVersionMetaRequest request) {
            return updateVersionMetadata(id,versionId,request,null);
          }

          /**
           * Update a published version's label, description, or pinned status. At least one field is required.
           * Published versions (both active and inactive) are config-immutable — their agent
           * configuration cannot be changed. To modify config, create a new draft from the version,
           * edit the draft, and publish it as a new version.
           */
          public SmallestAIHttpResponse<UpdateVersionMetadataAgentVersioningVersionsResponse> updateVersionMetadata(
              String id, String versionId, UpdateVersionMetaRequest request,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

              .addPathSegments("agent")
              .addPathSegment(id)
              .addPathSegments("versions")
              .addPathSegment(versionId);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              RequestBody body;
              try {
                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
              }
              catch(JsonProcessingException e) {
                throw new SmallestAIException("Failed to serialize request", e);
              }
              Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateVersionMetadataAgentVersioningVersionsResponse.class), response);
                }
                try {
                  switch (response.code()) {
                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (JsonProcessingException e) {
                throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
              }
              catch (IOException e) {
                throw new SmallestAIException("Network error executing HTTP request", e);
              }
            }

            /**
             * Set a published version as the active version for the agent. The previously
             * active version is deactivated. This does not modify the version's config — it
             * only changes which version serves live traffic. Activation is idempotent: if
             * the version is already active, the endpoint returns that version without
             * changing config.
             */
            public SmallestAIHttpResponse<ActivateVersionAgentVersioningVersionsResponse> activateVersion(
                String id, String versionId) {
              return activateVersion(id,versionId,ActivateVersionAgentVersioningVersionsRequest.builder().build());
            }

            /**
             * Set a published version as the active version for the agent. The previously
             * active version is deactivated. This does not modify the version's config — it
             * only changes which version serves live traffic. Activation is idempotent: if
             * the version is already active, the endpoint returns that version without
             * changing config.
             */
            public SmallestAIHttpResponse<ActivateVersionAgentVersioningVersionsResponse> activateVersion(
                String id, String versionId, RequestOptions requestOptions) {
              return activateVersion(id,versionId,ActivateVersionAgentVersioningVersionsRequest.builder().build(),requestOptions);
            }

            /**
             * Set a published version as the active version for the agent. The previously
             * active version is deactivated. This does not modify the version's config — it
             * only changes which version serves live traffic. Activation is idempotent: if
             * the version is already active, the endpoint returns that version without
             * changing config.
             */
            public SmallestAIHttpResponse<ActivateVersionAgentVersioningVersionsResponse> activateVersion(
                String id, String versionId,
                ActivateVersionAgentVersioningVersionsRequest request) {
              return activateVersion(id,versionId,request,null);
            }

            /**
             * Set a published version as the active version for the agent. The previously
             * active version is deactivated. This does not modify the version's config — it
             * only changes which version serves live traffic. Activation is idempotent: if
             * the version is already active, the endpoint returns that version without
             * changing config.
             */
            public SmallestAIHttpResponse<ActivateVersionAgentVersioningVersionsResponse> activateVersion(
                String id, String versionId, ActivateVersionAgentVersioningVersionsRequest request,
                RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                .addPathSegments("agent")
                .addPathSegment(id)
                .addPathSegments("versions")
                .addPathSegment(versionId)
                .addPathSegments("activate");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("PATCH", RequestBody.create("", null))
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Accept", "application/json");
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                  okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ActivateVersionAgentVersioningVersionsResponse.class), response);
                  }
                  try {
                    switch (response.code()) {
                      case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (JsonProcessingException e) {
                  throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                }
                catch (IOException e) {
                  throw new SmallestAIException("Network error executing HTTP request", e);
                }
              }

              /**
               * Initiate a test call using a specific published version's configuration.
               * The response always includes <code>conversationId</code> and <code>callId</code>. For <code>webcall</code>
               * and <code>chat</code>, it also includes <code>token</code>, <code>roomName</code>, and <code>host</code>. Those fields
               * are omitted for <code>telephony</code>.
               */
              public SmallestAIHttpResponse<PostAgentIdVersionsVersionIdTestCallResponse> testCallWithVersionConfig(
                  String id, String versionId,
                  PostAgentIdVersionsVersionIdTestCallRequest request) {
                return testCallWithVersionConfig(id,versionId,request,null);
              }

              /**
               * Initiate a test call using a specific published version's configuration.
               * The response always includes <code>conversationId</code> and <code>callId</code>. For <code>webcall</code>
               * and <code>chat</code>, it also includes <code>token</code>, <code>roomName</code>, and <code>host</code>. Those fields
               * are omitted for <code>telephony</code>.
               */
              public SmallestAIHttpResponse<PostAgentIdVersionsVersionIdTestCallResponse> testCallWithVersionConfig(
                  String id, String versionId, PostAgentIdVersionsVersionIdTestCallRequest request,
                  RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                  .addPathSegments("agent")
                  .addPathSegment(id)
                  .addPathSegments("versions")
                  .addPathSegment(versionId)
                  .addPathSegments("test-call");if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  RequestBody body;
                  try {
                    body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
                  }
                  catch(JsonProcessingException e) {
                    throw new SmallestAIException("Failed to serialize request", e);
                  }
                  Request okhttpRequest = new Request.Builder()
                    .url(httpUrl.build())
                    .method("POST", body)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Content-Type", "application/json")
                    .addHeader("Accept", "application/json")
                    .build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                    okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                  }
                  try (Response response = client.newCall(okhttpRequest).execute()) {
                    ResponseBody responseBody = response.body();
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostAgentIdVersionsVersionIdTestCallResponse.class), response);
                    }
                    try {
                      switch (response.code()) {
                        case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                  }
                  catch (JsonProcessingException e) {
                    throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                  }
                  catch (IOException e) {
                    throw new SmallestAIException("Network error executing HTTP request", e);
                  }
                }
              }
