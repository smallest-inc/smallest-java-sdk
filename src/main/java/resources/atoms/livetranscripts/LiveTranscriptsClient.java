/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.livetranscripts;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.RequestOptions;
import ai.smallest.resources.atoms.livetranscripts.requests.SubscribeToLiveEventsLiveTranscriptsRequest;
import ai.smallest.resources.atoms.livetranscripts.types.SubscribeToLiveEventsLiveTranscriptsResponse;
import java.lang.Iterable;

public class LiveTranscriptsClient {
  protected final ClientOptions clientOptions;

  private final RawLiveTranscriptsClient rawClient;

  public LiveTranscriptsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawLiveTranscriptsClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawLiveTranscriptsClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Real-time streaming of user speech (STT) and agent speech (TTS) events for an active call via Server-Sent Events.
   * <p>The connection is real-time — events stream directly from the call runtime as they are produced. The SSE connection auto-closes when the call ends (<code>sse_close</code> event). Only active calls can be subscribed to; completed calls return a 400 error.</p>
   * <p><strong>Transcript event types:</strong></p>
   * <ul>
   * <li><code>user_interim_transcription</code> — Partial, in-progress transcription as the user speaks. Use for live preview only; will be superseded by <code>user_transcription</code>.</li>
   * <li><code>user_transcription</code> — Final transcription for a completed user speech turn.</li>
   * <li><code>tts_completed</code> — Fired when the agent finishes speaking a TTS segment. Includes the spoken text and optionally TTS latency.</li>
   * </ul>
   * <p><strong>Lifecycle events:</strong></p>
   * <ul>
   * <li><code>sse_init</code> — Sent immediately when the SSE connection is established.</li>
   * <li><code>sse_close</code> — Sent when the call ends, right before the server closes the connection.</li>
   * </ul>
   * <p>Other event types (e.g. <code>tool_call_start</code>, <code>pre_call_api</code>, <code>agent_log</code>, metrics) are also sent on this stream.</p>
   * <ul>
   * <li><code>call_start</code></li>
   * <li><code>call_end</code></li>
   * <li><code>turn_latency</code></li>
   * <li><code>metrics</code></li>
   * <li><code>agent_node_state</code></li>
   * <li><code>hopping</code></li>
   * <li><code>knowledgebase</code></li>
   * <li><code>variable_extraction</code></li>
   * <li><code>pre_call_api</code></li>
   * <li><code>post_call_api</code></li>
   * <li><code>agent_error</code></li>
   * <li><code>agent_log</code></li>
   * <li><code>tool_call_start</code></li>
   * <li><code>tool_call_end</code></li>
   * <li><code>tool_call_error</code></li>
   * <li><code>call_cancelled</code></li>
   * <li><code>call_recording</code></li>
   * </ul>
   */
  public Iterable<SubscribeToLiveEventsLiveTranscriptsResponse> subscribeToLiveEvents(
      SubscribeToLiveEventsLiveTranscriptsRequest request) {
    return this.rawClient.subscribeToLiveEvents(request).body();
  }

  /**
   * Real-time streaming of user speech (STT) and agent speech (TTS) events for an active call via Server-Sent Events.
   * <p>The connection is real-time — events stream directly from the call runtime as they are produced. The SSE connection auto-closes when the call ends (<code>sse_close</code> event). Only active calls can be subscribed to; completed calls return a 400 error.</p>
   * <p><strong>Transcript event types:</strong></p>
   * <ul>
   * <li><code>user_interim_transcription</code> — Partial, in-progress transcription as the user speaks. Use for live preview only; will be superseded by <code>user_transcription</code>.</li>
   * <li><code>user_transcription</code> — Final transcription for a completed user speech turn.</li>
   * <li><code>tts_completed</code> — Fired when the agent finishes speaking a TTS segment. Includes the spoken text and optionally TTS latency.</li>
   * </ul>
   * <p><strong>Lifecycle events:</strong></p>
   * <ul>
   * <li><code>sse_init</code> — Sent immediately when the SSE connection is established.</li>
   * <li><code>sse_close</code> — Sent when the call ends, right before the server closes the connection.</li>
   * </ul>
   * <p>Other event types (e.g. <code>tool_call_start</code>, <code>pre_call_api</code>, <code>agent_log</code>, metrics) are also sent on this stream.</p>
   * <ul>
   * <li><code>call_start</code></li>
   * <li><code>call_end</code></li>
   * <li><code>turn_latency</code></li>
   * <li><code>metrics</code></li>
   * <li><code>agent_node_state</code></li>
   * <li><code>hopping</code></li>
   * <li><code>knowledgebase</code></li>
   * <li><code>variable_extraction</code></li>
   * <li><code>pre_call_api</code></li>
   * <li><code>post_call_api</code></li>
   * <li><code>agent_error</code></li>
   * <li><code>agent_log</code></li>
   * <li><code>tool_call_start</code></li>
   * <li><code>tool_call_end</code></li>
   * <li><code>tool_call_error</code></li>
   * <li><code>call_cancelled</code></li>
   * <li><code>call_recording</code></li>
   * </ul>
   */
  public Iterable<SubscribeToLiveEventsLiveTranscriptsResponse> subscribeToLiveEvents(
      SubscribeToLiveEventsLiveTranscriptsRequest request, RequestOptions requestOptions) {
    return this.rawClient.subscribeToLiveEvents(request, requestOptions).body();
  }
}
