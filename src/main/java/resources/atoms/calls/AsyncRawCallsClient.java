/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.calls;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.MediaTypes;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.QueryStringMapper;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.calls.requests.GetCallsRequest;
import ai.smallest.resources.atoms.calls.requests.ListCallsRequest;
import ai.smallest.resources.atoms.calls.requests.SearchCallsRequest;
import ai.smallest.resources.atoms.calls.requests.StartOutboundCallCallsRequest;
import ai.smallest.resources.atoms.calls.types.GetCallsResponse;
import ai.smallest.resources.atoms.calls.types.ListCallsResponse;
import ai.smallest.resources.atoms.calls.types.SearchCallsResponse;
import ai.smallest.resources.atoms.calls.types.StartOutboundCallCallsResponse;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.ForbiddenError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.NotFoundError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.Override;
import java.lang.RuntimeException;
import java.lang.String;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawCallsClient {
  protected final ClientOptions clientOptions;

  public AsyncRawCallsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve paginated conversation logs with support for various filters. Returns call logs for agents belonging to the authenticated user's organization.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListCallsResponse>> list() {
    return list(ListCallsRequest.builder().build());
  }

  /**
   * Retrieve paginated conversation logs with support for various filters. Returns call logs for agents belonging to the authenticated user's organization.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListCallsResponse>> list(
      RequestOptions requestOptions) {
    return list(ListCallsRequest.builder().build(),requestOptions);
  }

  /**
   * Retrieve paginated conversation logs with support for various filters. Returns call logs for agents belonging to the authenticated user's organization.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListCallsResponse>> list(
      ListCallsRequest request) {
    return list(request,null);
  }

  /**
   * Retrieve paginated conversation logs with support for various filters. Returns call logs for agents belonging to the authenticated user's organization.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListCallsResponse>> list(ListCallsRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("conversation");if (request.getPage().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
      }
      if (request.getLimit().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "limit", request.getLimit().get(), false);
      }
      if (request.getAgentIds().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "agentIds", request.getAgentIds().get(), false);
      }
      if (request.getCampaignIds().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "campaignIds", request.getCampaignIds().get(), false);
      }
      if (request.getCallTypes().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "callTypes", request.getCallTypes().get(), false);
      }
      if (request.getSearch().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "search", request.getSearch().get(), false);
      }
      if (request.getStatusFilter().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "statusFilter", request.getStatusFilter().get(), false);
      }
      if (request.getDisconnectReasonFilter().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "disconnectReasonFilter", request.getDisconnectReasonFilter().get(), false);
      }
      if (request.getCallAttemptFilter().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "callAttemptFilter", request.getCallAttemptFilter().get(), false);
      }
      if (request.getDurationFilter().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "durationFilter", request.getDurationFilter().get(), false);
      }
      if (request.getSortBy().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sortBy", request.getSortBy().get(), false);
      }
      if (request.getSortOrder().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sortOrder", request.getSortOrder().get(), false);
      }
      if (request.getDateFrom().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "dateFrom", request.getDateFrom().get(), false);
      }
      if (request.getDateTo().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "dateTo", request.getDateTo().get(), false);
      }
      if (request.getVersionFilter().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "versionFilter", request.getVersionFilter().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      CompletableFuture<SmallestAIHttpResponse<ListCallsResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListCallsResponse.class), response));
              return;
            }
            try {
              switch (response.code()) {
                case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (JsonProcessingException e) {
            future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
          }
          catch (IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Fetch specific conversation logs by their callIds. This endpoint allows you to retrieve up to 100 specific calls at once.
     * Only returns calls that belong to agents in your organization (security check enforced).
     * Unlike the GET /conversation endpoint, this endpoint can also return retry calls (non-root calls).
     * <p><strong>Differences from GET /conversation response:</strong> each log item has the same base structure but
     * the following three fields are <strong>not</strong> included here:</p>
     * <ul>
     * <li><code>dispositionMetrics</code> — not enriched</li>
     * <li><code>agentDispositionConfig</code> — not enriched</li>
     * <li><code>versionNumber</code> — not enriched</li>
     * </ul>
     */
    public CompletableFuture<SmallestAIHttpResponse<SearchCallsResponse>> search(
        SearchCallsRequest request) {
      return search(request,null);
    }

    /**
     * Fetch specific conversation logs by their callIds. This endpoint allows you to retrieve up to 100 specific calls at once.
     * Only returns calls that belong to agents in your organization (security check enforced).
     * Unlike the GET /conversation endpoint, this endpoint can also return retry calls (non-root calls).
     * <p><strong>Differences from GET /conversation response:</strong> each log item has the same base structure but
     * the following three fields are <strong>not</strong> included here:</p>
     * <ul>
     * <li><code>dispositionMetrics</code> — not enriched</li>
     * <li><code>agentDispositionConfig</code> — not enriched</li>
     * <li><code>versionNumber</code> — not enriched</li>
     * </ul>
     */
    public CompletableFuture<SmallestAIHttpResponse<SearchCallsResponse>> search(
        SearchCallsRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("conversation/search");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new SmallestAIException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        CompletableFuture<SmallestAIHttpResponse<SearchCallsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SearchCallsResponse.class), response));
                return;
              }
              try {
                switch (response.code()) {
                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (JsonProcessingException e) {
              future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
            }
            catch (IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Retrieve detailed information about a specific conversation including transcript, events, and latency metrics.
       */
      public CompletableFuture<SmallestAIHttpResponse<GetCallsResponse>> get(String id) {
        return get(id,GetCallsRequest.builder().build());
      }

      /**
       * Retrieve detailed information about a specific conversation including transcript, events, and latency metrics.
       */
      public CompletableFuture<SmallestAIHttpResponse<GetCallsResponse>> get(String id,
          RequestOptions requestOptions) {
        return get(id,GetCallsRequest.builder().build(),requestOptions);
      }

      /**
       * Retrieve detailed information about a specific conversation including transcript, events, and latency metrics.
       */
      public CompletableFuture<SmallestAIHttpResponse<GetCallsResponse>> get(String id,
          GetCallsRequest request) {
        return get(id,request,null);
      }

      /**
       * Retrieve detailed information about a specific conversation including transcript, events, and latency metrics.
       */
      public CompletableFuture<SmallestAIHttpResponse<GetCallsResponse>> get(String id,
          GetCallsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("conversation")
          .addPathSegment(id);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          CompletableFuture<SmallestAIHttpResponse<GetCallsResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetCallsResponse.class), response));
                  return;
                }
                try {
                  switch (response.code()) {
                    case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (JsonProcessingException e) {
                future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
              }
              catch (IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Initiates an outbound telephony call with a specified agent and phone number.
         * <h2>Caller-ID resolution</h2>
         * <p>When <code>fromProductId</code> is omitted <strong>and</strong> the agent has no telephony
         * product attached, the call dispatches from a Smallest-managed Plivo
         * trunk using a default caller-ID number (chosen by destination
         * country). The call still places and the response is still
         * <code>200 + conversationId</code>, but the recipient sees the default Smallest
         * number rather than your own. For production traffic, either:</p>
         * <ul>
         * <li>pass <code>fromProductId</code> explicitly (look up your owned numbers via
         * <code>GET /product/phone-numbers</code>), or</li>
         * <li>attach a phone-number product to the agent.</li>
         * </ul>
         * <h2>Resolved-config check</h2>
         * <p>The call uses the agent's currently-active version. If your most
         * recent prompt change went through <code>PATCH /workflow/{workflowId}</code> and
         * the agent has versioning enabled, that change may not have
         * propagated to the active version — and the call will play the
         * platform-default greeting instead of your prompt. Before placing a
         * production call, fetch <code>GET /agent/{agentId}</code> and confirm
         * <code>_resolvedConfig.firstMessage</code> (and related fields) match what you
         * intended. The
         * <a href="/atoms/developer-guide/build/agents/versioning-lifecycle">Versioning Lifecycle</a>
         * guide covers the correct edit flow.</p>
         * <p><strong>400 is returned for:</strong></p>
         * <ul>
         * <li>Invalid <code>agentId</code> format (<code>&quot;Invalid agent id&quot;</code>)</li>
         * <li>Invalid <code>phoneNumber</code> format (<code>&quot;Invalid phone number&quot;</code>)</li>
         * <li>Invalid <code>fromProductId</code> format (<code>&quot;Invalid product id&quot;</code>)</li>
         * <li>Agent not found or not in the caller's org (<code>&quot;Agent not found&quot;</code>)</li>
         * <li>Agent is archived (<code>&quot;Agent is archived and cannot initiate calls&quot;</code>)</li>
         * <li><code>workflow_graph</code> agent has no workflow configured (<code>&quot;Workflow not found&quot;</code>)</li>
         * <li>Workflow has validation errors (<code>&quot;Invalid workflow, please fix the errors...&quot;</code>)</li>
         * </ul>
         * <p><strong>403</strong> is returned for <code>workflow_graph</code> agents when the org lacks conversational agents access.</p>
         * <p><strong>Test calls:</strong> set the <code>x-test-call: true</code> header to mark the resulting call log as a test call
         * (<code>isTest: true</code>). Test calls are subject to concurrent slot limits.</p>
         */
        public CompletableFuture<SmallestAIHttpResponse<StartOutboundCallCallsResponse>> startOutboundCall(
            StartOutboundCallCallsRequest request) {
          return startOutboundCall(request,null);
        }

        /**
         * Initiates an outbound telephony call with a specified agent and phone number.
         * <h2>Caller-ID resolution</h2>
         * <p>When <code>fromProductId</code> is omitted <strong>and</strong> the agent has no telephony
         * product attached, the call dispatches from a Smallest-managed Plivo
         * trunk using a default caller-ID number (chosen by destination
         * country). The call still places and the response is still
         * <code>200 + conversationId</code>, but the recipient sees the default Smallest
         * number rather than your own. For production traffic, either:</p>
         * <ul>
         * <li>pass <code>fromProductId</code> explicitly (look up your owned numbers via
         * <code>GET /product/phone-numbers</code>), or</li>
         * <li>attach a phone-number product to the agent.</li>
         * </ul>
         * <h2>Resolved-config check</h2>
         * <p>The call uses the agent's currently-active version. If your most
         * recent prompt change went through <code>PATCH /workflow/{workflowId}</code> and
         * the agent has versioning enabled, that change may not have
         * propagated to the active version — and the call will play the
         * platform-default greeting instead of your prompt. Before placing a
         * production call, fetch <code>GET /agent/{agentId}</code> and confirm
         * <code>_resolvedConfig.firstMessage</code> (and related fields) match what you
         * intended. The
         * <a href="/atoms/developer-guide/build/agents/versioning-lifecycle">Versioning Lifecycle</a>
         * guide covers the correct edit flow.</p>
         * <p><strong>400 is returned for:</strong></p>
         * <ul>
         * <li>Invalid <code>agentId</code> format (<code>&quot;Invalid agent id&quot;</code>)</li>
         * <li>Invalid <code>phoneNumber</code> format (<code>&quot;Invalid phone number&quot;</code>)</li>
         * <li>Invalid <code>fromProductId</code> format (<code>&quot;Invalid product id&quot;</code>)</li>
         * <li>Agent not found or not in the caller's org (<code>&quot;Agent not found&quot;</code>)</li>
         * <li>Agent is archived (<code>&quot;Agent is archived and cannot initiate calls&quot;</code>)</li>
         * <li><code>workflow_graph</code> agent has no workflow configured (<code>&quot;Workflow not found&quot;</code>)</li>
         * <li>Workflow has validation errors (<code>&quot;Invalid workflow, please fix the errors...&quot;</code>)</li>
         * </ul>
         * <p><strong>403</strong> is returned for <code>workflow_graph</code> agents when the org lacks conversational agents access.</p>
         * <p><strong>Test calls:</strong> set the <code>x-test-call: true</code> header to mark the resulting call log as a test call
         * (<code>isTest: true</code>). Test calls are subject to concurrent slot limits.</p>
         */
        public CompletableFuture<SmallestAIHttpResponse<StartOutboundCallCallsResponse>> startOutboundCall(
            StartOutboundCallCallsRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("conversation/outbound");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(Exception e) {
              throw new RuntimeException(e);
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json");
            if (request.getTestCall().isPresent()) {
              _requestBuilder.addHeader("x-test-call", request.getTestCall().get().toString());
            }
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            CompletableFuture<SmallestAIHttpResponse<StartOutboundCallCallsResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, StartOutboundCallCallsResponse.class), response));
                    return;
                  }
                  try {
                    switch (response.code()) {
                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (JsonProcessingException e) {
                  future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                }
                catch (IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            });
            return future;
          }
        }
