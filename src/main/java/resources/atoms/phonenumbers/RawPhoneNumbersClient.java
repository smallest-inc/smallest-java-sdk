/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.phonenumbers;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.MediaTypes;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.QueryStringMapper;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import ai.smallest.resources.atoms.phonenumbers.requests.ImportSipPhoneNumbersRequest;
import ai.smallest.resources.atoms.phonenumbers.requests.ReleasePhoneNumbersRequest;
import ai.smallest.resources.atoms.phonenumbers.requests.RentPhoneNumbersRequest;
import ai.smallest.resources.atoms.phonenumbers.requests.SearchRentablePhoneNumbersRequest;
import ai.smallest.resources.atoms.phonenumbers.types.GetProductAllNumbersResponse;
import ai.smallest.resources.atoms.phonenumbers.types.GetProductManageSubscriptionResponse;
import ai.smallest.resources.atoms.phonenumbers.types.GetProductProrationAmountResponse;
import ai.smallest.resources.atoms.phonenumbers.types.GetProductUnpaidInvoicesResponse;
import ai.smallest.resources.atoms.phonenumbers.types.ImportSipPhoneNumbersResponse;
import ai.smallest.resources.atoms.phonenumbers.types.ListPhoneNumbersResponse;
import ai.smallest.resources.atoms.phonenumbers.types.ReleasePhoneNumbersResponse;
import ai.smallest.resources.atoms.phonenumbers.types.RentPhoneNumbersResponse;
import ai.smallest.resources.atoms.phonenumbers.types.SearchRentablePhoneNumbersResponse;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawPhoneNumbersClient {
  protected final ClientOptions clientOptions;

  public RawPhoneNumbersClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve all platform-purchased telephony numbers (Twilio/Plivo) for the organization.
   * <p><strong>Note:</strong> Imported SIP numbers added via <code>POST /product/import-phone-number</code> are <strong>not</strong> included
   * in this response — they are stored as a separate product type and returned by a different internal call.</p>
   */
  public SmallestAIHttpResponse<ListPhoneNumbersResponse> list() {
    return list(null);
  }

  /**
   * Retrieve all platform-purchased telephony numbers (Twilio/Plivo) for the organization.
   * <p><strong>Note:</strong> Imported SIP numbers added via <code>POST /product/import-phone-number</code> are <strong>not</strong> included
   * in this response — they are stored as a separate product type and returned by a different internal call.</p>
   */
  public SmallestAIHttpResponse<ListPhoneNumbersResponse> list(RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("product/phone-numbers");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListPhoneNumbersResponse.class), response);
        }
        try {
          switch (response.code()) {
            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (JsonProcessingException e) {
        throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
      }
      catch (IOException e) {
        throw new SmallestAIException("Network error executing HTTP request", e);
      }
    }

    /**
     * Returns every phone number owned by the organization in one response:
     * <ul>
     * <li><code>telephonyProducts</code> — numbers rented via the Atoms platform (Plivo / Twilio).</li>
     * <li><code>customProducts</code> — numbers imported via <a href="#operation/importSipPhoneNumber"><code>POST /product/import-phone-number</code></a> with your own SIP trunks.</li>
     * </ul>
     * <p>Use this when you need a single combined view (e.g. a &quot;Pick a number&quot; dropdown). To list only platform-rented numbers, use <a href="#operation/getAcquiredPhoneNumbers"><code>GET /product/phone-numbers</code></a>.</p>
     */
    public SmallestAIHttpResponse<GetProductAllNumbersResponse> listAllPhoneNumbersPlatformSip() {
      return listAllPhoneNumbersPlatformSip(null);
    }

    /**
     * Returns every phone number owned by the organization in one response:
     * <ul>
     * <li><code>telephonyProducts</code> — numbers rented via the Atoms platform (Plivo / Twilio).</li>
     * <li><code>customProducts</code> — numbers imported via <a href="#operation/importSipPhoneNumber"><code>POST /product/import-phone-number</code></a> with your own SIP trunks.</li>
     * </ul>
     * <p>Use this when you need a single combined view (e.g. a &quot;Pick a number&quot; dropdown). To list only platform-rented numbers, use <a href="#operation/getAcquiredPhoneNumbers"><code>GET /product/phone-numbers</code></a>.</p>
     */
    public SmallestAIHttpResponse<GetProductAllNumbersResponse> listAllPhoneNumbersPlatformSip(
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("product/all-numbers");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetProductAllNumbersResponse.class), response);
          }
          try {
            switch (response.code()) {
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (JsonProcessingException e) {
          throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
        }
        catch (IOException e) {
          throw new SmallestAIException("Network error executing HTTP request", e);
        }
      }

      /**
       * Searches the telephony provider's inventory for available numbers matching the requested country (and optional area code). Returns up to 5 candidates per call.
       * <p>Use the returned <code>phoneNumber</code> value in <a href="#operation/rentPhoneNumber"><code>POST /product/rent-number</code></a> to actually rent it.</p>
       */
      public SmallestAIHttpResponse<SearchRentablePhoneNumbersResponse> searchRentable(
          SearchRentablePhoneNumbersRequest request) {
        return searchRentable(request,null);
      }

      /**
       * Searches the telephony provider's inventory for available numbers matching the requested country (and optional area code). Returns up to 5 candidates per call.
       * <p>Use the returned <code>phoneNumber</code> value in <a href="#operation/rentPhoneNumber"><code>POST /product/rent-number</code></a> to actually rent it.</p>
       */
      public SmallestAIHttpResponse<SearchRentablePhoneNumbersResponse> searchRentable(
          SearchRentablePhoneNumbersRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("product/get-available-numbers");QueryStringMapper.addQueryParameter(httpUrl, "countryCode", request.getCountryCode(), false);
          QueryStringMapper.addQueryParameter(httpUrl, "provider", request.getProvider(), false);
          if (request.getAreaCode().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "areaCode", request.getAreaCode().get(), false);
          }
          if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SearchRentablePhoneNumbersResponse.class), response);
            }
            try {
              switch (response.code()) {
                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (JsonProcessingException e) {
            throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
          }
          catch (IOException e) {
            throw new SmallestAIException("Network error executing HTTP request", e);
          }
        }

        /**
         * Returns the immediate prorated charge for renting one phone number from today through the end of the current billing cycle, plus the recurring monthly rate. Use this to show a &quot;you'll be charged $X today&quot; preview before calling <a href="#operation/rentPhoneNumber"><code>POST /product/rent-number</code></a>.
         * <p>Returns <code>400</code> if the organization doesn't have the phone-numbers feature configured (contact support) or if the org is currently locked (e.g. unpaid invoices — call <a href="#operation/getUnpaidInvoices"><code>GET /product/unpaid-invoices</code></a> first to check).</p>
         */
        public SmallestAIHttpResponse<GetProductProrationAmountResponse> previewProratedRentalCostForRentingAPhoneNumberToday(
            ) {
          return previewProratedRentalCostForRentingAPhoneNumberToday(null);
        }

        /**
         * Returns the immediate prorated charge for renting one phone number from today through the end of the current billing cycle, plus the recurring monthly rate. Use this to show a &quot;you'll be charged $X today&quot; preview before calling <a href="#operation/rentPhoneNumber"><code>POST /product/rent-number</code></a>.
         * <p>Returns <code>400</code> if the organization doesn't have the phone-numbers feature configured (contact support) or if the org is currently locked (e.g. unpaid invoices — call <a href="#operation/getUnpaidInvoices"><code>GET /product/unpaid-invoices</code></a> first to check).</p>
         */
        public SmallestAIHttpResponse<GetProductProrationAmountResponse> previewProratedRentalCostForRentingAPhoneNumberToday(
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("product/proration-amount");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("GET", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetProductProrationAmountResponse.class), response);
              }
              try {
                switch (response.code()) {
                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (JsonProcessingException e) {
              throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
            }
            catch (IOException e) {
              throw new SmallestAIException("Network error executing HTTP request", e);
            }
          }

          /**
           * Rents an available number returned by <a href="#operation/searchAvailablePhoneNumbers"><code>GET /product/get-available-numbers</code></a>. Charges the organization the prorated amount returned by <a href="#operation/getProrationAmount"><code>GET /product/proration-amount</code></a> immediately, then the monthly rate on each billing cycle.
           * <p>Always call <code>GET /product/proration-amount</code> first to surface the immediate charge to your customer. The endpoint may return <code>200</code> with a body containing <code>requiresAction: true</code> when payment requires customer interaction (3-D Secure, etc.) — handle that branch in your client.</p>
           * <p>Released later via <a href="#operation/releasePhoneNumber"><code>POST /product/release-number</code></a>.</p>
           */
          public SmallestAIHttpResponse<RentPhoneNumbersResponse> rent(
              RentPhoneNumbersRequest request) {
            return rent(request,null);
          }

          /**
           * Rents an available number returned by <a href="#operation/searchAvailablePhoneNumbers"><code>GET /product/get-available-numbers</code></a>. Charges the organization the prorated amount returned by <a href="#operation/getProrationAmount"><code>GET /product/proration-amount</code></a> immediately, then the monthly rate on each billing cycle.
           * <p>Always call <code>GET /product/proration-amount</code> first to surface the immediate charge to your customer. The endpoint may return <code>200</code> with a body containing <code>requiresAction: true</code> when payment requires customer interaction (3-D Secure, etc.) — handle that branch in your client.</p>
           * <p>Released later via <a href="#operation/releasePhoneNumber"><code>POST /product/release-number</code></a>.</p>
           */
          public SmallestAIHttpResponse<RentPhoneNumbersResponse> rent(
              RentPhoneNumbersRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

              .addPathSegments("product/rent-number");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              RequestBody body;
              try {
                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
              }
              catch(JsonProcessingException e) {
                throw new SmallestAIException("Failed to serialize request", e);
              }
              Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, RentPhoneNumbersResponse.class), response);
                }
                try {
                  switch (response.code()) {
                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (JsonProcessingException e) {
                throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
              }
              catch (IOException e) {
                throw new SmallestAIException("Network error executing HTTP request", e);
              }
            }

            /**
             * Releases a phone number previously rented via <code>POST /product/rent-number</code>. The number goes back into provider inventory and recurring charges stop.
             * <p>Returns <code>400</code> if the number is still assigned to an agent — detach it from the agent first (<code>PATCH /agent/{agentId}</code> with <code>productId: null</code>).</p>
             */
            public SmallestAIHttpResponse<ReleasePhoneNumbersResponse> release(
                ReleasePhoneNumbersRequest request) {
              return release(request,null);
            }

            /**
             * Releases a phone number previously rented via <code>POST /product/rent-number</code>. The number goes back into provider inventory and recurring charges stop.
             * <p>Returns <code>400</code> if the number is still assigned to an agent — detach it from the agent first (<code>PATCH /agent/{agentId}</code> with <code>productId: null</code>).</p>
             */
            public SmallestAIHttpResponse<ReleasePhoneNumbersResponse> release(
                ReleasePhoneNumbersRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                .addPathSegments("product/release-number");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                RequestBody body;
                try {
                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                }
                catch(JsonProcessingException e) {
                  throw new SmallestAIException("Failed to serialize request", e);
                }
                Request okhttpRequest = new Request.Builder()
                  .url(httpUrl.build())
                  .method("POST", body)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Content-Type", "application/json")
                  .addHeader("Accept", "application/json")
                  .build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                  okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ReleasePhoneNumbersResponse.class), response);
                  }
                  try {
                    switch (response.code()) {
                      case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (JsonProcessingException e) {
                  throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                }
                catch (IOException e) {
                  throw new SmallestAIException("Network error executing HTTP request", e);
                }
              }

              /**
               * Returns a time-limited Stripe Customer Portal URL the user can open to manage their subscription (update payment method, view invoices, etc.). Returns an empty object if the organization isn't on a Stripe-backed plan.
               */
              public SmallestAIHttpResponse<GetProductManageSubscriptionResponse> getStripeCustomerPortalUrl(
                  ) {
                return getStripeCustomerPortalUrl(null);
              }

              /**
               * Returns a time-limited Stripe Customer Portal URL the user can open to manage their subscription (update payment method, view invoices, etc.). Returns an empty object if the organization isn't on a Stripe-backed plan.
               */
              public SmallestAIHttpResponse<GetProductManageSubscriptionResponse> getStripeCustomerPortalUrl(
                  RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                  .addPathSegments("product/manage-subscription");if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  Request okhttpRequest = new Request.Builder()
                    .url(httpUrl.build())
                    .method("GET", null)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Accept", "application/json")
                    .build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                    okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                  }
                  try (Response response = client.newCall(okhttpRequest).execute()) {
                    ResponseBody responseBody = response.body();
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetProductManageSubscriptionResponse.class), response);
                    }
                    try {
                      switch (response.code()) {
                        case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                  }
                  catch (JsonProcessingException e) {
                    throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                  }
                  catch (IOException e) {
                    throw new SmallestAIException("Network error executing HTTP request", e);
                  }
                }

                /**
                 * Returns whether the org has unpaid invoices that would block destructive actions (renting numbers, etc.). Call this before any billable mutation to surface the &quot;Pay outstanding balance&quot; flow.
                 */
                public SmallestAIHttpResponse<GetProductUnpaidInvoicesResponse> checkWhetherTheOrganizationHasUnpaidInvoices(
                    ) {
                  return checkWhetherTheOrganizationHasUnpaidInvoices(null);
                }

                /**
                 * Returns whether the org has unpaid invoices that would block destructive actions (renting numbers, etc.). Call this before any billable mutation to surface the &quot;Pay outstanding balance&quot; flow.
                 */
                public SmallestAIHttpResponse<GetProductUnpaidInvoicesResponse> checkWhetherTheOrganizationHasUnpaidInvoices(
                    RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                    .addPathSegments("product/unpaid-invoices");if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    Request okhttpRequest = new Request.Builder()
                      .url(httpUrl.build())
                      .method("GET", null)
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Accept", "application/json")
                      .build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                      okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                    }
                    try (Response response = client.newCall(okhttpRequest).execute()) {
                      ResponseBody responseBody = response.body();
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetProductUnpaidInvoicesResponse.class), response);
                      }
                      try {
                        switch (response.code()) {
                          case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                          case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                    }
                    catch (JsonProcessingException e) {
                      throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                    }
                    catch (IOException e) {
                      throw new SmallestAIException("Network error executing HTTP request", e);
                    }
                  }

                  /**
                   * Bring your own SIP trunk by importing an existing phone number with its SIP termination URL.
                   * Atoms creates both inbound and outbound SIP trunks so your number works for making and receiving calls through the platform.
                   * <p>If <code>name</code> is omitted, a name is auto-generated from the phone number and user ID.</p>
                   */
                  public SmallestAIHttpResponse<ImportSipPhoneNumbersResponse> importSip(
                      ImportSipPhoneNumbersRequest request) {
                    return importSip(request,null);
                  }

                  /**
                   * Bring your own SIP trunk by importing an existing phone number with its SIP termination URL.
                   * Atoms creates both inbound and outbound SIP trunks so your number works for making and receiving calls through the platform.
                   * <p>If <code>name</code> is omitted, a name is auto-generated from the phone number and user ID.</p>
                   */
                  public SmallestAIHttpResponse<ImportSipPhoneNumbersResponse> importSip(
                      ImportSipPhoneNumbersRequest request, RequestOptions requestOptions) {
                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                      .addPathSegments("product/import-phone-number");if (requestOptions != null) {
                        requestOptions.getQueryParameters().forEach((_key, _value) -> {
                          httpUrl.addQueryParameter(_key, _value);
                        } );
                      }
                      RequestBody body;
                      try {
                        body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                      }
                      catch(JsonProcessingException e) {
                        throw new SmallestAIException("Failed to serialize request", e);
                      }
                      Request okhttpRequest = new Request.Builder()
                        .url(httpUrl.build())
                        .method("POST", body)
                        .headers(Headers.of(clientOptions.headers(requestOptions)))
                        .addHeader("Content-Type", "application/json")
                        .addHeader("Accept", "application/json")
                        .build();
                      OkHttpClient client = clientOptions.httpClient();
                      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                        client = clientOptions.httpClientWithTimeout(requestOptions);
                      }
                      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                      }
                      try (Response response = client.newCall(okhttpRequest).execute()) {
                        ResponseBody responseBody = response.body();
                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                        if (response.isSuccessful()) {
                          return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ImportSipPhoneNumbersResponse.class), response);
                        }
                        try {
                          switch (response.code()) {
                            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                          }
                        }
                        catch (JsonProcessingException ignored) {
                          // unable to map error response, throwing generic error
                        }
                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                        throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                      }
                      catch (JsonProcessingException e) {
                        throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                      }
                      catch (IOException e) {
                        throw new SmallestAIException("Network error executing HTTP request", e);
                      }
                    }
                  }
