/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Object;
import java.lang.String;

public final class TestCallRequestMode {
  public static final TestCallRequestMode TELEPHONY = new TestCallRequestMode(Value.TELEPHONY, "telephony");

  public static final TestCallRequestMode WEBCALL = new TestCallRequestMode(Value.WEBCALL, "webcall");

  public static final TestCallRequestMode CHAT = new TestCallRequestMode(Value.CHAT, "chat");

  private final Value value;

  private final String string;

  TestCallRequestMode(Value value, String string) {
    this.value = value;
    this.string = string;
  }

  public Value getEnumValue() {
    return value;
  }

  @java.lang.Override
  @JsonValue
  public String toString() {
    return this.string;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    return (this == other) 
      || (other instanceof TestCallRequestMode && this.string.equals(((TestCallRequestMode) other).string));
  }

  @java.lang.Override
  public int hashCode() {
    return this.string.hashCode();
  }

  public <T> T visit(Visitor<T> visitor) {
    switch (value) {
      case TELEPHONY:
        return visitor.visitTelephony();
      case WEBCALL:
        return visitor.visitWebcall();
      case CHAT:
        return visitor.visitChat();
      case UNKNOWN:
      default:
        return visitor.visitUnknown(string);
    }
  }

  @JsonCreator(
      mode = JsonCreator.Mode.DELEGATING
  )
  public static TestCallRequestMode valueOf(String value) {
    switch (value) {
      case "telephony":
        return TELEPHONY;
      case "webcall":
        return WEBCALL;
      case "chat":
        return CHAT;
      default:
        return new TestCallRequestMode(Value.UNKNOWN, value);
    }
  }

  public enum Value {
    WEBCALL,

    CHAT,

    TELEPHONY,

    UNKNOWN
  }

  public interface Visitor<T> {
    T visitWebcall();

    T visitChat();

    T visitTelephony();

    T visitUnknown(String unknownType);
  }
}
