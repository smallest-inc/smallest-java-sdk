/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.campaigns;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.RequestOptions;
import ai.smallest.resources.atoms.campaigns.requests.CreateCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.DeleteCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.GetCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.ListCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.PauseCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.StartOrResumeCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.types.CreateCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.DeleteCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.GetCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.ListCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.PauseCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.StartOrResumeCampaignsResponse;
import java.lang.String;
import java.util.concurrent.CompletableFuture;

public class AsyncCampaignsClient {
  protected final ClientOptions clientOptions;

  private final AsyncRawCampaignsClient rawClient;

  public AsyncCampaignsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new AsyncRawCampaignsClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public AsyncRawCampaignsClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public CompletableFuture<ListCampaignsResponse> list() {
    return this.rawClient.list().thenApply(response -> response.body());
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public CompletableFuture<ListCampaignsResponse> list(RequestOptions requestOptions) {
    return this.rawClient.list(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public CompletableFuture<ListCampaignsResponse> list(ListCampaignsRequest request) {
    return this.rawClient.list(request).thenApply(response -> response.body());
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public CompletableFuture<ListCampaignsResponse> list(ListCampaignsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.list(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Create a campaign
   */
  public CompletableFuture<CreateCampaignsResponse> create(CreateCampaignsRequest request) {
    return this.rawClient.create(request).thenApply(response -> response.body());
  }

  /**
   * Create a campaign
   */
  public CompletableFuture<CreateCampaignsResponse> create(CreateCampaignsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.create(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Get a campaign with detailed metrics
   */
  public CompletableFuture<GetCampaignsResponse> get(String id) {
    return this.rawClient.get(id).thenApply(response -> response.body());
  }

  /**
   * Get a campaign with detailed metrics
   */
  public CompletableFuture<GetCampaignsResponse> get(String id, RequestOptions requestOptions) {
    return this.rawClient.get(id, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Get a campaign with detailed metrics
   */
  public CompletableFuture<GetCampaignsResponse> get(String id, GetCampaignsRequest request) {
    return this.rawClient.get(id, request).thenApply(response -> response.body());
  }

  /**
   * Get a campaign with detailed metrics
   */
  public CompletableFuture<GetCampaignsResponse> get(String id, GetCampaignsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.get(id, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Delete a campaign
   */
  public CompletableFuture<DeleteCampaignsResponse> delete(String id) {
    return this.rawClient.delete(id).thenApply(response -> response.body());
  }

  /**
   * Delete a campaign
   */
  public CompletableFuture<DeleteCampaignsResponse> delete(String id,
      RequestOptions requestOptions) {
    return this.rawClient.delete(id, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Delete a campaign
   */
  public CompletableFuture<DeleteCampaignsResponse> delete(String id,
      DeleteCampaignsRequest request) {
    return this.rawClient.delete(id, request).thenApply(response -> response.body());
  }

  /**
   * Delete a campaign
   */
  public CompletableFuture<DeleteCampaignsResponse> delete(String id,
      DeleteCampaignsRequest request, RequestOptions requestOptions) {
    return this.rawClient.delete(id, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
   * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
   * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
   * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
   */
  public CompletableFuture<StartOrResumeCampaignsResponse> startOrResume(String id) {
    return this.rawClient.startOrResume(id).thenApply(response -> response.body());
  }

  /**
   * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
   * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
   * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
   * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
   */
  public CompletableFuture<StartOrResumeCampaignsResponse> startOrResume(String id,
      RequestOptions requestOptions) {
    return this.rawClient.startOrResume(id, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
   * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
   * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
   * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
   */
  public CompletableFuture<StartOrResumeCampaignsResponse> startOrResume(String id,
      StartOrResumeCampaignsRequest request) {
    return this.rawClient.startOrResume(id, request).thenApply(response -> response.body());
  }

  /**
   * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
   * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
   * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
   * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
   */
  public CompletableFuture<StartOrResumeCampaignsResponse> startOrResume(String id,
      StartOrResumeCampaignsRequest request, RequestOptions requestOptions) {
    return this.rawClient.startOrResume(id, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
   * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
   */
  public CompletableFuture<PauseCampaignsResponse> pause(String id) {
    return this.rawClient.pause(id).thenApply(response -> response.body());
  }

  /**
   * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
   * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
   */
  public CompletableFuture<PauseCampaignsResponse> pause(String id, RequestOptions requestOptions) {
    return this.rawClient.pause(id, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
   * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
   */
  public CompletableFuture<PauseCampaignsResponse> pause(String id, PauseCampaignsRequest request) {
    return this.rawClient.pause(id, request).thenApply(response -> response.body());
  }

  /**
   * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
   * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
   */
  public CompletableFuture<PauseCampaignsResponse> pause(String id, PauseCampaignsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.pause(id, request, requestOptions).thenApply(response -> response.body());
  }
}
