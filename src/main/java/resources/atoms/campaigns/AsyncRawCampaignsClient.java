/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.campaigns;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.MediaTypes;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.QueryStringMapper;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.campaigns.requests.CreateCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.DeleteCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.GetCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.ListCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.PauseCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.StartOrResumeCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.types.CreateCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.DeleteCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.GetCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.ListCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.PauseCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.StartOrResumeCampaignsResponse;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.ForbiddenError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.NotFoundError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawCampaignsClient {
  protected final ClientOptions clientOptions;

  public AsyncRawCampaignsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListCampaignsResponse>> list() {
    return list(ListCampaignsRequest.builder().build());
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListCampaignsResponse>> list(
      RequestOptions requestOptions) {
    return list(ListCampaignsRequest.builder().build(),requestOptions);
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListCampaignsResponse>> list(
      ListCampaignsRequest request) {
    return list(request,null);
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListCampaignsResponse>> list(
      ListCampaignsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("campaign");if (request.getPage().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
      }
      if (request.getOffset().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "offset", request.getOffset().get(), false);
      }
      if (request.getStatus().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "status", request.getStatus().get(), false);
      }
      if (request.getSearch().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "search", request.getSearch().get(), false);
      }
      if (request.getSortField().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sortField", request.getSortField().get(), false);
      }
      if (request.getSortOrder().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sortOrder", request.getSortOrder().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      CompletableFuture<SmallestAIHttpResponse<ListCampaignsResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListCampaignsResponse.class), response));
              return;
            }
            try {
              switch (response.code()) {
                case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (JsonProcessingException e) {
            future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
          }
          catch (IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Create a campaign
     */
    public CompletableFuture<SmallestAIHttpResponse<CreateCampaignsResponse>> create(
        CreateCampaignsRequest request) {
      return create(request,null);
    }

    /**
     * Create a campaign
     */
    public CompletableFuture<SmallestAIHttpResponse<CreateCampaignsResponse>> create(
        CreateCampaignsRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("campaign");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new SmallestAIException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        CompletableFuture<SmallestAIHttpResponse<CreateCampaignsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateCampaignsResponse.class), response));
                return;
              }
              try {
                switch (response.code()) {
                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (JsonProcessingException e) {
              future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
            }
            catch (IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Get a campaign with detailed metrics
       */
      public CompletableFuture<SmallestAIHttpResponse<GetCampaignsResponse>> get(String id) {
        return get(id,GetCampaignsRequest.builder().build());
      }

      /**
       * Get a campaign with detailed metrics
       */
      public CompletableFuture<SmallestAIHttpResponse<GetCampaignsResponse>> get(String id,
          RequestOptions requestOptions) {
        return get(id,GetCampaignsRequest.builder().build(),requestOptions);
      }

      /**
       * Get a campaign with detailed metrics
       */
      public CompletableFuture<SmallestAIHttpResponse<GetCampaignsResponse>> get(String id,
          GetCampaignsRequest request) {
        return get(id,request,null);
      }

      /**
       * Get a campaign with detailed metrics
       */
      public CompletableFuture<SmallestAIHttpResponse<GetCampaignsResponse>> get(String id,
          GetCampaignsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("campaign")
          .addPathSegment(id);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          CompletableFuture<SmallestAIHttpResponse<GetCampaignsResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetCampaignsResponse.class), response));
                  return;
                }
                try {
                  switch (response.code()) {
                    case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (JsonProcessingException e) {
                future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
              }
              catch (IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Delete a campaign
         */
        public CompletableFuture<SmallestAIHttpResponse<DeleteCampaignsResponse>> delete(
            String id) {
          return delete(id,DeleteCampaignsRequest.builder().build());
        }

        /**
         * Delete a campaign
         */
        public CompletableFuture<SmallestAIHttpResponse<DeleteCampaignsResponse>> delete(String id,
            RequestOptions requestOptions) {
          return delete(id,DeleteCampaignsRequest.builder().build(),requestOptions);
        }

        /**
         * Delete a campaign
         */
        public CompletableFuture<SmallestAIHttpResponse<DeleteCampaignsResponse>> delete(String id,
            DeleteCampaignsRequest request) {
          return delete(id,request,null);
        }

        /**
         * Delete a campaign
         */
        public CompletableFuture<SmallestAIHttpResponse<DeleteCampaignsResponse>> delete(String id,
            DeleteCampaignsRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("campaign")
            .addPathSegment(id);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("DELETE", null)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            CompletableFuture<SmallestAIHttpResponse<DeleteCampaignsResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeleteCampaignsResponse.class), response));
                    return;
                  }
                  try {
                    switch (response.code()) {
                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (JsonProcessingException e) {
                  future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                }
                catch (IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          /**
           * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
           * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
           * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
           * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
           */
          public CompletableFuture<SmallestAIHttpResponse<StartOrResumeCampaignsResponse>> startOrResume(
              String id) {
            return startOrResume(id,StartOrResumeCampaignsRequest.builder().build());
          }

          /**
           * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
           * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
           * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
           * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
           */
          public CompletableFuture<SmallestAIHttpResponse<StartOrResumeCampaignsResponse>> startOrResume(
              String id, RequestOptions requestOptions) {
            return startOrResume(id,StartOrResumeCampaignsRequest.builder().build(),requestOptions);
          }

          /**
           * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
           * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
           * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
           * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
           */
          public CompletableFuture<SmallestAIHttpResponse<StartOrResumeCampaignsResponse>> startOrResume(
              String id, StartOrResumeCampaignsRequest request) {
            return startOrResume(id,request,null);
          }

          /**
           * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
           * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
           * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
           * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
           */
          public CompletableFuture<SmallestAIHttpResponse<StartOrResumeCampaignsResponse>> startOrResume(
              String id, StartOrResumeCampaignsRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

              .addPathSegments("campaign")
              .addPathSegment(id)
              .addPathSegments("start");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
              }
              CompletableFuture<SmallestAIHttpResponse<StartOrResumeCampaignsResponse>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, StartOrResumeCampaignsResponse.class), response));
                      return;
                    }
                    try {
                      switch (response.code()) {
                        case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (JsonProcessingException e) {
                    future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              });
              return future;
            }

            /**
             * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
             * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
             */
            public CompletableFuture<SmallestAIHttpResponse<PauseCampaignsResponse>> pause(
                String id) {
              return pause(id,PauseCampaignsRequest.builder().build());
            }

            /**
             * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
             * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
             */
            public CompletableFuture<SmallestAIHttpResponse<PauseCampaignsResponse>> pause(
                String id, RequestOptions requestOptions) {
              return pause(id,PauseCampaignsRequest.builder().build(),requestOptions);
            }

            /**
             * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
             * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
             */
            public CompletableFuture<SmallestAIHttpResponse<PauseCampaignsResponse>> pause(
                String id, PauseCampaignsRequest request) {
              return pause(id,request,null);
            }

            /**
             * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
             * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
             */
            public CompletableFuture<SmallestAIHttpResponse<PauseCampaignsResponse>> pause(
                String id, PauseCampaignsRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                .addPathSegments("campaign")
                .addPathSegment(id)
                .addPathSegments("pause");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("POST", RequestBody.create("", null))
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Accept", "application/json");
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                  okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                }
                CompletableFuture<SmallestAIHttpResponse<PauseCampaignsResponse>> future = new CompletableFuture<>();
                client.newCall(okhttpRequest).enqueue(new Callback() {
                  @Override
                  public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                    try (ResponseBody responseBody = response.body()) {
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PauseCampaignsResponse.class), response));
                        return;
                      }
                      try {
                        switch (response.code()) {
                          case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                      return;
                    }
                    catch (JsonProcessingException e) {
                      future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                    }
                    catch (IOException e) {
                      future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                    }
                  }

                  @Override
                  public void onFailure(@NotNull Call call, @NotNull IOException e) {
                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                  }
                });
                return future;
              }
            }
