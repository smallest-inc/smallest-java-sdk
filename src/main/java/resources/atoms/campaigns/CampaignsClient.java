/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.campaigns;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.RequestOptions;
import ai.smallest.resources.atoms.campaigns.requests.CreateCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.DeleteCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.GetCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.ListCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.PauseCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.requests.StartOrResumeCampaignsRequest;
import ai.smallest.resources.atoms.campaigns.types.CreateCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.DeleteCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.GetCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.ListCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.PauseCampaignsResponse;
import ai.smallest.resources.atoms.campaigns.types.StartOrResumeCampaignsResponse;
import java.lang.String;

public class CampaignsClient {
  protected final ClientOptions clientOptions;

  private final RawCampaignsClient rawClient;

  public CampaignsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new RawCampaignsClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public RawCampaignsClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public ListCampaignsResponse list() {
    return this.rawClient.list().body();
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public ListCampaignsResponse list(RequestOptions requestOptions) {
    return this.rawClient.list(requestOptions).body();
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public ListCampaignsResponse list(ListCampaignsRequest request) {
    return this.rawClient.list(request).body();
  }

  /**
   * Get all campaigns for the authenticated organization.
   */
  public ListCampaignsResponse list(ListCampaignsRequest request, RequestOptions requestOptions) {
    return this.rawClient.list(request, requestOptions).body();
  }

  /**
   * Create a campaign
   */
  public CreateCampaignsResponse create(CreateCampaignsRequest request) {
    return this.rawClient.create(request).body();
  }

  /**
   * Create a campaign
   */
  public CreateCampaignsResponse create(CreateCampaignsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.create(request, requestOptions).body();
  }

  /**
   * Get a campaign with detailed metrics
   */
  public GetCampaignsResponse get(String id) {
    return this.rawClient.get(id).body();
  }

  /**
   * Get a campaign with detailed metrics
   */
  public GetCampaignsResponse get(String id, RequestOptions requestOptions) {
    return this.rawClient.get(id, requestOptions).body();
  }

  /**
   * Get a campaign with detailed metrics
   */
  public GetCampaignsResponse get(String id, GetCampaignsRequest request) {
    return this.rawClient.get(id, request).body();
  }

  /**
   * Get a campaign with detailed metrics
   */
  public GetCampaignsResponse get(String id, GetCampaignsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.get(id, request, requestOptions).body();
  }

  /**
   * Delete a campaign
   */
  public DeleteCampaignsResponse delete(String id) {
    return this.rawClient.delete(id).body();
  }

  /**
   * Delete a campaign
   */
  public DeleteCampaignsResponse delete(String id, RequestOptions requestOptions) {
    return this.rawClient.delete(id, requestOptions).body();
  }

  /**
   * Delete a campaign
   */
  public DeleteCampaignsResponse delete(String id, DeleteCampaignsRequest request) {
    return this.rawClient.delete(id, request).body();
  }

  /**
   * Delete a campaign
   */
  public DeleteCampaignsResponse delete(String id, DeleteCampaignsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.delete(id, request, requestOptions).body();
  }

  /**
   * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
   * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
   * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
   * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
   */
  public StartOrResumeCampaignsResponse startOrResume(String id) {
    return this.rawClient.startOrResume(id).body();
  }

  /**
   * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
   * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
   * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
   * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
   */
  public StartOrResumeCampaignsResponse startOrResume(String id, RequestOptions requestOptions) {
    return this.rawClient.startOrResume(id, requestOptions).body();
  }

  /**
   * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
   * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
   * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
   * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
   */
  public StartOrResumeCampaignsResponse startOrResume(String id,
      StartOrResumeCampaignsRequest request) {
    return this.rawClient.startOrResume(id, request).body();
  }

  /**
   * Queues the campaign for processing and returns immediately — the campaign is <strong>not</strong> yet
   * running when the 202 is returned. Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;running&quot;</code>.
   * <p>This endpoint also acts as a <strong>resume</strong> endpoint: if the campaign is currently paused,
   * calling this endpoint resumes it (<code>status</code> transitions from <code>paused</code> → <code>running</code>).</p>
   */
  public StartOrResumeCampaignsResponse startOrResume(String id,
      StartOrResumeCampaignsRequest request, RequestOptions requestOptions) {
    return this.rawClient.startOrResume(id, request, requestOptions).body();
  }

  /**
   * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
   * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
   */
  public PauseCampaignsResponse pause(String id) {
    return this.rawClient.pause(id).body();
  }

  /**
   * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
   * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
   */
  public PauseCampaignsResponse pause(String id, RequestOptions requestOptions) {
    return this.rawClient.pause(id, requestOptions).body();
  }

  /**
   * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
   * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
   */
  public PauseCampaignsResponse pause(String id, PauseCampaignsRequest request) {
    return this.rawClient.pause(id, request).body();
  }

  /**
   * Queues a pause task and returns immediately — the campaign is <strong>not</strong> immediately paused.
   * Poll <code>GET /campaign/{id}</code> and watch for <code>status: &quot;paused&quot;</code>.
   */
  public PauseCampaignsResponse pause(String id, PauseCampaignsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.pause(id, request, requestOptions).body();
  }
}
