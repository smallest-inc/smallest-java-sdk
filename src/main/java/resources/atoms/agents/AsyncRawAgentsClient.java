/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.agents;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.MediaTypes;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.QueryStringMapper;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.agents.requests.ArchiveAgentAgentsRequest;
import ai.smallest.resources.atoms.agents.requests.CreateAgentRequest;
import ai.smallest.resources.atoms.agents.requests.CreateAgentWithAiRequest;
import ai.smallest.resources.atoms.agents.requests.DuplicateAgentAgentsRequest;
import ai.smallest.resources.atoms.agents.requests.GetAgentAgentsRequest;
import ai.smallest.resources.atoms.agents.requests.GetAgentAvatarPresignedUrlRequest;
import ai.smallest.resources.atoms.agents.requests.GetAgentCallLogsRequest;
import ai.smallest.resources.atoms.agents.requests.GetAgentIdWorkflowRequest;
import ai.smallest.resources.atoms.agents.requests.GetAgentWidgetConfigRequest;
import ai.smallest.resources.atoms.agents.requests.ListAgentsAgentsRequest;
import ai.smallest.resources.atoms.agents.requests.UpdateAgentRequest;
import ai.smallest.resources.atoms.agents.requests.UpdateAgentWidgetConfigRequest;
import ai.smallest.resources.atoms.agents.requests.UpdateWorkflowConfigurationAgentsRequest;
import ai.smallest.resources.atoms.agents.types.ArchiveAgentAgentsResponse;
import ai.smallest.resources.atoms.agents.types.CreateAgentAgentsResponse;
import ai.smallest.resources.atoms.agents.types.CreateAgentWithAiResponse;
import ai.smallest.resources.atoms.agents.types.DuplicateAgentAgentsResponse;
import ai.smallest.resources.atoms.agents.types.GetAgentAgentsResponse;
import ai.smallest.resources.atoms.agents.types.GetAgentAvatarPresignedUrlResponse;
import ai.smallest.resources.atoms.agents.types.GetAgentCallLogsResponse;
import ai.smallest.resources.atoms.agents.types.GetAgentIdWorkflowResponse;
import ai.smallest.resources.atoms.agents.types.GetAgentPromptConfigResponse;
import ai.smallest.resources.atoms.agents.types.GetAgentWidgetConfigResponse;
import ai.smallest.resources.atoms.agents.types.ListAgentsAgentsResponse;
import ai.smallest.resources.atoms.agents.types.UpdateAgentAgentsResponse;
import ai.smallest.resources.atoms.agents.types.UpdateAgentWidgetConfigResponse;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.ConflictError;
import ai.smallest.resources.atoms.errors.ForbiddenError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.NotFoundError;
import ai.smallest.resources.atoms.errors.ServiceUnavailableError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import ai.smallest.resources.atoms.types.ApiResponse;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.lang.Void;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawAgentsClient {
  protected final ClientOptions clientOptions;

  public AsyncRawAgentsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Agents are the main entities in the system. Agents are used to create conversations. You can create workflow for an agent and configure it for different use cases. You can also create custom workflows for an agent. This API will give you the list of agents created by organization you are a part of.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListAgentsAgentsResponse>> listAgents() {
    return listAgents(ListAgentsAgentsRequest.builder().build());
  }

  /**
   * Agents are the main entities in the system. Agents are used to create conversations. You can create workflow for an agent and configure it for different use cases. You can also create custom workflows for an agent. This API will give you the list of agents created by organization you are a part of.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListAgentsAgentsResponse>> listAgents(
      RequestOptions requestOptions) {
    return listAgents(ListAgentsAgentsRequest.builder().build(),requestOptions);
  }

  /**
   * Agents are the main entities in the system. Agents are used to create conversations. You can create workflow for an agent and configure it for different use cases. You can also create custom workflows for an agent. This API will give you the list of agents created by organization you are a part of.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListAgentsAgentsResponse>> listAgents(
      ListAgentsAgentsRequest request) {
    return listAgents(request,null);
  }

  /**
   * Agents are the main entities in the system. Agents are used to create conversations. You can create workflow for an agent and configure it for different use cases. You can also create custom workflows for an agent. This API will give you the list of agents created by organization you are a part of.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListAgentsAgentsResponse>> listAgents(
      ListAgentsAgentsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("agent");if (request.getPage().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
      }
      if (request.getOffset().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "offset", request.getOffset().get(), false);
      }
      if (request.getSearch().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "search", request.getSearch().get(), false);
      }
      if (request.getType().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "type", request.getType().get(), false);
      }
      if (request.getSortField().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sortField", request.getSortField().get(), false);
      }
      if (request.getSortOrder().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sortOrder", request.getSortOrder().get(), false);
      }
      if (request.getArchived().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "archived", request.getArchived().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      CompletableFuture<SmallestAIHttpResponse<ListAgentsAgentsResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListAgentsAgentsResponse.class), response));
              return;
            }
            try {
              switch (response.code()) {
                case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (JsonProcessingException e) {
            future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
          }
          catch (IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Create a new agent by passing the agent name in the request body.
     * <p>New agents have versioning enabled by default. To set the prompt,
     * <code>firstMessage</code>, tools, or any runtime config, fork a draft from the
     * auto-created initial version, edit it, publish, and activate — see
     * the <a href="/atoms/developer-guide/build/agents/versioning-lifecycle">Versioning Lifecycle</a>
     * guide for the full flow.</p>
     * <p>The legacy <code>PATCH /workflow/{workflowId}</code> endpoint writes directly to
     * the underlying workflow document and bypasses the version lifecycle;
     * edits made that way are not captured as a version and may not
     * propagate to live calls. Use the drafts flow above.</p>
     */
    public CompletableFuture<SmallestAIHttpResponse<CreateAgentAgentsResponse>> createAgent(
        CreateAgentRequest request) {
      return createAgent(request,null);
    }

    /**
     * Create a new agent by passing the agent name in the request body.
     * <p>New agents have versioning enabled by default. To set the prompt,
     * <code>firstMessage</code>, tools, or any runtime config, fork a draft from the
     * auto-created initial version, edit it, publish, and activate — see
     * the <a href="/atoms/developer-guide/build/agents/versioning-lifecycle">Versioning Lifecycle</a>
     * guide for the full flow.</p>
     * <p>The legacy <code>PATCH /workflow/{workflowId}</code> endpoint writes directly to
     * the underlying workflow document and bypasses the version lifecycle;
     * edits made that way are not captured as a version and may not
     * propagate to live calls. Use the drafts flow above.</p>
     */
    public CompletableFuture<SmallestAIHttpResponse<CreateAgentAgentsResponse>> createAgent(
        CreateAgentRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("agent");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new SmallestAIException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        CompletableFuture<SmallestAIHttpResponse<CreateAgentAgentsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateAgentAgentsResponse.class), response));
                return;
              }
              try {
                switch (response.code()) {
                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (JsonProcessingException e) {
              future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
            }
            catch (IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Creates a new agent using AI generation. Accepts either a guided questionnaire (array of Q&amp;A pairs) or a free-text description. The AI generates the agent's prompt and configuration. A security check is run on the generated prompt before the agent is persisted. Deducts credits on success.
       * <p><strong>Rules:</strong></p>
       * <ul>
       * <li>Either <code>questions</code> (non-empty array) or <code>description</code> must be provided — not both.</li>
       * <li><code>emotiveToggle</code>, <code>voiceId</code>, and <code>voiceModel</code> must either all be present or all be absent.</li>
       * <li>When <code>emotiveToggle=true</code>, <code>voiceModel</code> must be <code>GPT_REALTIME</code>.</li>
       * <li>When <code>emotiveToggle=false</code>, <code>voiceModel</code> cannot be <code>GPT_REALTIME</code>.</li>
       * </ul>
       */
      public CompletableFuture<SmallestAIHttpResponse<CreateAgentWithAiResponse>> createAgentWithAi(
          ) {
        return createAgentWithAi(CreateAgentWithAiRequest.builder().build());
      }

      /**
       * Creates a new agent using AI generation. Accepts either a guided questionnaire (array of Q&amp;A pairs) or a free-text description. The AI generates the agent's prompt and configuration. A security check is run on the generated prompt before the agent is persisted. Deducts credits on success.
       * <p><strong>Rules:</strong></p>
       * <ul>
       * <li>Either <code>questions</code> (non-empty array) or <code>description</code> must be provided — not both.</li>
       * <li><code>emotiveToggle</code>, <code>voiceId</code>, and <code>voiceModel</code> must either all be present or all be absent.</li>
       * <li>When <code>emotiveToggle=true</code>, <code>voiceModel</code> must be <code>GPT_REALTIME</code>.</li>
       * <li>When <code>emotiveToggle=false</code>, <code>voiceModel</code> cannot be <code>GPT_REALTIME</code>.</li>
       * </ul>
       */
      public CompletableFuture<SmallestAIHttpResponse<CreateAgentWithAiResponse>> createAgentWithAi(
          RequestOptions requestOptions) {
        return createAgentWithAi(CreateAgentWithAiRequest.builder().build(),requestOptions);
      }

      /**
       * Creates a new agent using AI generation. Accepts either a guided questionnaire (array of Q&amp;A pairs) or a free-text description. The AI generates the agent's prompt and configuration. A security check is run on the generated prompt before the agent is persisted. Deducts credits on success.
       * <p><strong>Rules:</strong></p>
       * <ul>
       * <li>Either <code>questions</code> (non-empty array) or <code>description</code> must be provided — not both.</li>
       * <li><code>emotiveToggle</code>, <code>voiceId</code>, and <code>voiceModel</code> must either all be present or all be absent.</li>
       * <li>When <code>emotiveToggle=true</code>, <code>voiceModel</code> must be <code>GPT_REALTIME</code>.</li>
       * <li>When <code>emotiveToggle=false</code>, <code>voiceModel</code> cannot be <code>GPT_REALTIME</code>.</li>
       * </ul>
       */
      public CompletableFuture<SmallestAIHttpResponse<CreateAgentWithAiResponse>> createAgentWithAi(
          CreateAgentWithAiRequest request) {
        return createAgentWithAi(request,null);
      }

      /**
       * Creates a new agent using AI generation. Accepts either a guided questionnaire (array of Q&amp;A pairs) or a free-text description. The AI generates the agent's prompt and configuration. A security check is run on the generated prompt before the agent is persisted. Deducts credits on success.
       * <p><strong>Rules:</strong></p>
       * <ul>
       * <li>Either <code>questions</code> (non-empty array) or <code>description</code> must be provided — not both.</li>
       * <li><code>emotiveToggle</code>, <code>voiceId</code>, and <code>voiceModel</code> must either all be present or all be absent.</li>
       * <li>When <code>emotiveToggle=true</code>, <code>voiceModel</code> must be <code>GPT_REALTIME</code>.</li>
       * <li>When <code>emotiveToggle=false</code>, <code>voiceModel</code> cannot be <code>GPT_REALTIME</code>.</li>
       * </ul>
       */
      public CompletableFuture<SmallestAIHttpResponse<CreateAgentWithAiResponse>> createAgentWithAi(
          CreateAgentWithAiRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("agent/with-ai");if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(JsonProcessingException e) {
            throw new SmallestAIException("Failed to serialize request", e);
          }
          Request okhttpRequest = new Request.Builder()
            .url(httpUrl.build())
            .method("POST", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .addHeader("Accept", "application/json")
            .build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          CompletableFuture<SmallestAIHttpResponse<CreateAgentWithAiResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateAgentWithAiResponse.class), response));
                  return;
                }
                try {
                  switch (response.code()) {
                    case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 503:future.completeExceptionally(new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ApiResponse.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (JsonProcessingException e) {
                future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
              }
              catch (IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Duplicates a SINGLE_PROMPT agent's live active version into a target organization
         * (can also be the same organization). Copies all versioned configuration but strips
         * organization-specific resources: knowledge base tools are removed, default variable
         * values are blanked, and a new avatar is generated. The duplicate starts with a
         * published V1 as its active version.
         * <p><strong>400 is returned when:</strong></p>
         * <ul>
         * <li>The source agent is archived (<code>&quot;Cannot duplicate an archived agent&quot;</code>)</li>
         * <li>The agent has no <code>activeVersionId</code> (<code>&quot;This agent has no active version and cannot be duplicated&quot;</code>)</li>
         * <li>The active version exists but is not published/active (<code>&quot;This agent has no active published version and cannot be duplicated&quot;</code>)</li>
         * <li>The agent is not <code>SINGLE_PROMPT</code> workflow type</li>
         * </ul>
         */
        public CompletableFuture<SmallestAIHttpResponse<DuplicateAgentAgentsResponse>> duplicateAgent(
            String id, DuplicateAgentAgentsRequest request) {
          return duplicateAgent(id,request,null);
        }

        /**
         * Duplicates a SINGLE_PROMPT agent's live active version into a target organization
         * (can also be the same organization). Copies all versioned configuration but strips
         * organization-specific resources: knowledge base tools are removed, default variable
         * values are blanked, and a new avatar is generated. The duplicate starts with a
         * published V1 as its active version.
         * <p><strong>400 is returned when:</strong></p>
         * <ul>
         * <li>The source agent is archived (<code>&quot;Cannot duplicate an archived agent&quot;</code>)</li>
         * <li>The agent has no <code>activeVersionId</code> (<code>&quot;This agent has no active version and cannot be duplicated&quot;</code>)</li>
         * <li>The active version exists but is not published/active (<code>&quot;This agent has no active published version and cannot be duplicated&quot;</code>)</li>
         * <li>The agent is not <code>SINGLE_PROMPT</code> workflow type</li>
         * </ul>
         */
        public CompletableFuture<SmallestAIHttpResponse<DuplicateAgentAgentsResponse>> duplicateAgent(
            String id, DuplicateAgentAgentsRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("agent")
            .addPathSegment(id)
            .addPathSegments("duplicate");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new SmallestAIException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            CompletableFuture<SmallestAIHttpResponse<DuplicateAgentAgentsResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DuplicateAgentAgentsResponse.class), response));
                    return;
                  }
                  try {
                    switch (response.code()) {
                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (JsonProcessingException e) {
                  future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                }
                catch (IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          /**
           * Returns the agent document merged with the resolved config of the active version
           * under <code>_resolvedConfig</code>. Non-versioned fields (name, telephonyProductId, allowInboundCall,
           * etc.) sit at the top level; versioned fields (prompt, tools, language, synthesizer,
           * post-call analytics, …) are resolved from the target version and exposed under <code>_resolvedConfig</code>.
           * <p><strong>Previewing a draft or specific version</strong></p>
           * <p>Pass <code>?draftId=&lt;id&gt;</code> to resolve config from a specific draft instead of the active version.
           * Pass <code>?versionId=&lt;id&gt;</code> to resolve config from a specific published version.
           * When either param is used, the response includes <code>_configSource: &quot;draft&quot; | &quot;version&quot; | &quot;active&quot;</code>
           * indicating which config was resolved.</p>
           * <p>Notable resolved fields in <code>_resolvedConfig</code>:</p>
           * <ul>
           * <li><code>prompt</code> — active version's single-prompt text</li>
           * <li><code>tools</code> — configured tools on the resolved version</li>
           * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics model flags</li>
           * <li><code>modelName</code> — LLM model name on the resolved version</li>
           * <li><code>defaultLanguage</code>, <code>supportedLanguages</code> — active language config</li>
           * <li><code>firstMessage</code>, <code>globalPrompt</code> — active messaging config</li>
           * <li><code>workflowGraph</code> — full node graph for <code>workflow_graph</code> agents</li>
           * </ul>
           * <p>To read prompt + tools alone, use <code>GET /agent/{id}/workflow</code> (deprecated for
           * new integrations but still live). To inspect a specific non-active version,
           * use <code>GET /agent/{id}/versions/{versionId}</code>.</p>
           * <p><strong>400 — also used for &quot;not found&quot;:</strong> if the agent ID does not exist in the
           * organization, the API returns 400 with <code>errors: [&quot;No agent found&quot;]</code> rather than 404.</p>
           */
          public CompletableFuture<SmallestAIHttpResponse<GetAgentAgentsResponse>> getAgent(
              String id) {
            return getAgent(id,GetAgentAgentsRequest.builder().build());
          }

          /**
           * Returns the agent document merged with the resolved config of the active version
           * under <code>_resolvedConfig</code>. Non-versioned fields (name, telephonyProductId, allowInboundCall,
           * etc.) sit at the top level; versioned fields (prompt, tools, language, synthesizer,
           * post-call analytics, …) are resolved from the target version and exposed under <code>_resolvedConfig</code>.
           * <p><strong>Previewing a draft or specific version</strong></p>
           * <p>Pass <code>?draftId=&lt;id&gt;</code> to resolve config from a specific draft instead of the active version.
           * Pass <code>?versionId=&lt;id&gt;</code> to resolve config from a specific published version.
           * When either param is used, the response includes <code>_configSource: &quot;draft&quot; | &quot;version&quot; | &quot;active&quot;</code>
           * indicating which config was resolved.</p>
           * <p>Notable resolved fields in <code>_resolvedConfig</code>:</p>
           * <ul>
           * <li><code>prompt</code> — active version's single-prompt text</li>
           * <li><code>tools</code> — configured tools on the resolved version</li>
           * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics model flags</li>
           * <li><code>modelName</code> — LLM model name on the resolved version</li>
           * <li><code>defaultLanguage</code>, <code>supportedLanguages</code> — active language config</li>
           * <li><code>firstMessage</code>, <code>globalPrompt</code> — active messaging config</li>
           * <li><code>workflowGraph</code> — full node graph for <code>workflow_graph</code> agents</li>
           * </ul>
           * <p>To read prompt + tools alone, use <code>GET /agent/{id}/workflow</code> (deprecated for
           * new integrations but still live). To inspect a specific non-active version,
           * use <code>GET /agent/{id}/versions/{versionId}</code>.</p>
           * <p><strong>400 — also used for &quot;not found&quot;:</strong> if the agent ID does not exist in the
           * organization, the API returns 400 with <code>errors: [&quot;No agent found&quot;]</code> rather than 404.</p>
           */
          public CompletableFuture<SmallestAIHttpResponse<GetAgentAgentsResponse>> getAgent(
              String id, RequestOptions requestOptions) {
            return getAgent(id,GetAgentAgentsRequest.builder().build(),requestOptions);
          }

          /**
           * Returns the agent document merged with the resolved config of the active version
           * under <code>_resolvedConfig</code>. Non-versioned fields (name, telephonyProductId, allowInboundCall,
           * etc.) sit at the top level; versioned fields (prompt, tools, language, synthesizer,
           * post-call analytics, …) are resolved from the target version and exposed under <code>_resolvedConfig</code>.
           * <p><strong>Previewing a draft or specific version</strong></p>
           * <p>Pass <code>?draftId=&lt;id&gt;</code> to resolve config from a specific draft instead of the active version.
           * Pass <code>?versionId=&lt;id&gt;</code> to resolve config from a specific published version.
           * When either param is used, the response includes <code>_configSource: &quot;draft&quot; | &quot;version&quot; | &quot;active&quot;</code>
           * indicating which config was resolved.</p>
           * <p>Notable resolved fields in <code>_resolvedConfig</code>:</p>
           * <ul>
           * <li><code>prompt</code> — active version's single-prompt text</li>
           * <li><code>tools</code> — configured tools on the resolved version</li>
           * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics model flags</li>
           * <li><code>modelName</code> — LLM model name on the resolved version</li>
           * <li><code>defaultLanguage</code>, <code>supportedLanguages</code> — active language config</li>
           * <li><code>firstMessage</code>, <code>globalPrompt</code> — active messaging config</li>
           * <li><code>workflowGraph</code> — full node graph for <code>workflow_graph</code> agents</li>
           * </ul>
           * <p>To read prompt + tools alone, use <code>GET /agent/{id}/workflow</code> (deprecated for
           * new integrations but still live). To inspect a specific non-active version,
           * use <code>GET /agent/{id}/versions/{versionId}</code>.</p>
           * <p><strong>400 — also used for &quot;not found&quot;:</strong> if the agent ID does not exist in the
           * organization, the API returns 400 with <code>errors: [&quot;No agent found&quot;]</code> rather than 404.</p>
           */
          public CompletableFuture<SmallestAIHttpResponse<GetAgentAgentsResponse>> getAgent(
              String id, GetAgentAgentsRequest request) {
            return getAgent(id,request,null);
          }

          /**
           * Returns the agent document merged with the resolved config of the active version
           * under <code>_resolvedConfig</code>. Non-versioned fields (name, telephonyProductId, allowInboundCall,
           * etc.) sit at the top level; versioned fields (prompt, tools, language, synthesizer,
           * post-call analytics, …) are resolved from the target version and exposed under <code>_resolvedConfig</code>.
           * <p><strong>Previewing a draft or specific version</strong></p>
           * <p>Pass <code>?draftId=&lt;id&gt;</code> to resolve config from a specific draft instead of the active version.
           * Pass <code>?versionId=&lt;id&gt;</code> to resolve config from a specific published version.
           * When either param is used, the response includes <code>_configSource: &quot;draft&quot; | &quot;version&quot; | &quot;active&quot;</code>
           * indicating which config was resolved.</p>
           * <p>Notable resolved fields in <code>_resolvedConfig</code>:</p>
           * <ul>
           * <li><code>prompt</code> — active version's single-prompt text</li>
           * <li><code>tools</code> — configured tools on the resolved version</li>
           * <li><code>postCallAnalyticsConfig</code> — disposition metrics + analytics model flags</li>
           * <li><code>modelName</code> — LLM model name on the resolved version</li>
           * <li><code>defaultLanguage</code>, <code>supportedLanguages</code> — active language config</li>
           * <li><code>firstMessage</code>, <code>globalPrompt</code> — active messaging config</li>
           * <li><code>workflowGraph</code> — full node graph for <code>workflow_graph</code> agents</li>
           * </ul>
           * <p>To read prompt + tools alone, use <code>GET /agent/{id}/workflow</code> (deprecated for
           * new integrations but still live). To inspect a specific non-active version,
           * use <code>GET /agent/{id}/versions/{versionId}</code>.</p>
           * <p><strong>400 — also used for &quot;not found&quot;:</strong> if the agent ID does not exist in the
           * organization, the API returns 400 with <code>errors: [&quot;No agent found&quot;]</code> rather than 404.</p>
           */
          public CompletableFuture<SmallestAIHttpResponse<GetAgentAgentsResponse>> getAgent(
              String id, GetAgentAgentsRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

              .addPathSegments("agent")
              .addPathSegment(id);if (request.getDraftId().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "draftId", request.getDraftId().get(), false);
              }
              if (request.getVersionId().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "versionId", request.getVersionId().get(), false);
              }
              if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
              }
              CompletableFuture<SmallestAIHttpResponse<GetAgentAgentsResponse>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentAgentsResponse.class), response));
                      return;
                    }
                    try {
                      switch (response.code()) {
                        case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (JsonProcessingException e) {
                    future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              });
              return future;
            }

            /**
             * Update agent fields. Behavior depends on whether the agent has versioning enabled:
             * <p><strong>Versioned agents</strong> (have an active published version): only metadata fields are accepted —
             * <code>name</code>, <code>description</code>, <code>avatarUrl</code>, <code>telephonyProductId</code>, <code>allowInboundCall</code>, <code>visibleToEveryone</code>.
             * Submitting any config-level field returns 400 with
             * <code>&quot;Agent has versioning enabled. Config changes must be made through drafts.&quot;</code>.
             * Use <code>PATCH /agent/{id}/drafts/{draftId}/config</code> instead.</p>
             * <p><strong>Non-versioned agents</strong> (no active version): all configuration fields are accepted,
             * the same full set as <code>POST /agent</code>.</p>
             * <p><strong>400 is also returned when:</strong></p>
             * <ul>
             * <li>The agent is locked (<code>&quot;Agent is locked, please unlock it to update&quot;</code>)</li>
             * <li>Cross-field constraint violated (e.g. <code>north_indic</code> language requires <code>transcriberType: pulse</code>)</li>
             * </ul>
             * <p><strong>403</strong> is returned when selecting a gated model (<code>gpt-5.2</code>, <code>electron-kogta</code>, <code>electron-kogta-v2</code>)
             * without org-level access.</p>
             */
            public CompletableFuture<SmallestAIHttpResponse<UpdateAgentAgentsResponse>> updateAgent(
                String id) {
              return updateAgent(id,UpdateAgentRequest.builder().build());
            }

            /**
             * Update agent fields. Behavior depends on whether the agent has versioning enabled:
             * <p><strong>Versioned agents</strong> (have an active published version): only metadata fields are accepted —
             * <code>name</code>, <code>description</code>, <code>avatarUrl</code>, <code>telephonyProductId</code>, <code>allowInboundCall</code>, <code>visibleToEveryone</code>.
             * Submitting any config-level field returns 400 with
             * <code>&quot;Agent has versioning enabled. Config changes must be made through drafts.&quot;</code>.
             * Use <code>PATCH /agent/{id}/drafts/{draftId}/config</code> instead.</p>
             * <p><strong>Non-versioned agents</strong> (no active version): all configuration fields are accepted,
             * the same full set as <code>POST /agent</code>.</p>
             * <p><strong>400 is also returned when:</strong></p>
             * <ul>
             * <li>The agent is locked (<code>&quot;Agent is locked, please unlock it to update&quot;</code>)</li>
             * <li>Cross-field constraint violated (e.g. <code>north_indic</code> language requires <code>transcriberType: pulse</code>)</li>
             * </ul>
             * <p><strong>403</strong> is returned when selecting a gated model (<code>gpt-5.2</code>, <code>electron-kogta</code>, <code>electron-kogta-v2</code>)
             * without org-level access.</p>
             */
            public CompletableFuture<SmallestAIHttpResponse<UpdateAgentAgentsResponse>> updateAgent(
                String id, RequestOptions requestOptions) {
              return updateAgent(id,UpdateAgentRequest.builder().build(),requestOptions);
            }

            /**
             * Update agent fields. Behavior depends on whether the agent has versioning enabled:
             * <p><strong>Versioned agents</strong> (have an active published version): only metadata fields are accepted —
             * <code>name</code>, <code>description</code>, <code>avatarUrl</code>, <code>telephonyProductId</code>, <code>allowInboundCall</code>, <code>visibleToEveryone</code>.
             * Submitting any config-level field returns 400 with
             * <code>&quot;Agent has versioning enabled. Config changes must be made through drafts.&quot;</code>.
             * Use <code>PATCH /agent/{id}/drafts/{draftId}/config</code> instead.</p>
             * <p><strong>Non-versioned agents</strong> (no active version): all configuration fields are accepted,
             * the same full set as <code>POST /agent</code>.</p>
             * <p><strong>400 is also returned when:</strong></p>
             * <ul>
             * <li>The agent is locked (<code>&quot;Agent is locked, please unlock it to update&quot;</code>)</li>
             * <li>Cross-field constraint violated (e.g. <code>north_indic</code> language requires <code>transcriberType: pulse</code>)</li>
             * </ul>
             * <p><strong>403</strong> is returned when selecting a gated model (<code>gpt-5.2</code>, <code>electron-kogta</code>, <code>electron-kogta-v2</code>)
             * without org-level access.</p>
             */
            public CompletableFuture<SmallestAIHttpResponse<UpdateAgentAgentsResponse>> updateAgent(
                String id, UpdateAgentRequest request) {
              return updateAgent(id,request,null);
            }

            /**
             * Update agent fields. Behavior depends on whether the agent has versioning enabled:
             * <p><strong>Versioned agents</strong> (have an active published version): only metadata fields are accepted —
             * <code>name</code>, <code>description</code>, <code>avatarUrl</code>, <code>telephonyProductId</code>, <code>allowInboundCall</code>, <code>visibleToEveryone</code>.
             * Submitting any config-level field returns 400 with
             * <code>&quot;Agent has versioning enabled. Config changes must be made through drafts.&quot;</code>.
             * Use <code>PATCH /agent/{id}/drafts/{draftId}/config</code> instead.</p>
             * <p><strong>Non-versioned agents</strong> (no active version): all configuration fields are accepted,
             * the same full set as <code>POST /agent</code>.</p>
             * <p><strong>400 is also returned when:</strong></p>
             * <ul>
             * <li>The agent is locked (<code>&quot;Agent is locked, please unlock it to update&quot;</code>)</li>
             * <li>Cross-field constraint violated (e.g. <code>north_indic</code> language requires <code>transcriberType: pulse</code>)</li>
             * </ul>
             * <p><strong>403</strong> is returned when selecting a gated model (<code>gpt-5.2</code>, <code>electron-kogta</code>, <code>electron-kogta-v2</code>)
             * without org-level access.</p>
             */
            public CompletableFuture<SmallestAIHttpResponse<UpdateAgentAgentsResponse>> updateAgent(
                String id, UpdateAgentRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                .addPathSegments("agent")
                .addPathSegment(id);if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                RequestBody body;
                try {
                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                }
                catch(JsonProcessingException e) {
                  throw new SmallestAIException("Failed to serialize request", e);
                }
                Request okhttpRequest = new Request.Builder()
                  .url(httpUrl.build())
                  .method("PATCH", body)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Content-Type", "application/json")
                  .addHeader("Accept", "application/json")
                  .build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                  okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                }
                CompletableFuture<SmallestAIHttpResponse<UpdateAgentAgentsResponse>> future = new CompletableFuture<>();
                client.newCall(okhttpRequest).enqueue(new Callback() {
                  @Override
                  public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                    try (ResponseBody responseBody = response.body()) {
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateAgentAgentsResponse.class), response));
                        return;
                      }
                      try {
                        switch (response.code()) {
                          case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                      return;
                    }
                    catch (JsonProcessingException e) {
                      future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                    }
                    catch (IOException e) {
                      future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                    }
                  }

                  @Override
                  public void onFailure(@NotNull Call call, @NotNull IOException e) {
                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                  }
                });
                return future;
              }

              /**
               * Returns the questionnaire configuration used by the &quot;Create with AI&quot; flow — the list of guided questions, their types, available options, and pre-filled example answers per label. Only a user token is required; no organization auth needed.
               */
              public CompletableFuture<SmallestAIHttpResponse<GetAgentPromptConfigResponse>> getAgentPromptConfig(
                  ) {
                return getAgentPromptConfig(null);
              }

              /**
               * Returns the questionnaire configuration used by the &quot;Create with AI&quot; flow — the list of guided questions, their types, available options, and pre-filled example answers per label. Only a user token is required; no organization auth needed.
               */
              public CompletableFuture<SmallestAIHttpResponse<GetAgentPromptConfigResponse>> getAgentPromptConfig(
                  RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                  .addPathSegments("agent/prompt-config");if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  Request okhttpRequest = new Request.Builder()
                    .url(httpUrl.build())
                    .method("GET", null)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Accept", "application/json")
                    .build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                    okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                  }
                  CompletableFuture<SmallestAIHttpResponse<GetAgentPromptConfigResponse>> future = new CompletableFuture<>();
                  client.newCall(okhttpRequest).enqueue(new Callback() {
                    @Override
                    public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                      try (ResponseBody responseBody = response.body()) {
                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                        if (response.isSuccessful()) {
                          future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentPromptConfigResponse.class), response));
                          return;
                        }
                        try {
                          switch (response.code()) {
                            case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                            case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                          }
                        }
                        catch (JsonProcessingException ignored) {
                          // unable to map error response, throwing generic error
                        }
                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                        future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                        return;
                      }
                      catch (JsonProcessingException e) {
                        future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                      }
                      catch (IOException e) {
                        future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                      }
                    }

                    @Override
                    public void onFailure(@NotNull Call call, @NotNull IOException e) {
                      future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                    }
                  });
                  return future;
                }

                /**
                 * Returns the current web widget configuration for the agent. Also includes <code>assistantId</code> (same as the agent ID) as a convenience field for the widget embed code.
                 */
                public CompletableFuture<SmallestAIHttpResponse<GetAgentWidgetConfigResponse>> getAgentWidgetConfig(
                    String id) {
                  return getAgentWidgetConfig(id,GetAgentWidgetConfigRequest.builder().build());
                }

                /**
                 * Returns the current web widget configuration for the agent. Also includes <code>assistantId</code> (same as the agent ID) as a convenience field for the widget embed code.
                 */
                public CompletableFuture<SmallestAIHttpResponse<GetAgentWidgetConfigResponse>> getAgentWidgetConfig(
                    String id, RequestOptions requestOptions) {
                  return getAgentWidgetConfig(id,GetAgentWidgetConfigRequest.builder().build(),requestOptions);
                }

                /**
                 * Returns the current web widget configuration for the agent. Also includes <code>assistantId</code> (same as the agent ID) as a convenience field for the widget embed code.
                 */
                public CompletableFuture<SmallestAIHttpResponse<GetAgentWidgetConfigResponse>> getAgentWidgetConfig(
                    String id, GetAgentWidgetConfigRequest request) {
                  return getAgentWidgetConfig(id,request,null);
                }

                /**
                 * Returns the current web widget configuration for the agent. Also includes <code>assistantId</code> (same as the agent ID) as a convenience field for the widget embed code.
                 */
                public CompletableFuture<SmallestAIHttpResponse<GetAgentWidgetConfigResponse>> getAgentWidgetConfig(
                    String id, GetAgentWidgetConfigRequest request, RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                    .addPathSegments("agent")
                    .addPathSegment(id)
                    .addPathSegments("widget-config");if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    Request.Builder _requestBuilder = new Request.Builder()
                      .url(httpUrl.build())
                      .method("GET", null)
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Accept", "application/json");
                    Request okhttpRequest = _requestBuilder.build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                      okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                    }
                    CompletableFuture<SmallestAIHttpResponse<GetAgentWidgetConfigResponse>> future = new CompletableFuture<>();
                    client.newCall(okhttpRequest).enqueue(new Callback() {
                      @Override
                      public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                        try (ResponseBody responseBody = response.body()) {
                          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                          if (response.isSuccessful()) {
                            future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentWidgetConfigResponse.class), response));
                            return;
                          }
                          try {
                            switch (response.code()) {
                              case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                            }
                          }
                          catch (JsonProcessingException ignored) {
                            // unable to map error response, throwing generic error
                          }
                          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                          future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                          return;
                        }
                        catch (JsonProcessingException e) {
                          future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                        }
                        catch (IOException e) {
                          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                        }
                      }

                      @Override
                      public void onFailure(@NotNull Call call, @NotNull IOException e) {
                        future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                      }
                    });
                    return future;
                  }

                  /**
                   * Updates the web widget configuration for the agent. Only provided fields are updated (partial update). When <code>avatarUrl</code> is changed, the old CDN avatar is automatically deleted from S3. The <code>avatarUrl</code> must be a URL from the platform's CDN domain — use <code>POST /agent/{id}/avatar/presigned-url</code> to upload first.
                   */
                  public CompletableFuture<SmallestAIHttpResponse<UpdateAgentWidgetConfigResponse>> updateAgentWidgetConfig(
                      String id) {
                    return updateAgentWidgetConfig(id,UpdateAgentWidgetConfigRequest.builder().build());
                  }

                  /**
                   * Updates the web widget configuration for the agent. Only provided fields are updated (partial update). When <code>avatarUrl</code> is changed, the old CDN avatar is automatically deleted from S3. The <code>avatarUrl</code> must be a URL from the platform's CDN domain — use <code>POST /agent/{id}/avatar/presigned-url</code> to upload first.
                   */
                  public CompletableFuture<SmallestAIHttpResponse<UpdateAgentWidgetConfigResponse>> updateAgentWidgetConfig(
                      String id, RequestOptions requestOptions) {
                    return updateAgentWidgetConfig(id,UpdateAgentWidgetConfigRequest.builder().build(),requestOptions);
                  }

                  /**
                   * Updates the web widget configuration for the agent. Only provided fields are updated (partial update). When <code>avatarUrl</code> is changed, the old CDN avatar is automatically deleted from S3. The <code>avatarUrl</code> must be a URL from the platform's CDN domain — use <code>POST /agent/{id}/avatar/presigned-url</code> to upload first.
                   */
                  public CompletableFuture<SmallestAIHttpResponse<UpdateAgentWidgetConfigResponse>> updateAgentWidgetConfig(
                      String id, UpdateAgentWidgetConfigRequest request) {
                    return updateAgentWidgetConfig(id,request,null);
                  }

                  /**
                   * Updates the web widget configuration for the agent. Only provided fields are updated (partial update). When <code>avatarUrl</code> is changed, the old CDN avatar is automatically deleted from S3. The <code>avatarUrl</code> must be a URL from the platform's CDN domain — use <code>POST /agent/{id}/avatar/presigned-url</code> to upload first.
                   */
                  public CompletableFuture<SmallestAIHttpResponse<UpdateAgentWidgetConfigResponse>> updateAgentWidgetConfig(
                      String id, UpdateAgentWidgetConfigRequest request,
                      RequestOptions requestOptions) {
                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                      .addPathSegments("agent")
                      .addPathSegment(id)
                      .addPathSegments("widget-config");if (requestOptions != null) {
                        requestOptions.getQueryParameters().forEach((_key, _value) -> {
                          httpUrl.addQueryParameter(_key, _value);
                        } );
                      }
                      RequestBody body;
                      try {
                        body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                      }
                      catch(JsonProcessingException e) {
                        throw new SmallestAIException("Failed to serialize request", e);
                      }
                      Request okhttpRequest = new Request.Builder()
                        .url(httpUrl.build())
                        .method("PATCH", body)
                        .headers(Headers.of(clientOptions.headers(requestOptions)))
                        .addHeader("Content-Type", "application/json")
                        .addHeader("Accept", "application/json")
                        .build();
                      OkHttpClient client = clientOptions.httpClient();
                      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                        client = clientOptions.httpClientWithTimeout(requestOptions);
                      }
                      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                      }
                      CompletableFuture<SmallestAIHttpResponse<UpdateAgentWidgetConfigResponse>> future = new CompletableFuture<>();
                      client.newCall(okhttpRequest).enqueue(new Callback() {
                        @Override
                        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                          try (ResponseBody responseBody = response.body()) {
                            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                            if (response.isSuccessful()) {
                              future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, UpdateAgentWidgetConfigResponse.class), response));
                              return;
                            }
                            try {
                              switch (response.code()) {
                                case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                              }
                            }
                            catch (JsonProcessingException ignored) {
                              // unable to map error response, throwing generic error
                            }
                            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                            future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                            return;
                          }
                          catch (JsonProcessingException e) {
                            future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                          }
                          catch (IOException e) {
                            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                          }
                        }

                        @Override
                        public void onFailure(@NotNull Call call, @NotNull IOException e) {
                          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                        }
                      });
                      return future;
                    }

                    /**
                     * Generates a pre-signed S3 upload URL for the agent's widget avatar image. Upload the image directly to S3 using the returned <code>presignedUrl</code>, then save <code>cdnUrl</code> as the agent's avatar via <code>PATCH /agent/{id}/widget-config</code>.
                     */
                    public CompletableFuture<SmallestAIHttpResponse<GetAgentAvatarPresignedUrlResponse>> getAgentAvatarPresignedUrl(
                        String id, GetAgentAvatarPresignedUrlRequest request) {
                      return getAgentAvatarPresignedUrl(id,request,null);
                    }

                    /**
                     * Generates a pre-signed S3 upload URL for the agent's widget avatar image. Upload the image directly to S3 using the returned <code>presignedUrl</code>, then save <code>cdnUrl</code> as the agent's avatar via <code>PATCH /agent/{id}/widget-config</code>.
                     */
                    public CompletableFuture<SmallestAIHttpResponse<GetAgentAvatarPresignedUrlResponse>> getAgentAvatarPresignedUrl(
                        String id, GetAgentAvatarPresignedUrlRequest request,
                        RequestOptions requestOptions) {
                      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                        .addPathSegments("agent")
                        .addPathSegment(id)
                        .addPathSegments("avatar")
                        .addPathSegments("presigned-url");if (requestOptions != null) {
                          requestOptions.getQueryParameters().forEach((_key, _value) -> {
                            httpUrl.addQueryParameter(_key, _value);
                          } );
                        }
                        RequestBody body;
                        try {
                          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                        }
                        catch(JsonProcessingException e) {
                          throw new SmallestAIException("Failed to serialize request", e);
                        }
                        Request okhttpRequest = new Request.Builder()
                          .url(httpUrl.build())
                          .method("POST", body)
                          .headers(Headers.of(clientOptions.headers(requestOptions)))
                          .addHeader("Content-Type", "application/json")
                          .addHeader("Accept", "application/json")
                          .build();
                        OkHttpClient client = clientOptions.httpClient();
                        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                          client = clientOptions.httpClientWithTimeout(requestOptions);
                        }
                        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                        }
                        CompletableFuture<SmallestAIHttpResponse<GetAgentAvatarPresignedUrlResponse>> future = new CompletableFuture<>();
                        client.newCall(okhttpRequest).enqueue(new Callback() {
                          @Override
                          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                            try (ResponseBody responseBody = response.body()) {
                              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                              if (response.isSuccessful()) {
                                future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentAvatarPresignedUrlResponse.class), response));
                                return;
                              }
                              try {
                                switch (response.code()) {
                                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                  case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                  case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                  case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                }
                              }
                              catch (JsonProcessingException ignored) {
                                // unable to map error response, throwing generic error
                              }
                              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                              future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                              return;
                            }
                            catch (JsonProcessingException e) {
                              future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                            }
                            catch (IOException e) {
                              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                            }
                          }

                          @Override
                          public void onFailure(@NotNull Call call, @NotNull IOException e) {
                            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                          }
                        });
                        return future;
                      }

                      /**
                       * <strong>Deprecated</strong> — prefer <code>GET /agent/{id}</code> (config is resolved into <code>_resolvedConfig</code>
                       * including prompt, tools, and post-call analytics).
                       * <p>Returns the active version's prompt and tools for single-prompt agents, or the
                       * workflow graph data for workflow_graph agents. Customers still rely on this to
                       * fetch their current prompt + tools — endpoint is kept live for now.</p>
                       * <p><strong>Caveat:</strong> the <code>versionId</code> query param (if passed) is silently ignored.
                       * The response always reflects the currently-active version. To inspect a
                       * non-active version, use <code>GET /agent/{id}/versions/{versionId}</code>.</p>
                       */
                      public CompletableFuture<SmallestAIHttpResponse<GetAgentIdWorkflowResponse>> getAgentWorkflow(
                          String id) {
                        return getAgentWorkflow(id,GetAgentIdWorkflowRequest.builder().build());
                      }

                      /**
                       * <strong>Deprecated</strong> — prefer <code>GET /agent/{id}</code> (config is resolved into <code>_resolvedConfig</code>
                       * including prompt, tools, and post-call analytics).
                       * <p>Returns the active version's prompt and tools for single-prompt agents, or the
                       * workflow graph data for workflow_graph agents. Customers still rely on this to
                       * fetch their current prompt + tools — endpoint is kept live for now.</p>
                       * <p><strong>Caveat:</strong> the <code>versionId</code> query param (if passed) is silently ignored.
                       * The response always reflects the currently-active version. To inspect a
                       * non-active version, use <code>GET /agent/{id}/versions/{versionId}</code>.</p>
                       */
                      public CompletableFuture<SmallestAIHttpResponse<GetAgentIdWorkflowResponse>> getAgentWorkflow(
                          String id, RequestOptions requestOptions) {
                        return getAgentWorkflow(id,GetAgentIdWorkflowRequest.builder().build(),requestOptions);
                      }

                      /**
                       * <strong>Deprecated</strong> — prefer <code>GET /agent/{id}</code> (config is resolved into <code>_resolvedConfig</code>
                       * including prompt, tools, and post-call analytics).
                       * <p>Returns the active version's prompt and tools for single-prompt agents, or the
                       * workflow graph data for workflow_graph agents. Customers still rely on this to
                       * fetch their current prompt + tools — endpoint is kept live for now.</p>
                       * <p><strong>Caveat:</strong> the <code>versionId</code> query param (if passed) is silently ignored.
                       * The response always reflects the currently-active version. To inspect a
                       * non-active version, use <code>GET /agent/{id}/versions/{versionId}</code>.</p>
                       */
                      public CompletableFuture<SmallestAIHttpResponse<GetAgentIdWorkflowResponse>> getAgentWorkflow(
                          String id, GetAgentIdWorkflowRequest request) {
                        return getAgentWorkflow(id,request,null);
                      }

                      /**
                       * <strong>Deprecated</strong> — prefer <code>GET /agent/{id}</code> (config is resolved into <code>_resolvedConfig</code>
                       * including prompt, tools, and post-call analytics).
                       * <p>Returns the active version's prompt and tools for single-prompt agents, or the
                       * workflow graph data for workflow_graph agents. Customers still rely on this to
                       * fetch their current prompt + tools — endpoint is kept live for now.</p>
                       * <p><strong>Caveat:</strong> the <code>versionId</code> query param (if passed) is silently ignored.
                       * The response always reflects the currently-active version. To inspect a
                       * non-active version, use <code>GET /agent/{id}/versions/{versionId}</code>.</p>
                       */
                      public CompletableFuture<SmallestAIHttpResponse<GetAgentIdWorkflowResponse>> getAgentWorkflow(
                          String id, GetAgentIdWorkflowRequest request,
                          RequestOptions requestOptions) {
                        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                          .addPathSegments("agent")
                          .addPathSegment(id)
                          .addPathSegments("workflow");if (requestOptions != null) {
                            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                              httpUrl.addQueryParameter(_key, _value);
                            } );
                          }
                          Request.Builder _requestBuilder = new Request.Builder()
                            .url(httpUrl.build())
                            .method("GET", null)
                            .headers(Headers.of(clientOptions.headers(requestOptions)))
                            .addHeader("Accept", "application/json");
                          Request okhttpRequest = _requestBuilder.build();
                          OkHttpClient client = clientOptions.httpClient();
                          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                            client = clientOptions.httpClientWithTimeout(requestOptions);
                          }
                          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                          }
                          CompletableFuture<SmallestAIHttpResponse<GetAgentIdWorkflowResponse>> future = new CompletableFuture<>();
                          client.newCall(okhttpRequest).enqueue(new Callback() {
                            @Override
                            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                              try (ResponseBody responseBody = response.body()) {
                                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                if (response.isSuccessful()) {
                                  future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentIdWorkflowResponse.class), response));
                                  return;
                                }
                                try {
                                  switch (response.code()) {
                                    case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                    return;
                                    case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                    return;
                                    case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                    return;
                                  }
                                }
                                catch (JsonProcessingException ignored) {
                                  // unable to map error response, throwing generic error
                                }
                                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                return;
                              }
                              catch (JsonProcessingException e) {
                                future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                              }
                              catch (IOException e) {
                                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                              }
                            }

                            @Override
                            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                            }
                          });
                          return future;
                        }

                        /**
                         * <strong>Deprecated</strong> — use <code>PATCH /agent/{id}/drafts/{draftId}/config</code> instead.
                         * <p>Directly mutates the legacy workflow document for an agent. This write path
                         * bypasses the versioning system entirely: the change is not captured as a
                         * new version, and future version activations may overwrite the legacy doc
                         * back to whatever the version snapshot contains.</p>
                         * <p>⚠ <strong>Writing here on a versioned agent can silently wipe tools, prompt, or
                         * other fields that were missing from the PATCH payload.</strong> Only use this if
                         * you know the agent is not using versioning, or if you are intentionally
                         * hot-patching the legacy doc.</p>
                         */
                        public CompletableFuture<SmallestAIHttpResponse<Void>> updateWorkflowConfiguration(
                            String id, UpdateWorkflowConfigurationAgentsRequest request) {
                          return updateWorkflowConfiguration(id,request,null);
                        }

                        /**
                         * <strong>Deprecated</strong> — use <code>PATCH /agent/{id}/drafts/{draftId}/config</code> instead.
                         * <p>Directly mutates the legacy workflow document for an agent. This write path
                         * bypasses the versioning system entirely: the change is not captured as a
                         * new version, and future version activations may overwrite the legacy doc
                         * back to whatever the version snapshot contains.</p>
                         * <p>⚠ <strong>Writing here on a versioned agent can silently wipe tools, prompt, or
                         * other fields that were missing from the PATCH payload.</strong> Only use this if
                         * you know the agent is not using versioning, or if you are intentionally
                         * hot-patching the legacy doc.</p>
                         */
                        public CompletableFuture<SmallestAIHttpResponse<Void>> updateWorkflowConfiguration(
                            String id, UpdateWorkflowConfigurationAgentsRequest request,
                            RequestOptions requestOptions) {
                          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                            .addPathSegments("workflow")
                            .addPathSegment(id);if (requestOptions != null) {
                              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                httpUrl.addQueryParameter(_key, _value);
                              } );
                            }
                            RequestBody body;
                            try {
                              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                            }
                            catch(JsonProcessingException e) {
                              throw new SmallestAIException("Failed to serialize request", e);
                            }
                            Request okhttpRequest = new Request.Builder()
                              .url(httpUrl.build())
                              .method("PATCH", body)
                              .headers(Headers.of(clientOptions.headers(requestOptions)))
                              .addHeader("Content-Type", "application/json")
                              .addHeader("Accept", "application/json")
                              .build();
                            OkHttpClient client = clientOptions.httpClient();
                            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                              client = clientOptions.httpClientWithTimeout(requestOptions);
                            }
                            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                            }
                            CompletableFuture<SmallestAIHttpResponse<Void>> future = new CompletableFuture<>();
                            client.newCall(okhttpRequest).enqueue(new Callback() {
                              @Override
                              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                                try (ResponseBody responseBody = response.body()) {
                                  if (response.isSuccessful()) {
                                    future.complete(new SmallestAIHttpResponse<>(null, response));
                                    return;
                                  }
                                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                  try {
                                    switch (response.code()) {
                                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                      case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                      case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                      case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                    }
                                  }
                                  catch (JsonProcessingException ignored) {
                                    // unable to map error response, throwing generic error
                                  }
                                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                  future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                  return;
                                }
                                catch (JsonProcessingException e) {
                                  future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                                }
                                catch (IOException e) {
                                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                                }
                              }

                              @Override
                              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                              }
                            });
                            return future;
                          }

                          /**
                           * Returns paginated call logs for a specific agent.
                           */
                          public CompletableFuture<SmallestAIHttpResponse<GetAgentCallLogsResponse>> getAgentCallLogs(
                              String id) {
                            return getAgentCallLogs(id,GetAgentCallLogsRequest.builder().build());
                          }

                          /**
                           * Returns paginated call logs for a specific agent.
                           */
                          public CompletableFuture<SmallestAIHttpResponse<GetAgentCallLogsResponse>> getAgentCallLogs(
                              String id, RequestOptions requestOptions) {
                            return getAgentCallLogs(id,GetAgentCallLogsRequest.builder().build(),requestOptions);
                          }

                          /**
                           * Returns paginated call logs for a specific agent.
                           */
                          public CompletableFuture<SmallestAIHttpResponse<GetAgentCallLogsResponse>> getAgentCallLogs(
                              String id, GetAgentCallLogsRequest request) {
                            return getAgentCallLogs(id,request,null);
                          }

                          /**
                           * Returns paginated call logs for a specific agent.
                           */
                          public CompletableFuture<SmallestAIHttpResponse<GetAgentCallLogsResponse>> getAgentCallLogs(
                              String id, GetAgentCallLogsRequest request,
                              RequestOptions requestOptions) {
                            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                              .addPathSegments("agent")
                              .addPathSegment(id)
                              .addPathSegments("call-logs");if (request.getPage().isPresent()) {
                                QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
                              }
                              if (request.getOffset().isPresent()) {
                                QueryStringMapper.addQueryParameter(httpUrl, "offset", request.getOffset().get(), false);
                              }
                              if (requestOptions != null) {
                                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                  httpUrl.addQueryParameter(_key, _value);
                                } );
                              }
                              Request.Builder _requestBuilder = new Request.Builder()
                                .url(httpUrl.build())
                                .method("GET", null)
                                .headers(Headers.of(clientOptions.headers(requestOptions)))
                                .addHeader("Accept", "application/json");
                              Request okhttpRequest = _requestBuilder.build();
                              OkHttpClient client = clientOptions.httpClient();
                              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                                client = clientOptions.httpClientWithTimeout(requestOptions);
                              }
                              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                              }
                              CompletableFuture<SmallestAIHttpResponse<GetAgentCallLogsResponse>> future = new CompletableFuture<>();
                              client.newCall(okhttpRequest).enqueue(new Callback() {
                                @Override
                                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                                  try (ResponseBody responseBody = response.body()) {
                                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                    if (response.isSuccessful()) {
                                      future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAgentCallLogsResponse.class), response));
                                      return;
                                    }
                                    try {
                                      switch (response.code()) {
                                        case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                        return;
                                        case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                        return;
                                      }
                                    }
                                    catch (JsonProcessingException ignored) {
                                      // unable to map error response, throwing generic error
                                    }
                                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                    future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                    return;
                                  }
                                  catch (JsonProcessingException e) {
                                    future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                                  }
                                  catch (IOException e) {
                                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                                  }
                                }

                                @Override
                                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                                }
                              });
                              return future;
                            }

                            /**
                             * Soft-archives the agent — it is excluded from listings and stops accepting calls,
                             * but all data is preserved and the operation is fully reversible.
                             * <p>Pass <code>?on=false</code> to unarchive (restore) a previously archived agent.</p>
                             * <p><strong>409 is returned when:</strong></p>
                             * <ul>
                             * <li>The agent is already in the requested state (<code>&quot;Agent is already archived&quot;</code> / <code>&quot;Agent is already active&quot;</code>)</li>
                             * <li>The agent is linked to an active campaign (<code>&quot;Agent is associated with the [status] campaign &quot;[name]&quot;. Complete or remove the campaign before archiving.&quot;</code>)</li>
                             * </ul>
                             */
                            public CompletableFuture<SmallestAIHttpResponse<ArchiveAgentAgentsResponse>> archiveAgent(
                                String id) {
                              return archiveAgent(id,ArchiveAgentAgentsRequest.builder().build());
                            }

                            /**
                             * Soft-archives the agent — it is excluded from listings and stops accepting calls,
                             * but all data is preserved and the operation is fully reversible.
                             * <p>Pass <code>?on=false</code> to unarchive (restore) a previously archived agent.</p>
                             * <p><strong>409 is returned when:</strong></p>
                             * <ul>
                             * <li>The agent is already in the requested state (<code>&quot;Agent is already archived&quot;</code> / <code>&quot;Agent is already active&quot;</code>)</li>
                             * <li>The agent is linked to an active campaign (<code>&quot;Agent is associated with the [status] campaign &quot;[name]&quot;. Complete or remove the campaign before archiving.&quot;</code>)</li>
                             * </ul>
                             */
                            public CompletableFuture<SmallestAIHttpResponse<ArchiveAgentAgentsResponse>> archiveAgent(
                                String id, RequestOptions requestOptions) {
                              return archiveAgent(id,ArchiveAgentAgentsRequest.builder().build(),requestOptions);
                            }

                            /**
                             * Soft-archives the agent — it is excluded from listings and stops accepting calls,
                             * but all data is preserved and the operation is fully reversible.
                             * <p>Pass <code>?on=false</code> to unarchive (restore) a previously archived agent.</p>
                             * <p><strong>409 is returned when:</strong></p>
                             * <ul>
                             * <li>The agent is already in the requested state (<code>&quot;Agent is already archived&quot;</code> / <code>&quot;Agent is already active&quot;</code>)</li>
                             * <li>The agent is linked to an active campaign (<code>&quot;Agent is associated with the [status] campaign &quot;[name]&quot;. Complete or remove the campaign before archiving.&quot;</code>)</li>
                             * </ul>
                             */
                            public CompletableFuture<SmallestAIHttpResponse<ArchiveAgentAgentsResponse>> archiveAgent(
                                String id, ArchiveAgentAgentsRequest request) {
                              return archiveAgent(id,request,null);
                            }

                            /**
                             * Soft-archives the agent — it is excluded from listings and stops accepting calls,
                             * but all data is preserved and the operation is fully reversible.
                             * <p>Pass <code>?on=false</code> to unarchive (restore) a previously archived agent.</p>
                             * <p><strong>409 is returned when:</strong></p>
                             * <ul>
                             * <li>The agent is already in the requested state (<code>&quot;Agent is already archived&quot;</code> / <code>&quot;Agent is already active&quot;</code>)</li>
                             * <li>The agent is linked to an active campaign (<code>&quot;Agent is associated with the [status] campaign &quot;[name]&quot;. Complete or remove the campaign before archiving.&quot;</code>)</li>
                             * </ul>
                             */
                            public CompletableFuture<SmallestAIHttpResponse<ArchiveAgentAgentsResponse>> archiveAgent(
                                String id, ArchiveAgentAgentsRequest request,
                                RequestOptions requestOptions) {
                              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                                .addPathSegments("agent")
                                .addPathSegment(id)
                                .addPathSegments("archive");if (request.getOn().isPresent()) {
                                  QueryStringMapper.addQueryParameter(httpUrl, "on", request.getOn().get(), false);
                                }
                                if (requestOptions != null) {
                                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                    httpUrl.addQueryParameter(_key, _value);
                                  } );
                                }
                                Request.Builder _requestBuilder = new Request.Builder()
                                  .url(httpUrl.build())
                                  .method("DELETE", null)
                                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                                  .addHeader("Accept", "application/json");
                                Request okhttpRequest = _requestBuilder.build();
                                OkHttpClient client = clientOptions.httpClient();
                                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                                  client = clientOptions.httpClientWithTimeout(requestOptions);
                                }
                                if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                                  okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                                }
                                CompletableFuture<SmallestAIHttpResponse<ArchiveAgentAgentsResponse>> future = new CompletableFuture<>();
                                client.newCall(okhttpRequest).enqueue(new Callback() {
                                  @Override
                                  public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                                    try (ResponseBody responseBody = response.body()) {
                                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                      if (response.isSuccessful()) {
                                        future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ArchiveAgentAgentsResponse.class), response));
                                        return;
                                      }
                                      try {
                                        switch (response.code()) {
                                          case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                          return;
                                          case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                          return;
                                          case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                          return;
                                          case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                          return;
                                          case 409:future.completeExceptionally(new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                          return;
                                          case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                          return;
                                        }
                                      }
                                      catch (JsonProcessingException ignored) {
                                        // unable to map error response, throwing generic error
                                      }
                                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                      future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                      return;
                                    }
                                    catch (JsonProcessingException e) {
                                      future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                                    }
                                    catch (IOException e) {
                                      future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                                    }
                                  }

                                  @Override
                                  public void onFailure(@NotNull Call call, @NotNull IOException e) {
                                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                                  }
                                });
                                return future;
                              }
                            }
