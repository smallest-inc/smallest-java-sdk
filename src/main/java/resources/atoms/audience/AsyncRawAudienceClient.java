/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.audience;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.FileStream;
import ai.smallest.core.MediaTypes;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.QueryStringMapper;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.audience.requests.DeleteAudienceIdMembersRequest;
import ai.smallest.resources.atoms.audience.requests.DeleteAudienceIdRequest;
import ai.smallest.resources.atoms.audience.requests.GetAudienceIdMembersRequest;
import ai.smallest.resources.atoms.audience.requests.GetAudienceIdMembersSearchRequest;
import ai.smallest.resources.atoms.audience.requests.GetAudienceRequest;
import ai.smallest.resources.atoms.audience.requests.PostAudienceIdMembersRequest;
import ai.smallest.resources.atoms.audience.requests.PostAudienceRequest;
import ai.smallest.resources.atoms.audience.types.DeleteAudienceIdMembersResponse;
import ai.smallest.resources.atoms.audience.types.DeleteAudienceIdResponse;
import ai.smallest.resources.atoms.audience.types.GetAudienceIdMembersResponse;
import ai.smallest.resources.atoms.audience.types.GetAudienceIdMembersSearchResponse;
import ai.smallest.resources.atoms.audience.types.GetAudienceResponse;
import ai.smallest.resources.atoms.audience.types.ListAudienceResponse;
import ai.smallest.resources.atoms.audience.types.PostAudienceIdMembersResponse;
import ai.smallest.resources.atoms.audience.types.PostAudienceResponse;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.ForbiddenError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.NotFoundError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.File;
import java.io.IOException;
import java.io.InputStream;
import java.lang.Exception;
import java.lang.Object;
import java.lang.Override;
import java.lang.RuntimeException;
import java.lang.String;
import java.nio.file.Files;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.MediaType;
import okhttp3.MultipartBody;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawAudienceClient {
  protected final ClientOptions clientOptions;

  public AsyncRawAudienceClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Retrieve all audiences created by the authenticated user. Users can only access audiences they have created.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListAudienceResponse>> list() {
    return list(null);
  }

  /**
   * Retrieve all audiences created by the authenticated user. Users can only access audiences they have created.
   */
  public CompletableFuture<SmallestAIHttpResponse<ListAudienceResponse>> list(
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("audience");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      CompletableFuture<SmallestAIHttpResponse<ListAudienceResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListAudienceResponse.class), response));
              return;
            }
            try {
              switch (response.code()) {
                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (JsonProcessingException e) {
            future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
          }
          catch (IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Create a new audience by uploading a CSV file containing phone numbers.
     * Only CSV text files are accepted — binary files will produce malformed data.
     * <p><strong>Additional 400 cases:</strong></p>
     * <ul>
     * <li>Duplicate phone numbers in the CSV: <code>&quot;Some phone numbers in your CSV already exist in this audience. Please remove duplicate entries and try again.&quot;</code></li>
     * <li>Member limit exceeded: <code>&quot;Audience cannot exceed X members&quot;</code></li>
     * </ul>
     */
    public CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> createAudienceWithCsvUpload(
        File file, PostAudienceRequest request) {
      return createAudienceWithCsvUpload(file,request,null);
    }

    /**
     * Create a new audience by uploading a CSV file containing phone numbers.
     * Only CSV text files are accepted — binary files will produce malformed data.
     * <p><strong>Additional 400 cases:</strong></p>
     * <ul>
     * <li>Duplicate phone numbers in the CSV: <code>&quot;Some phone numbers in your CSV already exist in this audience. Please remove duplicate entries and try again.&quot;</code></li>
     * <li>Member limit exceeded: <code>&quot;Audience cannot exceed X members&quot;</code></li>
     * </ul>
     */
    public CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> createAudienceWithCsvUpload(
        File file, PostAudienceRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("audience");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
        try {
          multipartBodyBuilder.addFormDataPart("name", request.getName());
          if (request.getDescription().isPresent()) {
            multipartBodyBuilder.addFormDataPart("description", request.getDescription().get());
          }
          multipartBodyBuilder.addFormDataPart("phoneNumberColumnName", request.getPhoneNumberColumnName());
          if (request.getIdentifierColumnName().isPresent()) {
            multipartBodyBuilder.addFormDataPart("identifierColumnName", request.getIdentifierColumnName().get());
          }
          String fileMimeType = Files.probeContentType(file.toPath());
          MediaType fileMimeTypeMediaType = fileMimeType != null ? MediaType.parse(fileMimeType) : null;
          multipartBodyBuilder.addFormDataPart("file", file.getName(), RequestBody.create(file, fileMimeTypeMediaType));
        }
        catch(Exception e) {
          throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", multipartBodyBuilder.build())
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostAudienceResponse.class), response));
                return;
              }
              try {
                switch (response.code()) {
                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (JsonProcessingException e) {
              future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
            }
            catch (IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      public CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> createAudienceWithCsvUpload(
          InputStream stream, String filename) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("audience");FileStream fs = new FileStream(stream, filename, null);
          MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
          multipartBodyBuilder.addFormDataPart("file", filename, fs.toRequestBody());
          RequestBody body = multipartBodyBuilder.build();
          Request.Builder _requestBuilder = new Request.Builder();
          _requestBuilder.url(httpUrl.build());
          _requestBuilder.method("POST", body);
          _requestBuilder.headers(Headers.of(this.clientOptions.headers((RequestOptions) null)));
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostAudienceResponse.class), response));
                  return;
                }
                try {
                  switch (response.code()) {
                    case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (JsonProcessingException e) {
                future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
              }
              catch (IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        public CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> createAudienceWithCsvUpload(
            InputStream stream, String filename, MediaType mediaType) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("audience");FileStream fs = new FileStream(stream, filename, mediaType);
            MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
            multipartBodyBuilder.addFormDataPart("file", filename, fs.toRequestBody());
            RequestBody body = multipartBodyBuilder.build();
            Request.Builder _requestBuilder = new Request.Builder();
            _requestBuilder.url(httpUrl.build());
            _requestBuilder.method("POST", body);
            _requestBuilder.headers(Headers.of(this.clientOptions.headers((RequestOptions) null)));
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostAudienceResponse.class), response));
                    return;
                  }
                  try {
                    switch (response.code()) {
                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (JsonProcessingException e) {
                  future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                }
                catch (IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          public CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> createAudienceWithCsvUpload(
              InputStream stream, String filename, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

              .addPathSegments("audience");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              FileStream fs = new FileStream(stream, filename, null);
              MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
              multipartBodyBuilder.addFormDataPart("file", filename, fs.toRequestBody());
              RequestBody body = multipartBodyBuilder.build();
              Request.Builder _requestBuilder = new Request.Builder();
              _requestBuilder.url(httpUrl.build());
              _requestBuilder.method("POST", body);
              _requestBuilder.headers(Headers.of(this.clientOptions.headers(requestOptions)));
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
              }
              CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostAudienceResponse.class), response));
                      return;
                    }
                    try {
                      switch (response.code()) {
                        case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (JsonProcessingException e) {
                    future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              });
              return future;
            }

            public CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> createAudienceWithCsvUpload(
                InputStream stream, String filename, MediaType mediaType,
                RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                .addPathSegments("audience");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                FileStream fs = new FileStream(stream, filename, mediaType);
                MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
                multipartBodyBuilder.addFormDataPart("file", filename, fs.toRequestBody());
                RequestBody body = multipartBodyBuilder.build();
                Request.Builder _requestBuilder = new Request.Builder();
                _requestBuilder.url(httpUrl.build());
                _requestBuilder.method("POST", body);
                _requestBuilder.headers(Headers.of(this.clientOptions.headers(requestOptions)));
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                  okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                }
                CompletableFuture<SmallestAIHttpResponse<PostAudienceResponse>> future = new CompletableFuture<>();
                client.newCall(okhttpRequest).enqueue(new Callback() {
                  @Override
                  public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                    try (ResponseBody responseBody = response.body()) {
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostAudienceResponse.class), response));
                        return;
                      }
                      try {
                        switch (response.code()) {
                          case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                          case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                          return;
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                      return;
                    }
                    catch (JsonProcessingException e) {
                      future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                    }
                    catch (IOException e) {
                      future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                    }
                  }

                  @Override
                  public void onFailure(@NotNull Call call, @NotNull IOException e) {
                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                  }
                });
                return future;
              }

              /**
               * Retrieve a specific audience by its ID.
               * Note: if the audience belongs to a different organization, the API returns 404 (not 403) — ownership is deliberately obscured.
               */
              public CompletableFuture<SmallestAIHttpResponse<GetAudienceResponse>> get(String id) {
                return get(id,GetAudienceRequest.builder().build());
              }

              /**
               * Retrieve a specific audience by its ID.
               * Note: if the audience belongs to a different organization, the API returns 404 (not 403) — ownership is deliberately obscured.
               */
              public CompletableFuture<SmallestAIHttpResponse<GetAudienceResponse>> get(String id,
                  RequestOptions requestOptions) {
                return get(id,GetAudienceRequest.builder().build(),requestOptions);
              }

              /**
               * Retrieve a specific audience by its ID.
               * Note: if the audience belongs to a different organization, the API returns 404 (not 403) — ownership is deliberately obscured.
               */
              public CompletableFuture<SmallestAIHttpResponse<GetAudienceResponse>> get(String id,
                  GetAudienceRequest request) {
                return get(id,request,null);
              }

              /**
               * Retrieve a specific audience by its ID.
               * Note: if the audience belongs to a different organization, the API returns 404 (not 403) — ownership is deliberately obscured.
               */
              public CompletableFuture<SmallestAIHttpResponse<GetAudienceResponse>> get(String id,
                  GetAudienceRequest request, RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                  .addPathSegments("audience")
                  .addPathSegment(id);if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  Request.Builder _requestBuilder = new Request.Builder()
                    .url(httpUrl.build())
                    .method("GET", null)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Accept", "application/json");
                  Request okhttpRequest = _requestBuilder.build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                    okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                  }
                  CompletableFuture<SmallestAIHttpResponse<GetAudienceResponse>> future = new CompletableFuture<>();
                  client.newCall(okhttpRequest).enqueue(new Callback() {
                    @Override
                    public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                      try (ResponseBody responseBody = response.body()) {
                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                        if (response.isSuccessful()) {
                          future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAudienceResponse.class), response));
                          return;
                        }
                        try {
                          switch (response.code()) {
                            case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                            case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                            case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                            case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                            return;
                          }
                        }
                        catch (JsonProcessingException ignored) {
                          // unable to map error response, throwing generic error
                        }
                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                        future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                        return;
                      }
                      catch (JsonProcessingException e) {
                        future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                      }
                      catch (IOException e) {
                        future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                      }
                    }

                    @Override
                    public void onFailure(@NotNull Call call, @NotNull IOException e) {
                      future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                    }
                  });
                  return future;
                }

                /**
                 * Delete a specific audience by its ID. Users can only delete audiences they created.
                 * <p><strong>400 is returned if the audience is used by an active campaign:</strong>
                 * <code>&quot;can't delete audience, campaign with this audience &lt;id&gt; exists&quot;</code>
                 * Remove the campaign first, then retry deletion.</p>
                 * <p>On success, <code>data</code> is always an empty array <code>[]</code>.</p>
                 */
                public CompletableFuture<SmallestAIHttpResponse<DeleteAudienceIdResponse>> deleteAudience(
                    String id) {
                  return deleteAudience(id,DeleteAudienceIdRequest.builder().build());
                }

                /**
                 * Delete a specific audience by its ID. Users can only delete audiences they created.
                 * <p><strong>400 is returned if the audience is used by an active campaign:</strong>
                 * <code>&quot;can't delete audience, campaign with this audience &lt;id&gt; exists&quot;</code>
                 * Remove the campaign first, then retry deletion.</p>
                 * <p>On success, <code>data</code> is always an empty array <code>[]</code>.</p>
                 */
                public CompletableFuture<SmallestAIHttpResponse<DeleteAudienceIdResponse>> deleteAudience(
                    String id, RequestOptions requestOptions) {
                  return deleteAudience(id,DeleteAudienceIdRequest.builder().build(),requestOptions);
                }

                /**
                 * Delete a specific audience by its ID. Users can only delete audiences they created.
                 * <p><strong>400 is returned if the audience is used by an active campaign:</strong>
                 * <code>&quot;can't delete audience, campaign with this audience &lt;id&gt; exists&quot;</code>
                 * Remove the campaign first, then retry deletion.</p>
                 * <p>On success, <code>data</code> is always an empty array <code>[]</code>.</p>
                 */
                public CompletableFuture<SmallestAIHttpResponse<DeleteAudienceIdResponse>> deleteAudience(
                    String id, DeleteAudienceIdRequest request) {
                  return deleteAudience(id,request,null);
                }

                /**
                 * Delete a specific audience by its ID. Users can only delete audiences they created.
                 * <p><strong>400 is returned if the audience is used by an active campaign:</strong>
                 * <code>&quot;can't delete audience, campaign with this audience &lt;id&gt; exists&quot;</code>
                 * Remove the campaign first, then retry deletion.</p>
                 * <p>On success, <code>data</code> is always an empty array <code>[]</code>.</p>
                 */
                public CompletableFuture<SmallestAIHttpResponse<DeleteAudienceIdResponse>> deleteAudience(
                    String id, DeleteAudienceIdRequest request, RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                    .addPathSegments("audience")
                    .addPathSegment(id);if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    Request.Builder _requestBuilder = new Request.Builder()
                      .url(httpUrl.build())
                      .method("DELETE", null)
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Accept", "application/json");
                    Request okhttpRequest = _requestBuilder.build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                      okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                    }
                    CompletableFuture<SmallestAIHttpResponse<DeleteAudienceIdResponse>> future = new CompletableFuture<>();
                    client.newCall(okhttpRequest).enqueue(new Callback() {
                      @Override
                      public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                        try (ResponseBody responseBody = response.body()) {
                          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                          if (response.isSuccessful()) {
                            future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeleteAudienceIdResponse.class), response));
                            return;
                          }
                          try {
                            switch (response.code()) {
                              case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                              case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                              return;
                            }
                          }
                          catch (JsonProcessingException ignored) {
                            // unable to map error response, throwing generic error
                          }
                          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                          future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                          return;
                        }
                        catch (JsonProcessingException e) {
                          future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                        }
                        catch (IOException e) {
                          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                        }
                      }

                      @Override
                      public void onFailure(@NotNull Call call, @NotNull IOException e) {
                        future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                      }
                    });
                    return future;
                  }

                  /**
                   * Retrieve members of a specific audience with pagination support. Users can only access members of audiences they created.
                   */
                  public CompletableFuture<SmallestAIHttpResponse<GetAudienceIdMembersResponse>> getAudienceMembers(
                      String id) {
                    return getAudienceMembers(id,GetAudienceIdMembersRequest.builder().build());
                  }

                  /**
                   * Retrieve members of a specific audience with pagination support. Users can only access members of audiences they created.
                   */
                  public CompletableFuture<SmallestAIHttpResponse<GetAudienceIdMembersResponse>> getAudienceMembers(
                      String id, RequestOptions requestOptions) {
                    return getAudienceMembers(id,GetAudienceIdMembersRequest.builder().build(),requestOptions);
                  }

                  /**
                   * Retrieve members of a specific audience with pagination support. Users can only access members of audiences they created.
                   */
                  public CompletableFuture<SmallestAIHttpResponse<GetAudienceIdMembersResponse>> getAudienceMembers(
                      String id, GetAudienceIdMembersRequest request) {
                    return getAudienceMembers(id,request,null);
                  }

                  /**
                   * Retrieve members of a specific audience with pagination support. Users can only access members of audiences they created.
                   */
                  public CompletableFuture<SmallestAIHttpResponse<GetAudienceIdMembersResponse>> getAudienceMembers(
                      String id, GetAudienceIdMembersRequest request,
                      RequestOptions requestOptions) {
                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                      .addPathSegments("audience")
                      .addPathSegment(id)
                      .addPathSegments("members");if (request.getPage().isPresent()) {
                        QueryStringMapper.addQueryParameter(httpUrl, "page", request.getPage().get(), false);
                      }
                      if (request.getOffset().isPresent()) {
                        QueryStringMapper.addQueryParameter(httpUrl, "offset", request.getOffset().get(), false);
                      }
                      if (requestOptions != null) {
                        requestOptions.getQueryParameters().forEach((_key, _value) -> {
                          httpUrl.addQueryParameter(_key, _value);
                        } );
                      }
                      Request.Builder _requestBuilder = new Request.Builder()
                        .url(httpUrl.build())
                        .method("GET", null)
                        .headers(Headers.of(clientOptions.headers(requestOptions)))
                        .addHeader("Accept", "application/json");
                      Request okhttpRequest = _requestBuilder.build();
                      OkHttpClient client = clientOptions.httpClient();
                      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                        client = clientOptions.httpClientWithTimeout(requestOptions);
                      }
                      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                      }
                      CompletableFuture<SmallestAIHttpResponse<GetAudienceIdMembersResponse>> future = new CompletableFuture<>();
                      client.newCall(okhttpRequest).enqueue(new Callback() {
                        @Override
                        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                          try (ResponseBody responseBody = response.body()) {
                            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                            if (response.isSuccessful()) {
                              future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAudienceIdMembersResponse.class), response));
                              return;
                            }
                            try {
                              switch (response.code()) {
                                case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                return;
                              }
                            }
                            catch (JsonProcessingException ignored) {
                              // unable to map error response, throwing generic error
                            }
                            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                            future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                            return;
                          }
                          catch (JsonProcessingException e) {
                            future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                          }
                          catch (IOException e) {
                            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                          }
                        }

                        @Override
                        public void onFailure(@NotNull Call call, @NotNull IOException e) {
                          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                        }
                      });
                      return future;
                    }

                    /**
                     * Add new members to an existing audience.
                     * <p>Each member object must include a key matching the audience's <code>phoneNumberColumnName</code>.
                     * If it's missing, the API returns 400: <code>&quot;Each member must have a &lt;phoneNumberColumnName&gt; field&quot;</code>.
                     * Adding members that would exceed the audience limit also returns 400.</p>
                     * <p>Note: if the audience belongs to a different organization, the API returns 404 (not 403).</p>
                     */
                    public CompletableFuture<SmallestAIHttpResponse<PostAudienceIdMembersResponse>> addAudienceMembers(
                        String id, PostAudienceIdMembersRequest request) {
                      return addAudienceMembers(id,request,null);
                    }

                    /**
                     * Add new members to an existing audience.
                     * <p>Each member object must include a key matching the audience's <code>phoneNumberColumnName</code>.
                     * If it's missing, the API returns 400: <code>&quot;Each member must have a &lt;phoneNumberColumnName&gt; field&quot;</code>.
                     * Adding members that would exceed the audience limit also returns 400.</p>
                     * <p>Note: if the audience belongs to a different organization, the API returns 404 (not 403).</p>
                     */
                    public CompletableFuture<SmallestAIHttpResponse<PostAudienceIdMembersResponse>> addAudienceMembers(
                        String id, PostAudienceIdMembersRequest request,
                        RequestOptions requestOptions) {
                      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                        .addPathSegments("audience")
                        .addPathSegment(id)
                        .addPathSegments("members");if (requestOptions != null) {
                          requestOptions.getQueryParameters().forEach((_key, _value) -> {
                            httpUrl.addQueryParameter(_key, _value);
                          } );
                        }
                        RequestBody body;
                        try {
                          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                        }
                        catch(JsonProcessingException e) {
                          throw new SmallestAIException("Failed to serialize request", e);
                        }
                        Request okhttpRequest = new Request.Builder()
                          .url(httpUrl.build())
                          .method("POST", body)
                          .headers(Headers.of(clientOptions.headers(requestOptions)))
                          .addHeader("Content-Type", "application/json")
                          .addHeader("Accept", "application/json")
                          .build();
                        OkHttpClient client = clientOptions.httpClient();
                        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                          client = clientOptions.httpClientWithTimeout(requestOptions);
                        }
                        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                        }
                        CompletableFuture<SmallestAIHttpResponse<PostAudienceIdMembersResponse>> future = new CompletableFuture<>();
                        client.newCall(okhttpRequest).enqueue(new Callback() {
                          @Override
                          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                            try (ResponseBody responseBody = response.body()) {
                              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                              if (response.isSuccessful()) {
                                future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostAudienceIdMembersResponse.class), response));
                                return;
                              }
                              try {
                                switch (response.code()) {
                                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                  case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                  case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                  case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                  return;
                                }
                              }
                              catch (JsonProcessingException ignored) {
                                // unable to map error response, throwing generic error
                              }
                              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                              future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                              return;
                            }
                            catch (JsonProcessingException e) {
                              future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                            }
                            catch (IOException e) {
                              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                            }
                          }

                          @Override
                          public void onFailure(@NotNull Call call, @NotNull IOException e) {
                            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                          }
                        });
                        return future;
                      }

                      /**
                       * Remove specific members from an audience by their member IDs. Users can only delete members from audiences they created.
                       */
                      public CompletableFuture<SmallestAIHttpResponse<DeleteAudienceIdMembersResponse>> deleteAudienceMembers(
                          String id, DeleteAudienceIdMembersRequest request) {
                        return deleteAudienceMembers(id,request,null);
                      }

                      /**
                       * Remove specific members from an audience by their member IDs. Users can only delete members from audiences they created.
                       */
                      public CompletableFuture<SmallestAIHttpResponse<DeleteAudienceIdMembersResponse>> deleteAudienceMembers(
                          String id, DeleteAudienceIdMembersRequest request,
                          RequestOptions requestOptions) {
                        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                          .addPathSegments("audience")
                          .addPathSegment(id)
                          .addPathSegments("members");if (requestOptions != null) {
                            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                              httpUrl.addQueryParameter(_key, _value);
                            } );
                          }
                          RequestBody body;
                          try {
                            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                          }
                          catch(JsonProcessingException e) {
                            throw new SmallestAIException("Failed to serialize request", e);
                          }
                          Request okhttpRequest = new Request.Builder()
                            .url(httpUrl.build())
                            .method("DELETE", body)
                            .headers(Headers.of(clientOptions.headers(requestOptions)))
                            .addHeader("Content-Type", "application/json")
                            .addHeader("Accept", "application/json")
                            .build();
                          OkHttpClient client = clientOptions.httpClient();
                          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                            client = clientOptions.httpClientWithTimeout(requestOptions);
                          }
                          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                          }
                          CompletableFuture<SmallestAIHttpResponse<DeleteAudienceIdMembersResponse>> future = new CompletableFuture<>();
                          client.newCall(okhttpRequest).enqueue(new Callback() {
                            @Override
                            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                              try (ResponseBody responseBody = response.body()) {
                                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                if (response.isSuccessful()) {
                                  future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeleteAudienceIdMembersResponse.class), response));
                                  return;
                                }
                                try {
                                  switch (response.code()) {
                                    case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                    return;
                                    case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                    return;
                                    case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                    return;
                                    case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                    return;
                                  }
                                }
                                catch (JsonProcessingException ignored) {
                                  // unable to map error response, throwing generic error
                                }
                                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                return;
                              }
                              catch (JsonProcessingException e) {
                                future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                              }
                              catch (IOException e) {
                                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                              }
                            }

                            @Override
                            public void onFailure(@NotNull Call call, @NotNull IOException e) {
                              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                            }
                          });
                          return future;
                        }

                        /**
                         * Search for members within a specific audience using flexible search parameters. Users can only search members of audiences they created.
                         * <p><strong>Search Types:</strong></p>
                         * <ul>
                         * <li><strong>General Search</strong> (<code>query</code>): Searches across all fields in the audience member data</li>
                         * <li><strong>Field-Specific Search</strong>: Use any field name as a parameter (e.g., <code>firstName=john</code>, <code>phoneNumber=123456</code>, <code>email=test@example.com</code>)</li>
                         * </ul>
                         * <p><strong>Examples:</strong></p>
                         * <ul>
                         * <li><code>?query=john</code> - General search across all fields</li>
                         * <li><code>?firstName=john</code> - Search specifically in firstName field</li>
                         * <li><code>?phoneNumber=555-1234</code> - Search specifically in phoneNumber field</li>
                         * <li><code>?firstName=john&amp;lastName=doe</code> - Search for members matching both criteria</li>
                         * </ul>
                         * <p><strong>Note:</strong> When using phoneNumber field, do not use quotes around the phone number. You can use either a general search OR field-specific searches, but not both simultaneously.</p>
                         */
                        public CompletableFuture<SmallestAIHttpResponse<GetAudienceIdMembersSearchResponse>> searchAudienceMembers(
                            String id) {
                          return searchAudienceMembers(id,GetAudienceIdMembersSearchRequest.builder().build());
                        }

                        /**
                         * Search for members within a specific audience using flexible search parameters. Users can only search members of audiences they created.
                         * <p><strong>Search Types:</strong></p>
                         * <ul>
                         * <li><strong>General Search</strong> (<code>query</code>): Searches across all fields in the audience member data</li>
                         * <li><strong>Field-Specific Search</strong>: Use any field name as a parameter (e.g., <code>firstName=john</code>, <code>phoneNumber=123456</code>, <code>email=test@example.com</code>)</li>
                         * </ul>
                         * <p><strong>Examples:</strong></p>
                         * <ul>
                         * <li><code>?query=john</code> - General search across all fields</li>
                         * <li><code>?firstName=john</code> - Search specifically in firstName field</li>
                         * <li><code>?phoneNumber=555-1234</code> - Search specifically in phoneNumber field</li>
                         * <li><code>?firstName=john&amp;lastName=doe</code> - Search for members matching both criteria</li>
                         * </ul>
                         * <p><strong>Note:</strong> When using phoneNumber field, do not use quotes around the phone number. You can use either a general search OR field-specific searches, but not both simultaneously.</p>
                         */
                        public CompletableFuture<SmallestAIHttpResponse<GetAudienceIdMembersSearchResponse>> searchAudienceMembers(
                            String id, RequestOptions requestOptions) {
                          return searchAudienceMembers(id,GetAudienceIdMembersSearchRequest.builder().build(),requestOptions);
                        }

                        /**
                         * Search for members within a specific audience using flexible search parameters. Users can only search members of audiences they created.
                         * <p><strong>Search Types:</strong></p>
                         * <ul>
                         * <li><strong>General Search</strong> (<code>query</code>): Searches across all fields in the audience member data</li>
                         * <li><strong>Field-Specific Search</strong>: Use any field name as a parameter (e.g., <code>firstName=john</code>, <code>phoneNumber=123456</code>, <code>email=test@example.com</code>)</li>
                         * </ul>
                         * <p><strong>Examples:</strong></p>
                         * <ul>
                         * <li><code>?query=john</code> - General search across all fields</li>
                         * <li><code>?firstName=john</code> - Search specifically in firstName field</li>
                         * <li><code>?phoneNumber=555-1234</code> - Search specifically in phoneNumber field</li>
                         * <li><code>?firstName=john&amp;lastName=doe</code> - Search for members matching both criteria</li>
                         * </ul>
                         * <p><strong>Note:</strong> When using phoneNumber field, do not use quotes around the phone number. You can use either a general search OR field-specific searches, but not both simultaneously.</p>
                         */
                        public CompletableFuture<SmallestAIHttpResponse<GetAudienceIdMembersSearchResponse>> searchAudienceMembers(
                            String id, GetAudienceIdMembersSearchRequest request) {
                          return searchAudienceMembers(id,request,null);
                        }

                        /**
                         * Search for members within a specific audience using flexible search parameters. Users can only search members of audiences they created.
                         * <p><strong>Search Types:</strong></p>
                         * <ul>
                         * <li><strong>General Search</strong> (<code>query</code>): Searches across all fields in the audience member data</li>
                         * <li><strong>Field-Specific Search</strong>: Use any field name as a parameter (e.g., <code>firstName=john</code>, <code>phoneNumber=123456</code>, <code>email=test@example.com</code>)</li>
                         * </ul>
                         * <p><strong>Examples:</strong></p>
                         * <ul>
                         * <li><code>?query=john</code> - General search across all fields</li>
                         * <li><code>?firstName=john</code> - Search specifically in firstName field</li>
                         * <li><code>?phoneNumber=555-1234</code> - Search specifically in phoneNumber field</li>
                         * <li><code>?firstName=john&amp;lastName=doe</code> - Search for members matching both criteria</li>
                         * </ul>
                         * <p><strong>Note:</strong> When using phoneNumber field, do not use quotes around the phone number. You can use either a general search OR field-specific searches, but not both simultaneously.</p>
                         */
                        public CompletableFuture<SmallestAIHttpResponse<GetAudienceIdMembersSearchResponse>> searchAudienceMembers(
                            String id, GetAudienceIdMembersSearchRequest request,
                            RequestOptions requestOptions) {
                          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                            .addPathSegments("audience")
                            .addPathSegment(id)
                            .addPathSegments("members")
                            .addPathSegments("search");if (request.getQuery().isPresent()) {
                              QueryStringMapper.addQueryParameter(httpUrl, "query", request.getQuery().get(), false);
                            }
                            if (requestOptions != null) {
                              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                httpUrl.addQueryParameter(_key, _value);
                              } );
                            }
                            Request.Builder _requestBuilder = new Request.Builder()
                              .url(httpUrl.build())
                              .method("GET", null)
                              .headers(Headers.of(clientOptions.headers(requestOptions)))
                              .addHeader("Accept", "application/json");
                            Request okhttpRequest = _requestBuilder.build();
                            OkHttpClient client = clientOptions.httpClient();
                            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                              client = clientOptions.httpClientWithTimeout(requestOptions);
                            }
                            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                            }
                            CompletableFuture<SmallestAIHttpResponse<GetAudienceIdMembersSearchResponse>> future = new CompletableFuture<>();
                            client.newCall(okhttpRequest).enqueue(new Callback() {
                              @Override
                              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                                try (ResponseBody responseBody = response.body()) {
                                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                  if (response.isSuccessful()) {
                                    future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetAudienceIdMembersSearchResponse.class), response));
                                    return;
                                  }
                                  try {
                                    switch (response.code()) {
                                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                      case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                      case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                      case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                                      return;
                                    }
                                  }
                                  catch (JsonProcessingException ignored) {
                                    // unable to map error response, throwing generic error
                                  }
                                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                  future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                                  return;
                                }
                                catch (JsonProcessingException e) {
                                  future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                                }
                                catch (IOException e) {
                                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                                }
                              }

                              @Override
                              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                              }
                            });
                            return future;
                          }
                        }
