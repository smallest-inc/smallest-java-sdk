/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.conversations;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.RequestOptions;
import ai.smallest.resources.atoms.conversations.requests.CancelConversationsRequest;
import ai.smallest.resources.atoms.conversations.requests.CancelQueuedConversationsRequest;
import ai.smallest.resources.atoms.conversations.requests.GetConversationCallIdRecordingDownloadUrlRequest;
import ai.smallest.resources.atoms.conversations.requests.ListRetryAttemptsConversationsRequest;
import ai.smallest.resources.atoms.conversations.types.CancelConversationsResponse;
import ai.smallest.resources.atoms.conversations.types.CancelQueuedConversationsResponse;
import ai.smallest.resources.atoms.conversations.types.GetConversationCallIdRecordingDownloadUrlResponse;
import ai.smallest.resources.atoms.conversations.types.ListRetryAttemptsConversationsResponse;
import java.lang.String;
import java.util.concurrent.CompletableFuture;

public class AsyncConversationsClient {
  protected final ClientOptions clientOptions;

  private final AsyncRawConversationsClient rawClient;

  public AsyncConversationsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new AsyncRawConversationsClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public AsyncRawConversationsClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is <strong>time-limited</strong> — typically usable for a few minutes — so don't cache it; request a fresh one each time you need the recording.
   * <p>Returns <code>404</code> if the call has no recording (call hasn't started, was cancelled before audio captured, or was deleted by the platform's retention policy). Returns <code>400 Invalid call ID format</code> if you pass a Mongo <code>_id</code> instead of the <code>callId</code> string.</p>
   */
  public CompletableFuture<GetConversationCallIdRecordingDownloadUrlResponse> getATimeLimitedRecordingDownloadUrl(
      String callId) {
    return this.rawClient.getATimeLimitedRecordingDownloadUrl(callId).thenApply(response -> response.body());
  }

  /**
   * Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is <strong>time-limited</strong> — typically usable for a few minutes — so don't cache it; request a fresh one each time you need the recording.
   * <p>Returns <code>404</code> if the call has no recording (call hasn't started, was cancelled before audio captured, or was deleted by the platform's retention policy). Returns <code>400 Invalid call ID format</code> if you pass a Mongo <code>_id</code> instead of the <code>callId</code> string.</p>
   */
  public CompletableFuture<GetConversationCallIdRecordingDownloadUrlResponse> getATimeLimitedRecordingDownloadUrl(
      String callId, RequestOptions requestOptions) {
    return this.rawClient.getATimeLimitedRecordingDownloadUrl(callId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is <strong>time-limited</strong> — typically usable for a few minutes — so don't cache it; request a fresh one each time you need the recording.
   * <p>Returns <code>404</code> if the call has no recording (call hasn't started, was cancelled before audio captured, or was deleted by the platform's retention policy). Returns <code>400 Invalid call ID format</code> if you pass a Mongo <code>_id</code> instead of the <code>callId</code> string.</p>
   */
  public CompletableFuture<GetConversationCallIdRecordingDownloadUrlResponse> getATimeLimitedRecordingDownloadUrl(
      String callId, GetConversationCallIdRecordingDownloadUrlRequest request) {
    return this.rawClient.getATimeLimitedRecordingDownloadUrl(callId, request).thenApply(response -> response.body());
  }

  /**
   * Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is <strong>time-limited</strong> — typically usable for a few minutes — so don't cache it; request a fresh one each time you need the recording.
   * <p>Returns <code>404</code> if the call has no recording (call hasn't started, was cancelled before audio captured, or was deleted by the platform's retention policy). Returns <code>400 Invalid call ID format</code> if you pass a Mongo <code>_id</code> instead of the <code>callId</code> string.</p>
   */
  public CompletableFuture<GetConversationCallIdRecordingDownloadUrlResponse> getATimeLimitedRecordingDownloadUrl(
      String callId, GetConversationCallIdRecordingDownloadUrlRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getATimeLimitedRecordingDownloadUrl(callId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the <strong>parent call</strong> plus every retry attempt that branched from it, ordered by attempt index. Use this when a customer asks &quot;did the platform retry this call?&quot; — typically driven by an outbound agent's auto-retry configuration (<code>maxRetries</code>, <code>retryDelay</code>).
   * <ul>
   * <li>If the <code>callId</code> you pass is the original (parent), the response contains that parent plus all child retries.</li>
   * <li>If the <code>callId</code> you pass is itself a retry, the response still includes the parent and every sibling retry — the API resolves to the family root automatically.</li>
   * </ul>
   * <p>Returns <code>404</code> if no call exists with that ID in your organization.</p>
   */
  public CompletableFuture<ListRetryAttemptsConversationsResponse> listRetryAttempts(
      String callId) {
    return this.rawClient.listRetryAttempts(callId).thenApply(response -> response.body());
  }

  /**
   * Returns the <strong>parent call</strong> plus every retry attempt that branched from it, ordered by attempt index. Use this when a customer asks &quot;did the platform retry this call?&quot; — typically driven by an outbound agent's auto-retry configuration (<code>maxRetries</code>, <code>retryDelay</code>).
   * <ul>
   * <li>If the <code>callId</code> you pass is the original (parent), the response contains that parent plus all child retries.</li>
   * <li>If the <code>callId</code> you pass is itself a retry, the response still includes the parent and every sibling retry — the API resolves to the family root automatically.</li>
   * </ul>
   * <p>Returns <code>404</code> if no call exists with that ID in your organization.</p>
   */
  public CompletableFuture<ListRetryAttemptsConversationsResponse> listRetryAttempts(String callId,
      RequestOptions requestOptions) {
    return this.rawClient.listRetryAttempts(callId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Returns the <strong>parent call</strong> plus every retry attempt that branched from it, ordered by attempt index. Use this when a customer asks &quot;did the platform retry this call?&quot; — typically driven by an outbound agent's auto-retry configuration (<code>maxRetries</code>, <code>retryDelay</code>).
   * <ul>
   * <li>If the <code>callId</code> you pass is the original (parent), the response contains that parent plus all child retries.</li>
   * <li>If the <code>callId</code> you pass is itself a retry, the response still includes the parent and every sibling retry — the API resolves to the family root automatically.</li>
   * </ul>
   * <p>Returns <code>404</code> if no call exists with that ID in your organization.</p>
   */
  public CompletableFuture<ListRetryAttemptsConversationsResponse> listRetryAttempts(String callId,
      ListRetryAttemptsConversationsRequest request) {
    return this.rawClient.listRetryAttempts(callId, request).thenApply(response -> response.body());
  }

  /**
   * Returns the <strong>parent call</strong> plus every retry attempt that branched from it, ordered by attempt index. Use this when a customer asks &quot;did the platform retry this call?&quot; — typically driven by an outbound agent's auto-retry configuration (<code>maxRetries</code>, <code>retryDelay</code>).
   * <ul>
   * <li>If the <code>callId</code> you pass is the original (parent), the response contains that parent plus all child retries.</li>
   * <li>If the <code>callId</code> you pass is itself a retry, the response still includes the parent and every sibling retry — the API resolves to the family root automatically.</li>
   * </ul>
   * <p>Returns <code>404</code> if no call exists with that ID in your organization.</p>
   */
  public CompletableFuture<ListRetryAttemptsConversationsResponse> listRetryAttempts(String callId,
      ListRetryAttemptsConversationsRequest request, RequestOptions requestOptions) {
    return this.rawClient.listRetryAttempts(callId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Cancels an outbound call that has been queued or is in progress. Use the body form to look the call up by <code>callId</code>; the path-param form (<code>POST /conversation/{callId}/cancel</code>) is the equivalent for REST conventions, but only handles <code>IN_QUEUE</code> calls.
   * <p>Returns <code>404</code> if no call with that ID exists in your organization. Returns <code>400</code> if the call is already in a terminal state (completed / failed / cancelled).</p>
   */
  public CompletableFuture<CancelConversationsResponse> cancel(CancelConversationsRequest request) {
    return this.rawClient.cancel(request).thenApply(response -> response.body());
  }

  /**
   * Cancels an outbound call that has been queued or is in progress. Use the body form to look the call up by <code>callId</code>; the path-param form (<code>POST /conversation/{callId}/cancel</code>) is the equivalent for REST conventions, but only handles <code>IN_QUEUE</code> calls.
   * <p>Returns <code>404</code> if no call with that ID exists in your organization. Returns <code>400</code> if the call is already in a terminal state (completed / failed / cancelled).</p>
   */
  public CompletableFuture<CancelConversationsResponse> cancel(CancelConversationsRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.cancel(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * REST-conventional path-param variant of <a href="#operation/cancelCallByBody"><code>POST /conversation/cancel</code></a>.
   * <p><strong>Behavior differs from the body form.</strong> This path-param endpoint only cancels calls that are still in the <code>IN_QUEUE</code> state — calls that have already started dialing or are in progress return <code>400 Bad Request</code> with <code>errors: [&quot;Conversation with ID ... is not in queue and cannot be cancelled&quot;]</code>. Use the body form (<code>POST /conversation/cancel</code>) if you need to cancel an in-progress call.</p>
   * <p>The path param is the <code>callId</code> string (e.g. <code>CALL-1778226705739-7e4c17</code>), <strong>not</strong> the Mongo <code>_id</code>. Passing <code>_id</code> returns <code>404 No conversation found</code>.</p>
   */
  public CompletableFuture<CancelQueuedConversationsResponse> cancelQueued(String callId) {
    return this.rawClient.cancelQueued(callId).thenApply(response -> response.body());
  }

  /**
   * REST-conventional path-param variant of <a href="#operation/cancelCallByBody"><code>POST /conversation/cancel</code></a>.
   * <p><strong>Behavior differs from the body form.</strong> This path-param endpoint only cancels calls that are still in the <code>IN_QUEUE</code> state — calls that have already started dialing or are in progress return <code>400 Bad Request</code> with <code>errors: [&quot;Conversation with ID ... is not in queue and cannot be cancelled&quot;]</code>. Use the body form (<code>POST /conversation/cancel</code>) if you need to cancel an in-progress call.</p>
   * <p>The path param is the <code>callId</code> string (e.g. <code>CALL-1778226705739-7e4c17</code>), <strong>not</strong> the Mongo <code>_id</code>. Passing <code>_id</code> returns <code>404 No conversation found</code>.</p>
   */
  public CompletableFuture<CancelQueuedConversationsResponse> cancelQueued(String callId,
      RequestOptions requestOptions) {
    return this.rawClient.cancelQueued(callId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * REST-conventional path-param variant of <a href="#operation/cancelCallByBody"><code>POST /conversation/cancel</code></a>.
   * <p><strong>Behavior differs from the body form.</strong> This path-param endpoint only cancels calls that are still in the <code>IN_QUEUE</code> state — calls that have already started dialing or are in progress return <code>400 Bad Request</code> with <code>errors: [&quot;Conversation with ID ... is not in queue and cannot be cancelled&quot;]</code>. Use the body form (<code>POST /conversation/cancel</code>) if you need to cancel an in-progress call.</p>
   * <p>The path param is the <code>callId</code> string (e.g. <code>CALL-1778226705739-7e4c17</code>), <strong>not</strong> the Mongo <code>_id</code>. Passing <code>_id</code> returns <code>404 No conversation found</code>.</p>
   */
  public CompletableFuture<CancelQueuedConversationsResponse> cancelQueued(String callId,
      CancelQueuedConversationsRequest request) {
    return this.rawClient.cancelQueued(callId, request).thenApply(response -> response.body());
  }

  /**
   * REST-conventional path-param variant of <a href="#operation/cancelCallByBody"><code>POST /conversation/cancel</code></a>.
   * <p><strong>Behavior differs from the body form.</strong> This path-param endpoint only cancels calls that are still in the <code>IN_QUEUE</code> state — calls that have already started dialing or are in progress return <code>400 Bad Request</code> with <code>errors: [&quot;Conversation with ID ... is not in queue and cannot be cancelled&quot;]</code>. Use the body form (<code>POST /conversation/cancel</code>) if you need to cancel an in-progress call.</p>
   * <p>The path param is the <code>callId</code> string (e.g. <code>CALL-1778226705739-7e4c17</code>), <strong>not</strong> the Mongo <code>_id</code>. Passing <code>_id</code> returns <code>404 No conversation found</code>.</p>
   */
  public CompletableFuture<CancelQueuedConversationsResponse> cancelQueued(String callId,
      CancelQueuedConversationsRequest request, RequestOptions requestOptions) {
    return this.rawClient.cancelQueued(callId, request, requestOptions).thenApply(response -> response.body());
  }
}
