/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.conversations;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.MediaTypes;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.conversations.requests.CancelConversationsRequest;
import ai.smallest.resources.atoms.conversations.requests.CancelQueuedConversationsRequest;
import ai.smallest.resources.atoms.conversations.requests.GetConversationCallIdRecordingDownloadUrlRequest;
import ai.smallest.resources.atoms.conversations.requests.ListRetryAttemptsConversationsRequest;
import ai.smallest.resources.atoms.conversations.types.CancelConversationsResponse;
import ai.smallest.resources.atoms.conversations.types.CancelQueuedConversationsResponse;
import ai.smallest.resources.atoms.conversations.types.GetConversationCallIdRecordingDownloadUrlResponse;
import ai.smallest.resources.atoms.conversations.types.ListRetryAttemptsConversationsResponse;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.ForbiddenError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.NotFoundError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawConversationsClient {
  protected final ClientOptions clientOptions;

  public AsyncRawConversationsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is <strong>time-limited</strong> — typically usable for a few minutes — so don't cache it; request a fresh one each time you need the recording.
   * <p>Returns <code>404</code> if the call has no recording (call hasn't started, was cancelled before audio captured, or was deleted by the platform's retention policy). Returns <code>400 Invalid call ID format</code> if you pass a Mongo <code>_id</code> instead of the <code>callId</code> string.</p>
   */
  public CompletableFuture<SmallestAIHttpResponse<GetConversationCallIdRecordingDownloadUrlResponse>> getATimeLimitedRecordingDownloadUrl(
      String callId) {
    return getATimeLimitedRecordingDownloadUrl(callId,GetConversationCallIdRecordingDownloadUrlRequest.builder().build());
  }

  /**
   * Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is <strong>time-limited</strong> — typically usable for a few minutes — so don't cache it; request a fresh one each time you need the recording.
   * <p>Returns <code>404</code> if the call has no recording (call hasn't started, was cancelled before audio captured, or was deleted by the platform's retention policy). Returns <code>400 Invalid call ID format</code> if you pass a Mongo <code>_id</code> instead of the <code>callId</code> string.</p>
   */
  public CompletableFuture<SmallestAIHttpResponse<GetConversationCallIdRecordingDownloadUrlResponse>> getATimeLimitedRecordingDownloadUrl(
      String callId, RequestOptions requestOptions) {
    return getATimeLimitedRecordingDownloadUrl(callId,GetConversationCallIdRecordingDownloadUrlRequest.builder().build(),requestOptions);
  }

  /**
   * Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is <strong>time-limited</strong> — typically usable for a few minutes — so don't cache it; request a fresh one each time you need the recording.
   * <p>Returns <code>404</code> if the call has no recording (call hasn't started, was cancelled before audio captured, or was deleted by the platform's retention policy). Returns <code>400 Invalid call ID format</code> if you pass a Mongo <code>_id</code> instead of the <code>callId</code> string.</p>
   */
  public CompletableFuture<SmallestAIHttpResponse<GetConversationCallIdRecordingDownloadUrlResponse>> getATimeLimitedRecordingDownloadUrl(
      String callId, GetConversationCallIdRecordingDownloadUrlRequest request) {
    return getATimeLimitedRecordingDownloadUrl(callId,request,null);
  }

  /**
   * Returns a presigned S3 URL for the call's recording. Hand the URL straight to the customer or pull bytes server-side. The presigned URL is <strong>time-limited</strong> — typically usable for a few minutes — so don't cache it; request a fresh one each time you need the recording.
   * <p>Returns <code>404</code> if the call has no recording (call hasn't started, was cancelled before audio captured, or was deleted by the platform's retention policy). Returns <code>400 Invalid call ID format</code> if you pass a Mongo <code>_id</code> instead of the <code>callId</code> string.</p>
   */
  public CompletableFuture<SmallestAIHttpResponse<GetConversationCallIdRecordingDownloadUrlResponse>> getATimeLimitedRecordingDownloadUrl(
      String callId, GetConversationCallIdRecordingDownloadUrlRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("conversation")
      .addPathSegment(callId)
      .addPathSegments("recording")
      .addPathSegments("download-url");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      CompletableFuture<SmallestAIHttpResponse<GetConversationCallIdRecordingDownloadUrlResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetConversationCallIdRecordingDownloadUrlResponse.class), response));
              return;
            }
            try {
              switch (response.code()) {
                case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (JsonProcessingException e) {
            future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
          }
          catch (IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Returns the <strong>parent call</strong> plus every retry attempt that branched from it, ordered by attempt index. Use this when a customer asks &quot;did the platform retry this call?&quot; — typically driven by an outbound agent's auto-retry configuration (<code>maxRetries</code>, <code>retryDelay</code>).
     * <ul>
     * <li>If the <code>callId</code> you pass is the original (parent), the response contains that parent plus all child retries.</li>
     * <li>If the <code>callId</code> you pass is itself a retry, the response still includes the parent and every sibling retry — the API resolves to the family root automatically.</li>
     * </ul>
     * <p>Returns <code>404</code> if no call exists with that ID in your organization.</p>
     */
    public CompletableFuture<SmallestAIHttpResponse<ListRetryAttemptsConversationsResponse>> listRetryAttempts(
        String callId) {
      return listRetryAttempts(callId,ListRetryAttemptsConversationsRequest.builder().build());
    }

    /**
     * Returns the <strong>parent call</strong> plus every retry attempt that branched from it, ordered by attempt index. Use this when a customer asks &quot;did the platform retry this call?&quot; — typically driven by an outbound agent's auto-retry configuration (<code>maxRetries</code>, <code>retryDelay</code>).
     * <ul>
     * <li>If the <code>callId</code> you pass is the original (parent), the response contains that parent plus all child retries.</li>
     * <li>If the <code>callId</code> you pass is itself a retry, the response still includes the parent and every sibling retry — the API resolves to the family root automatically.</li>
     * </ul>
     * <p>Returns <code>404</code> if no call exists with that ID in your organization.</p>
     */
    public CompletableFuture<SmallestAIHttpResponse<ListRetryAttemptsConversationsResponse>> listRetryAttempts(
        String callId, RequestOptions requestOptions) {
      return listRetryAttempts(callId,ListRetryAttemptsConversationsRequest.builder().build(),requestOptions);
    }

    /**
     * Returns the <strong>parent call</strong> plus every retry attempt that branched from it, ordered by attempt index. Use this when a customer asks &quot;did the platform retry this call?&quot; — typically driven by an outbound agent's auto-retry configuration (<code>maxRetries</code>, <code>retryDelay</code>).
     * <ul>
     * <li>If the <code>callId</code> you pass is the original (parent), the response contains that parent plus all child retries.</li>
     * <li>If the <code>callId</code> you pass is itself a retry, the response still includes the parent and every sibling retry — the API resolves to the family root automatically.</li>
     * </ul>
     * <p>Returns <code>404</code> if no call exists with that ID in your organization.</p>
     */
    public CompletableFuture<SmallestAIHttpResponse<ListRetryAttemptsConversationsResponse>> listRetryAttempts(
        String callId, ListRetryAttemptsConversationsRequest request) {
      return listRetryAttempts(callId,request,null);
    }

    /**
     * Returns the <strong>parent call</strong> plus every retry attempt that branched from it, ordered by attempt index. Use this when a customer asks &quot;did the platform retry this call?&quot; — typically driven by an outbound agent's auto-retry configuration (<code>maxRetries</code>, <code>retryDelay</code>).
     * <ul>
     * <li>If the <code>callId</code> you pass is the original (parent), the response contains that parent plus all child retries.</li>
     * <li>If the <code>callId</code> you pass is itself a retry, the response still includes the parent and every sibling retry — the API resolves to the family root automatically.</li>
     * </ul>
     * <p>Returns <code>404</code> if no call exists with that ID in your organization.</p>
     */
    public CompletableFuture<SmallestAIHttpResponse<ListRetryAttemptsConversationsResponse>> listRetryAttempts(
        String callId, ListRetryAttemptsConversationsRequest request,
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("conversation")
        .addPathSegment(callId)
        .addPathSegments("retries");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        CompletableFuture<SmallestAIHttpResponse<ListRetryAttemptsConversationsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListRetryAttemptsConversationsResponse.class), response));
                return;
              }
              try {
                switch (response.code()) {
                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (JsonProcessingException e) {
              future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
            }
            catch (IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Cancels an outbound call that has been queued or is in progress. Use the body form to look the call up by <code>callId</code>; the path-param form (<code>POST /conversation/{callId}/cancel</code>) is the equivalent for REST conventions, but only handles <code>IN_QUEUE</code> calls.
       * <p>Returns <code>404</code> if no call with that ID exists in your organization. Returns <code>400</code> if the call is already in a terminal state (completed / failed / cancelled).</p>
       */
      public CompletableFuture<SmallestAIHttpResponse<CancelConversationsResponse>> cancel(
          CancelConversationsRequest request) {
        return cancel(request,null);
      }

      /**
       * Cancels an outbound call that has been queued or is in progress. Use the body form to look the call up by <code>callId</code>; the path-param form (<code>POST /conversation/{callId}/cancel</code>) is the equivalent for REST conventions, but only handles <code>IN_QUEUE</code> calls.
       * <p>Returns <code>404</code> if no call with that ID exists in your organization. Returns <code>400</code> if the call is already in a terminal state (completed / failed / cancelled).</p>
       */
      public CompletableFuture<SmallestAIHttpResponse<CancelConversationsResponse>> cancel(
          CancelConversationsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("conversation/cancel");if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(JsonProcessingException e) {
            throw new SmallestAIException("Failed to serialize request", e);
          }
          Request okhttpRequest = new Request.Builder()
            .url(httpUrl.build())
            .method("POST", body)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .addHeader("Accept", "application/json")
            .build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          CompletableFuture<SmallestAIHttpResponse<CancelConversationsResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CancelConversationsResponse.class), response));
                  return;
                }
                try {
                  switch (response.code()) {
                    case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (JsonProcessingException e) {
                future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
              }
              catch (IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * REST-conventional path-param variant of <a href="#operation/cancelCallByBody"><code>POST /conversation/cancel</code></a>.
         * <p><strong>Behavior differs from the body form.</strong> This path-param endpoint only cancels calls that are still in the <code>IN_QUEUE</code> state — calls that have already started dialing or are in progress return <code>400 Bad Request</code> with <code>errors: [&quot;Conversation with ID ... is not in queue and cannot be cancelled&quot;]</code>. Use the body form (<code>POST /conversation/cancel</code>) if you need to cancel an in-progress call.</p>
         * <p>The path param is the <code>callId</code> string (e.g. <code>CALL-1778226705739-7e4c17</code>), <strong>not</strong> the Mongo <code>_id</code>. Passing <code>_id</code> returns <code>404 No conversation found</code>.</p>
         */
        public CompletableFuture<SmallestAIHttpResponse<CancelQueuedConversationsResponse>> cancelQueued(
            String callId) {
          return cancelQueued(callId,CancelQueuedConversationsRequest.builder().build());
        }

        /**
         * REST-conventional path-param variant of <a href="#operation/cancelCallByBody"><code>POST /conversation/cancel</code></a>.
         * <p><strong>Behavior differs from the body form.</strong> This path-param endpoint only cancels calls that are still in the <code>IN_QUEUE</code> state — calls that have already started dialing or are in progress return <code>400 Bad Request</code> with <code>errors: [&quot;Conversation with ID ... is not in queue and cannot be cancelled&quot;]</code>. Use the body form (<code>POST /conversation/cancel</code>) if you need to cancel an in-progress call.</p>
         * <p>The path param is the <code>callId</code> string (e.g. <code>CALL-1778226705739-7e4c17</code>), <strong>not</strong> the Mongo <code>_id</code>. Passing <code>_id</code> returns <code>404 No conversation found</code>.</p>
         */
        public CompletableFuture<SmallestAIHttpResponse<CancelQueuedConversationsResponse>> cancelQueued(
            String callId, RequestOptions requestOptions) {
          return cancelQueued(callId,CancelQueuedConversationsRequest.builder().build(),requestOptions);
        }

        /**
         * REST-conventional path-param variant of <a href="#operation/cancelCallByBody"><code>POST /conversation/cancel</code></a>.
         * <p><strong>Behavior differs from the body form.</strong> This path-param endpoint only cancels calls that are still in the <code>IN_QUEUE</code> state — calls that have already started dialing or are in progress return <code>400 Bad Request</code> with <code>errors: [&quot;Conversation with ID ... is not in queue and cannot be cancelled&quot;]</code>. Use the body form (<code>POST /conversation/cancel</code>) if you need to cancel an in-progress call.</p>
         * <p>The path param is the <code>callId</code> string (e.g. <code>CALL-1778226705739-7e4c17</code>), <strong>not</strong> the Mongo <code>_id</code>. Passing <code>_id</code> returns <code>404 No conversation found</code>.</p>
         */
        public CompletableFuture<SmallestAIHttpResponse<CancelQueuedConversationsResponse>> cancelQueued(
            String callId, CancelQueuedConversationsRequest request) {
          return cancelQueued(callId,request,null);
        }

        /**
         * REST-conventional path-param variant of <a href="#operation/cancelCallByBody"><code>POST /conversation/cancel</code></a>.
         * <p><strong>Behavior differs from the body form.</strong> This path-param endpoint only cancels calls that are still in the <code>IN_QUEUE</code> state — calls that have already started dialing or are in progress return <code>400 Bad Request</code> with <code>errors: [&quot;Conversation with ID ... is not in queue and cannot be cancelled&quot;]</code>. Use the body form (<code>POST /conversation/cancel</code>) if you need to cancel an in-progress call.</p>
         * <p>The path param is the <code>callId</code> string (e.g. <code>CALL-1778226705739-7e4c17</code>), <strong>not</strong> the Mongo <code>_id</code>. Passing <code>_id</code> returns <code>404 No conversation found</code>.</p>
         */
        public CompletableFuture<SmallestAIHttpResponse<CancelQueuedConversationsResponse>> cancelQueued(
            String callId, CancelQueuedConversationsRequest request,
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("conversation")
            .addPathSegment(callId)
            .addPathSegments("cancel");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", RequestBody.create("", null))
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            CompletableFuture<SmallestAIHttpResponse<CancelQueuedConversationsResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CancelQueuedConversationsResponse.class), response));
                    return;
                  }
                  try {
                    switch (response.code()) {
                      case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (JsonProcessingException e) {
                  future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                }
                catch (IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            });
            return future;
          }
        }
