/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.compliance;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.QueryStringMapper;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.compliance.requests.GetComplianceRequirementsRequest;
import ai.smallest.resources.atoms.compliance.requests.GetComplianceStatusRequest;
import ai.smallest.resources.atoms.compliance.requests.PostComplianceApplicationsIdRefreshRequest;
import ai.smallest.resources.atoms.compliance.requests.ResubmitComplianceRequest;
import ai.smallest.resources.atoms.compliance.requests.SubmitComplianceRequest;
import ai.smallest.resources.atoms.compliance.types.GetComplianceRequirementsResponse;
import ai.smallest.resources.atoms.compliance.types.GetComplianceStatusResponse;
import ai.smallest.resources.atoms.compliance.types.PostComplianceApplicationsIdRefreshResponse;
import ai.smallest.resources.atoms.compliance.types.ResubmitComplianceResponse;
import ai.smallest.resources.atoms.compliance.types.SubmitComplianceResponse;
import ai.smallest.resources.atoms.errors.BadGatewayError;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.ConflictError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.NotFoundError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import ai.smallest.resources.atoms.types.BadRequestErrorResponse;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.File;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.Override;
import java.lang.RuntimeException;
import java.lang.String;
import java.nio.file.Files;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.MediaType;
import okhttp3.MultipartBody;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncRawComplianceClient {
  protected final ClientOptions clientOptions;

  public AsyncRawComplianceClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Returns the current compliance status for a given country, number type, and user type.
   * This is the single endpoint the frontend uses to determine which step to render
   * (form, submitted, accepted, rejected, expired, or suspended).
   */
  public CompletableFuture<SmallestAIHttpResponse<GetComplianceStatusResponse>> getComplianceStatus(
      GetComplianceStatusRequest request) {
    return getComplianceStatus(request,null);
  }

  /**
   * Returns the current compliance status for a given country, number type, and user type.
   * This is the single endpoint the frontend uses to determine which step to render
   * (form, submitted, accepted, rejected, expired, or suspended).
   */
  public CompletableFuture<SmallestAIHttpResponse<GetComplianceStatusResponse>> getComplianceStatus(
      GetComplianceStatusRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("compliance/status");QueryStringMapper.addQueryParameter(httpUrl, "countryIso", request.getCountryIso(), false);
      QueryStringMapper.addQueryParameter(httpUrl, "numberType", request.getNumberType(), false);
      QueryStringMapper.addQueryParameter(httpUrl, "userType", request.getUserType(), false);
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      CompletableFuture<SmallestAIHttpResponse<GetComplianceStatusResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetComplianceStatusResponse.class), response));
              return;
            }
            try {
              switch (response.code()) {
                case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                return;
                case 502:future.completeExceptionally(new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BadRequestErrorResponse.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (JsonProcessingException e) {
            future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
          }
          catch (IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Discover what documents are required for a given country, number type, and user type.
     * Results are cached for 1 hour. Returns an empty <code>documentTypes</code> array if no compliance
     * is needed for the given combination.
     */
    public CompletableFuture<SmallestAIHttpResponse<GetComplianceRequirementsResponse>> getComplianceRequirements(
        GetComplianceRequirementsRequest request) {
      return getComplianceRequirements(request,null);
    }

    /**
     * Discover what documents are required for a given country, number type, and user type.
     * Results are cached for 1 hour. Returns an empty <code>documentTypes</code> array if no compliance
     * is needed for the given combination.
     */
    public CompletableFuture<SmallestAIHttpResponse<GetComplianceRequirementsResponse>> getComplianceRequirements(
        GetComplianceRequirementsRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("compliance/requirements");QueryStringMapper.addQueryParameter(httpUrl, "countryIso", request.getCountryIso(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "numberType", request.getNumberType(), false);
        QueryStringMapper.addQueryParameter(httpUrl, "userType", request.getUserType(), false);
        if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        CompletableFuture<SmallestAIHttpResponse<GetComplianceRequirementsResponse>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetComplianceRequirementsResponse.class), response));
                return;
              }
              try {
                switch (response.code()) {
                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                  return;
                  case 502:future.completeExceptionally(new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BadRequestErrorResponse.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (JsonProcessingException e) {
              future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
            }
            catch (IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Submit a new compliance application with end-user details and supporting documents.
       * One application is allowed per organization per country per number type per user type.
       * <p>The request uses <code>multipart/form-data</code> because documents are uploaded inline.
       * The <code>endUser</code> and <code>documents</code> fields are JSON strings embedded in the form data.</p>
       */
      public CompletableFuture<SmallestAIHttpResponse<SubmitComplianceResponse>> submit(File files,
          SubmitComplianceRequest request) {
        return submit(files,request,null);
      }

      /**
       * Submit a new compliance application with end-user details and supporting documents.
       * One application is allowed per organization per country per number type per user type.
       * <p>The request uses <code>multipart/form-data</code> because documents are uploaded inline.
       * The <code>endUser</code> and <code>documents</code> fields are JSON strings embedded in the form data.</p>
       */
      public CompletableFuture<SmallestAIHttpResponse<SubmitComplianceResponse>> submit(File files,
          SubmitComplianceRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("compliance/applications");if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
          try {
            multipartBodyBuilder.addFormDataPart("countryIso", request.getCountryIso());
            multipartBodyBuilder.addFormDataPart("numberType", request.getNumberType().toString());
            multipartBodyBuilder.addFormDataPart("userType", request.getUserType().toString());
            multipartBodyBuilder.addFormDataPart("endUser", request.getEndUser());
            multipartBodyBuilder.addFormDataPart("documents", request.getDocuments());
            String filesMimeType = Files.probeContentType(files.toPath());
            MediaType filesMimeTypeMediaType = filesMimeType != null ? MediaType.parse(filesMimeType) : null;
            multipartBodyBuilder.addFormDataPart("files", files.getName(), RequestBody.create(files, filesMimeTypeMediaType));
          }
          catch(Exception e) {
            throw new RuntimeException(e);
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("POST", multipartBodyBuilder.build())
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          CompletableFuture<SmallestAIHttpResponse<SubmitComplianceResponse>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmitComplianceResponse.class), response));
                  return;
                }
                try {
                  switch (response.code()) {
                    case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 409:future.completeExceptionally(new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                    return;
                    case 502:future.completeExceptionally(new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BadRequestErrorResponse.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (JsonProcessingException e) {
                future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
              }
              catch (IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * Resubmit a previously rejected compliance application with corrected documents.
         * Only applications in <code>rejected</code> status can be resubmitted. All documents must be
         * re-uploaded — partial updates are not supported.
         * <p>File/document count must match exactly. Mismatch returns 400 with message <code>&quot;Expected X files, got Y&quot;</code>.</p>
         */
        public CompletableFuture<SmallestAIHttpResponse<ResubmitComplianceResponse>> resubmit(
            String id, File files, ResubmitComplianceRequest request) {
          return resubmit(id,files,request,null);
        }

        /**
         * Resubmit a previously rejected compliance application with corrected documents.
         * Only applications in <code>rejected</code> status can be resubmitted. All documents must be
         * re-uploaded — partial updates are not supported.
         * <p>File/document count must match exactly. Mismatch returns 400 with message <code>&quot;Expected X files, got Y&quot;</code>.</p>
         */
        public CompletableFuture<SmallestAIHttpResponse<ResubmitComplianceResponse>> resubmit(
            String id, File files, ResubmitComplianceRequest request,
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("compliance/applications")
            .addPathSegment(id);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
            try {
              multipartBodyBuilder.addFormDataPart("documents", request.getDocuments());
              String filesMimeType = Files.probeContentType(files.toPath());
              MediaType filesMimeTypeMediaType = filesMimeType != null ? MediaType.parse(filesMimeType) : null;
              multipartBodyBuilder.addFormDataPart("files", files.getName(), RequestBody.create(files, filesMimeTypeMediaType));
            }
            catch(Exception e) {
              throw new RuntimeException(e);
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("PATCH", multipartBodyBuilder.build())
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            CompletableFuture<SmallestAIHttpResponse<ResubmitComplianceResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ResubmitComplianceResponse.class), response));
                    return;
                  }
                  try {
                    switch (response.code()) {
                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                      return;
                      case 502:future.completeExceptionally(new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BadRequestErrorResponse.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (JsonProcessingException e) {
                  future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                }
                catch (IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          /**
           * Manually poll Plivo for the latest status of a compliance application.
           * Use this as a fallback when webhooks are delayed. The frontend enforces a
           * 60-second cooldown between refreshes.
           */
          public CompletableFuture<SmallestAIHttpResponse<PostComplianceApplicationsIdRefreshResponse>> refreshComplianceApplicationStatus(
              String id) {
            return refreshComplianceApplicationStatus(id,PostComplianceApplicationsIdRefreshRequest.builder().build());
          }

          /**
           * Manually poll Plivo for the latest status of a compliance application.
           * Use this as a fallback when webhooks are delayed. The frontend enforces a
           * 60-second cooldown between refreshes.
           */
          public CompletableFuture<SmallestAIHttpResponse<PostComplianceApplicationsIdRefreshResponse>> refreshComplianceApplicationStatus(
              String id, RequestOptions requestOptions) {
            return refreshComplianceApplicationStatus(id,PostComplianceApplicationsIdRefreshRequest.builder().build(),requestOptions);
          }

          /**
           * Manually poll Plivo for the latest status of a compliance application.
           * Use this as a fallback when webhooks are delayed. The frontend enforces a
           * 60-second cooldown between refreshes.
           */
          public CompletableFuture<SmallestAIHttpResponse<PostComplianceApplicationsIdRefreshResponse>> refreshComplianceApplicationStatus(
              String id, PostComplianceApplicationsIdRefreshRequest request) {
            return refreshComplianceApplicationStatus(id,request,null);
          }

          /**
           * Manually poll Plivo for the latest status of a compliance application.
           * Use this as a fallback when webhooks are delayed. The frontend enforces a
           * 60-second cooldown between refreshes.
           */
          public CompletableFuture<SmallestAIHttpResponse<PostComplianceApplicationsIdRefreshResponse>> refreshComplianceApplicationStatus(
              String id, PostComplianceApplicationsIdRefreshRequest request,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

              .addPathSegments("compliance/applications")
              .addPathSegment(id)
              .addPathSegments("refresh");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", RequestBody.create("", null))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
              }
              CompletableFuture<SmallestAIHttpResponse<PostComplianceApplicationsIdRefreshResponse>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      future.complete(new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostComplianceApplicationsIdRefreshResponse.class), response));
                      return;
                    }
                    try {
                      switch (response.code()) {
                        case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response));
                        return;
                        case 502:future.completeExceptionally(new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, BadRequestErrorResponse.class), response));
                        return;
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (JsonProcessingException e) {
                    future.completeExceptionally(new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e));
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new SmallestAIException("Network error executing HTTP request", e));
                }
              });
              return future;
            }
          }
