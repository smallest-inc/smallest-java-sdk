/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.compliance.requests;

import ai.smallest.core.ObjectMappers;
import ai.smallest.resources.atoms.compliance.types.SubmitComplianceRequestNumberType;
import ai.smallest.resources.atoms.compliance.types.SubmitComplianceRequestUserType;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = SubmitComplianceRequest.Builder.class
)
public final class SubmitComplianceRequest {
  private final String countryIso;

  private final SubmitComplianceRequestNumberType numberType;

  private final SubmitComplianceRequestUserType userType;

  private final String endUser;

  private final String documents;

  private final Map<String, Object> additionalProperties;

  private SubmitComplianceRequest(String countryIso, SubmitComplianceRequestNumberType numberType,
      SubmitComplianceRequestUserType userType, String endUser, String documents,
      Map<String, Object> additionalProperties) {
    this.countryIso = countryIso;
    this.numberType = numberType;
    this.userType = userType;
    this.endUser = endUser;
    this.documents = documents;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return ISO 3166-1 alpha-2 country code
   */
  @JsonProperty("countryIso")
  public String getCountryIso() {
    return countryIso;
  }

  /**
   * @return The type of phone number
   */
  @JsonProperty("numberType")
  public SubmitComplianceRequestNumberType getNumberType() {
    return numberType;
  }

  /**
   * @return The type of end user
   */
  @JsonProperty("userType")
  public SubmitComplianceRequestUserType getUserType() {
    return userType;
  }

  /**
   * @return JSON-stringified end-user details. <code>name</code> is required; all other fields are optional
   * but may be required by Plivo depending on country/numberType. Accepted fields:
   * <ul>
   * <li><code>name</code> (required) — full name or business name</li>
   * <li><code>lastName</code> — last name</li>
   * <li><code>email</code> — email address</li>
   * <li><code>addressLine1</code> — street address line 1</li>
   * <li><code>addressLine2</code> — street address line 2</li>
   * <li><code>city</code> — city</li>
   * <li><code>state</code> — state or province</li>
   * <li><code>postalCode</code> — postal/ZIP code</li>
   * <li><code>country</code> — ISO country code; defaults to <code>countryIso</code> if omitted</li>
   * <li><code>registrationNumber</code> — business registration number (required for some business applications)</li>
   * </ul>
   */
  @JsonProperty("endUser")
  public String getEndUser() {
    return endUser;
  }

  /**
   * @return JSON string containing an array of document metadata. Each entry must have a
   * <code>documentTypeId</code> (from the requirements endpoint) and optional <code>dataFields</code>.
   * Example:
   * <pre><code class="language-json">[{&quot;documentTypeId&quot;: &quot;dt_123&quot;, &quot;dataFields&quot;: {&quot;business_name&quot;: &quot;Acme Corp&quot;}}]
   * </code></pre>
   */
  @JsonProperty("documents")
  public String getDocuments() {
    return documents;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof SubmitComplianceRequest && equalTo((SubmitComplianceRequest) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(SubmitComplianceRequest other) {
    return countryIso.equals(other.countryIso) && numberType.equals(other.numberType) && userType.equals(other.userType) && endUser.equals(other.endUser) && documents.equals(other.documents);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.countryIso, this.numberType, this.userType, this.endUser, this.documents);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static CountryIsoStage builder() {
    return new Builder();
  }

  public interface CountryIsoStage {
    /**
     * <p>ISO 3166-1 alpha-2 country code</p>
     */
    NumberTypeStage countryIso(@NotNull String countryIso);

    Builder from(SubmitComplianceRequest other);
  }

  public interface NumberTypeStage {
    /**
     * <p>The type of phone number</p>
     */
    UserTypeStage numberType(@NotNull SubmitComplianceRequestNumberType numberType);
  }

  public interface UserTypeStage {
    /**
     * <p>The type of end user</p>
     */
    EndUserStage userType(@NotNull SubmitComplianceRequestUserType userType);
  }

  public interface EndUserStage {
    /**
     * <p>JSON-stringified end-user details. <code>name</code> is required; all other fields are optional
     * but may be required by Plivo depending on country/numberType. Accepted fields:</p>
     * <ul>
     * <li><code>name</code> (required) — full name or business name</li>
     * <li><code>lastName</code> — last name</li>
     * <li><code>email</code> — email address</li>
     * <li><code>addressLine1</code> — street address line 1</li>
     * <li><code>addressLine2</code> — street address line 2</li>
     * <li><code>city</code> — city</li>
     * <li><code>state</code> — state or province</li>
     * <li><code>postalCode</code> — postal/ZIP code</li>
     * <li><code>country</code> — ISO country code; defaults to <code>countryIso</code> if omitted</li>
     * <li><code>registrationNumber</code> — business registration number (required for some business applications)</li>
     * </ul>
     */
    DocumentsStage endUser(@NotNull String endUser);
  }

  public interface DocumentsStage {
    /**
     * <p>JSON string containing an array of document metadata. Each entry must have a
     * <code>documentTypeId</code> (from the requirements endpoint) and optional <code>dataFields</code>.
     * Example:</p>
     * <pre><code class="language-json">[{&quot;documentTypeId&quot;: &quot;dt_123&quot;, &quot;dataFields&quot;: {&quot;business_name&quot;: &quot;Acme Corp&quot;}}]
     * </code></pre>
     */
    _FinalStage documents(@NotNull String documents);
  }

  public interface _FinalStage {
    SubmitComplianceRequest build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements CountryIsoStage, NumberTypeStage, UserTypeStage, EndUserStage, DocumentsStage, _FinalStage {
    private String countryIso;

    private SubmitComplianceRequestNumberType numberType;

    private SubmitComplianceRequestUserType userType;

    private String endUser;

    private String documents;

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(SubmitComplianceRequest other) {
      countryIso(other.getCountryIso());
      numberType(other.getNumberType());
      userType(other.getUserType());
      endUser(other.getEndUser());
      documents(other.getDocuments());
      return this;
    }

    /**
     * <p>ISO 3166-1 alpha-2 country code</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("countryIso")
    public NumberTypeStage countryIso(@NotNull String countryIso) {
      this.countryIso = Objects.requireNonNull(countryIso, "countryIso must not be null");
      return this;
    }

    /**
     * <p>The type of phone number</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("numberType")
    public UserTypeStage numberType(@NotNull SubmitComplianceRequestNumberType numberType) {
      this.numberType = Objects.requireNonNull(numberType, "numberType must not be null");
      return this;
    }

    /**
     * <p>The type of end user</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("userType")
    public EndUserStage userType(@NotNull SubmitComplianceRequestUserType userType) {
      this.userType = Objects.requireNonNull(userType, "userType must not be null");
      return this;
    }

    /**
     * <p>JSON-stringified end-user details. <code>name</code> is required; all other fields are optional
     * but may be required by Plivo depending on country/numberType. Accepted fields:</p>
     * <ul>
     * <li><code>name</code> (required) — full name or business name</li>
     * <li><code>lastName</code> — last name</li>
     * <li><code>email</code> — email address</li>
     * <li><code>addressLine1</code> — street address line 1</li>
     * <li><code>addressLine2</code> — street address line 2</li>
     * <li><code>city</code> — city</li>
     * <li><code>state</code> — state or province</li>
     * <li><code>postalCode</code> — postal/ZIP code</li>
     * <li><code>country</code> — ISO country code; defaults to <code>countryIso</code> if omitted</li>
     * <li><code>registrationNumber</code> — business registration number (required for some business applications)</li>
     * </ul>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("endUser")
    public DocumentsStage endUser(@NotNull String endUser) {
      this.endUser = Objects.requireNonNull(endUser, "endUser must not be null");
      return this;
    }

    /**
     * <p>JSON string containing an array of document metadata. Each entry must have a
     * <code>documentTypeId</code> (from the requirements endpoint) and optional <code>dataFields</code>.
     * Example:</p>
     * <pre><code class="language-json">[{&quot;documentTypeId&quot;: &quot;dt_123&quot;, &quot;dataFields&quot;: {&quot;business_name&quot;: &quot;Acme Corp&quot;}}]
     * </code></pre>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("documents")
    public _FinalStage documents(@NotNull String documents) {
      this.documents = Objects.requireNonNull(documents, "documents must not be null");
      return this;
    }

    @java.lang.Override
    public SubmitComplianceRequest build() {
      return new SubmitComplianceRequest(countryIso, numberType, userType, endUser, documents, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
