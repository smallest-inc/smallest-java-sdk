/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.atoms.knowledgebase;

import ai.smallest.core.ClientOptions;
import ai.smallest.core.FileStream;
import ai.smallest.core.MediaTypes;
import ai.smallest.core.ObjectMappers;
import ai.smallest.core.RequestOptions;
import ai.smallest.core.RetryInterceptor;
import ai.smallest.core.SmallestAIApiException;
import ai.smallest.core.SmallestAIException;
import ai.smallest.core.SmallestAIHttpResponse;
import ai.smallest.resources.atoms.errors.BadRequestError;
import ai.smallest.resources.atoms.errors.InternalServerError;
import ai.smallest.resources.atoms.errors.NotFoundError;
import ai.smallest.resources.atoms.errors.UnauthorizedError;
import ai.smallest.resources.atoms.errors.UnprocessableEntityError;
import ai.smallest.resources.atoms.knowledgebase.requests.CreateKnowledgeBaseRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.DeleteKnowledgeBaseRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.ExtractSitemapUrlsKnowledgeBaseRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.GetKnowledgeBaseRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.GetKnowledgebaseIdItemsRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.GetKnowledgebaseIdScrapedUrlsRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.PostKnowledgebaseCompeleteFileUploadRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.PostKnowledgebaseGetPresignedUrlRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.PostKnowledgebaseIdItemsUploadMediaRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.PostKnowledgebaseIdRequest;
import ai.smallest.resources.atoms.knowledgebase.requests.ScrapeUrlsKnowledgeBaseRequest;
import ai.smallest.resources.atoms.knowledgebase.types.CreateKnowledgeBaseResponse;
import ai.smallest.resources.atoms.knowledgebase.types.DeleteKnowledgeBaseResponse;
import ai.smallest.resources.atoms.knowledgebase.types.DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdResponse;
import ai.smallest.resources.atoms.knowledgebase.types.DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdResponse;
import ai.smallest.resources.atoms.knowledgebase.types.ExtractSitemapUrlsKnowledgeBaseResponse;
import ai.smallest.resources.atoms.knowledgebase.types.GetKnowledgeBaseResponse;
import ai.smallest.resources.atoms.knowledgebase.types.GetKnowledgebaseIdItemsResponse;
import ai.smallest.resources.atoms.knowledgebase.types.GetKnowledgebaseIdScrapedUrlsResponse;
import ai.smallest.resources.atoms.knowledgebase.types.ListKnowledgeBaseResponse;
import ai.smallest.resources.atoms.knowledgebase.types.PostKnowledgebaseCompeleteFileUploadResponse;
import ai.smallest.resources.atoms.knowledgebase.types.PostKnowledgebaseGetPresignedUrlResponse;
import ai.smallest.resources.atoms.knowledgebase.types.PostKnowledgebaseIdItemsUploadMediaResponse;
import ai.smallest.resources.atoms.knowledgebase.types.PostKnowledgebaseIdResponse;
import ai.smallest.resources.atoms.knowledgebase.types.ScrapeUrlsKnowledgeBaseResponse;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.File;
import java.io.IOException;
import java.io.InputStream;
import java.lang.Exception;
import java.lang.Object;
import java.lang.RuntimeException;
import java.lang.String;
import java.nio.file.Files;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.MediaType;
import okhttp3.MultipartBody;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawKnowledgeBaseClient {
  protected final ClientOptions clientOptions;

  public RawKnowledgeBaseClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Get all knowledge bases
   */
  public SmallestAIHttpResponse<ListKnowledgeBaseResponse> list() {
    return list(null);
  }

  /**
   * Get all knowledge bases
   */
  public SmallestAIHttpResponse<ListKnowledgeBaseResponse> list(RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

      .addPathSegments("knowledgebase");if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request okhttpRequest = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json")
        .build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListKnowledgeBaseResponse.class), response);
        }
        try {
          switch (response.code()) {
            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (JsonProcessingException e) {
        throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
      }
      catch (IOException e) {
        throw new SmallestAIException("Network error executing HTTP request", e);
      }
    }

    /**
     * Create a knowledge base
     */
    public SmallestAIHttpResponse<CreateKnowledgeBaseResponse> create(
        CreateKnowledgeBaseRequest request) {
      return create(request,null);
    }

    /**
     * Create a knowledge base
     */
    public SmallestAIHttpResponse<CreateKnowledgeBaseResponse> create(
        CreateKnowledgeBaseRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

        .addPathSegments("knowledgebase");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(JsonProcessingException e) {
          throw new SmallestAIException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json")
          .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateKnowledgeBaseResponse.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (JsonProcessingException e) {
          throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
        }
        catch (IOException e) {
          throw new SmallestAIException("Network error executing HTTP request", e);
        }
      }

      /**
       * Get a knowledge base
       */
      public SmallestAIHttpResponse<GetKnowledgeBaseResponse> get(String id) {
        return get(id,GetKnowledgeBaseRequest.builder().build());
      }

      /**
       * Get a knowledge base
       */
      public SmallestAIHttpResponse<GetKnowledgeBaseResponse> get(String id,
          RequestOptions requestOptions) {
        return get(id,GetKnowledgeBaseRequest.builder().build(),requestOptions);
      }

      /**
       * Get a knowledge base
       */
      public SmallestAIHttpResponse<GetKnowledgeBaseResponse> get(String id,
          GetKnowledgeBaseRequest request) {
        return get(id,request,null);
      }

      /**
       * Get a knowledge base
       */
      public SmallestAIHttpResponse<GetKnowledgeBaseResponse> get(String id,
          GetKnowledgeBaseRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

          .addPathSegments("knowledgebase")
          .addPathSegment(id);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetKnowledgeBaseResponse.class), response);
            }
            try {
              switch (response.code()) {
                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (JsonProcessingException e) {
            throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
          }
          catch (IOException e) {
            throw new SmallestAIException("Network error executing HTTP request", e);
          }
        }

        /**
         * Updates the metadata of a knowledge base. <strong>Note</strong>: the platform uses <code>POST</code> (not <code>PATCH</code>) on this path — preserved here as-is.
         * <p>Only <code>name</code> and <code>description</code> are mutable through this endpoint. To add or remove content (files, URLs, text snippets), use the items endpoints.</p>
         */
        public SmallestAIHttpResponse<PostKnowledgebaseIdResponse> updateAKnowledgeBaseNameDescription(
            String id, PostKnowledgebaseIdRequest request) {
          return updateAKnowledgeBaseNameDescription(id,request,null);
        }

        /**
         * Updates the metadata of a knowledge base. <strong>Note</strong>: the platform uses <code>POST</code> (not <code>PATCH</code>) on this path — preserved here as-is.
         * <p>Only <code>name</code> and <code>description</code> are mutable through this endpoint. To add or remove content (files, URLs, text snippets), use the items endpoints.</p>
         */
        public SmallestAIHttpResponse<PostKnowledgebaseIdResponse> updateAKnowledgeBaseNameDescription(
            String id, PostKnowledgebaseIdRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

            .addPathSegments("knowledgebase")
            .addPathSegment(id);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            RequestBody body;
            try {
              body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
            }
            catch(JsonProcessingException e) {
              throw new SmallestAIException("Failed to serialize request", e);
            }
            Request okhttpRequest = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", body)
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Content-Type", "application/json")
              .addHeader("Accept", "application/json")
              .build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostKnowledgebaseIdResponse.class), response);
              }
              try {
                switch (response.code()) {
                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (JsonProcessingException e) {
              throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
            }
            catch (IOException e) {
              throw new SmallestAIException("Network error executing HTTP request", e);
            }
          }

          /**
           * Delete a knowledge base.
           * <p><strong>400 is returned when the knowledge base is still linked to an agent:</strong>
           * <code>&quot;This knowledge base is connected to an agent. Please detach it from the agent before deleting.&quot;</code>
           * Detach the KB from all agents (via agent config) before attempting deletion.</p>
           */
          public SmallestAIHttpResponse<DeleteKnowledgeBaseResponse> delete(String id) {
            return delete(id,DeleteKnowledgeBaseRequest.builder().build());
          }

          /**
           * Delete a knowledge base.
           * <p><strong>400 is returned when the knowledge base is still linked to an agent:</strong>
           * <code>&quot;This knowledge base is connected to an agent. Please detach it from the agent before deleting.&quot;</code>
           * Detach the KB from all agents (via agent config) before attempting deletion.</p>
           */
          public SmallestAIHttpResponse<DeleteKnowledgeBaseResponse> delete(String id,
              RequestOptions requestOptions) {
            return delete(id,DeleteKnowledgeBaseRequest.builder().build(),requestOptions);
          }

          /**
           * Delete a knowledge base.
           * <p><strong>400 is returned when the knowledge base is still linked to an agent:</strong>
           * <code>&quot;This knowledge base is connected to an agent. Please detach it from the agent before deleting.&quot;</code>
           * Detach the KB from all agents (via agent config) before attempting deletion.</p>
           */
          public SmallestAIHttpResponse<DeleteKnowledgeBaseResponse> delete(String id,
              DeleteKnowledgeBaseRequest request) {
            return delete(id,request,null);
          }

          /**
           * Delete a knowledge base.
           * <p><strong>400 is returned when the knowledge base is still linked to an agent:</strong>
           * <code>&quot;This knowledge base is connected to an agent. Please detach it from the agent before deleting.&quot;</code>
           * Detach the KB from all agents (via agent config) before attempting deletion.</p>
           */
          public SmallestAIHttpResponse<DeleteKnowledgeBaseResponse> delete(String id,
              DeleteKnowledgeBaseRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

              .addPathSegments("knowledgebase")
              .addPathSegment(id);if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeleteKnowledgeBaseResponse.class), response);
                }
                try {
                  switch (response.code()) {
                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (JsonProcessingException e) {
                throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
              }
              catch (IOException e) {
                throw new SmallestAIException("Network error executing HTTP request", e);
              }
            }

            /**
             * Get all knowledge base items
             */
            public SmallestAIHttpResponse<GetKnowledgebaseIdItemsResponse> getAllKnowledgeBaseItems(
                String id) {
              return getAllKnowledgeBaseItems(id,GetKnowledgebaseIdItemsRequest.builder().build());
            }

            /**
             * Get all knowledge base items
             */
            public SmallestAIHttpResponse<GetKnowledgebaseIdItemsResponse> getAllKnowledgeBaseItems(
                String id, RequestOptions requestOptions) {
              return getAllKnowledgeBaseItems(id,GetKnowledgebaseIdItemsRequest.builder().build(),requestOptions);
            }

            /**
             * Get all knowledge base items
             */
            public SmallestAIHttpResponse<GetKnowledgebaseIdItemsResponse> getAllKnowledgeBaseItems(
                String id, GetKnowledgebaseIdItemsRequest request) {
              return getAllKnowledgeBaseItems(id,request,null);
            }

            /**
             * Get all knowledge base items
             */
            public SmallestAIHttpResponse<GetKnowledgebaseIdItemsResponse> getAllKnowledgeBaseItems(
                String id, GetKnowledgebaseIdItemsRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                .addPathSegments("knowledgebase")
                .addPathSegment(id)
                .addPathSegments("items");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("GET", null)
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Accept", "application/json");
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                  okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetKnowledgebaseIdItemsResponse.class), response);
                  }
                  try {
                    switch (response.code()) {
                      case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (JsonProcessingException e) {
                  throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                }
                catch (IOException e) {
                  throw new SmallestAIException("Network error executing HTTP request", e);
                }
              }

              /**
               * Delete a knowledge base item
               */
              public SmallestAIHttpResponse<DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdResponse> deleteAKnowledgeBaseItem(
                  String knowledgeBaseId, String knowledgeBaseItemId) {
                return deleteAKnowledgeBaseItem(knowledgeBaseId,knowledgeBaseItemId,DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdRequest.builder().build());
              }

              /**
               * Delete a knowledge base item
               */
              public SmallestAIHttpResponse<DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdResponse> deleteAKnowledgeBaseItem(
                  String knowledgeBaseId, String knowledgeBaseItemId,
                  RequestOptions requestOptions) {
                return deleteAKnowledgeBaseItem(knowledgeBaseId,knowledgeBaseItemId,DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdRequest.builder().build(),requestOptions);
              }

              /**
               * Delete a knowledge base item
               */
              public SmallestAIHttpResponse<DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdResponse> deleteAKnowledgeBaseItem(
                  String knowledgeBaseId, String knowledgeBaseItemId,
                  DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdRequest request) {
                return deleteAKnowledgeBaseItem(knowledgeBaseId,knowledgeBaseItemId,request,null);
              }

              /**
               * Delete a knowledge base item
               */
              public SmallestAIHttpResponse<DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdResponse> deleteAKnowledgeBaseItem(
                  String knowledgeBaseId, String knowledgeBaseItemId,
                  DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdRequest request,
                  RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                  .addPathSegments("knowledgebase")
                  .addPathSegment(knowledgeBaseId)
                  .addPathSegments("items")
                  .addPathSegment(knowledgeBaseItemId);if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  Request.Builder _requestBuilder = new Request.Builder()
                    .url(httpUrl.build())
                    .method("DELETE", null)
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Accept", "application/json");
                  Request okhttpRequest = _requestBuilder.build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                    okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                  }
                  try (Response response = client.newCall(okhttpRequest).execute()) {
                    ResponseBody responseBody = response.body();
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeleteKnowledgebaseKnowledgeBaseIdItemsKnowledgeBaseItemIdResponse.class), response);
                    }
                    try {
                      switch (response.code()) {
                        case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                  }
                  catch (JsonProcessingException e) {
                    throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                  }
                  catch (IOException e) {
                    throw new SmallestAIException("Network error executing HTTP request", e);
                  }
                }

                /**
                 * Upload a PDF file to a knowledge base. Only PDF files are accepted (validated by MIME type and extension).
                 * <p><strong>400 is returned for billing/entitlement failures before the file is processed:</strong></p>
                 * <ul>
                 * <li><code>&quot;Insufficient credits for KB storage upload.&quot;</code> — account lacks upload credits</li>
                 * <li><code>&quot;KB storage access is not enabled for your account.&quot;</code> — plan doesn't include KB storage</li>
                 * </ul>
                 * <p>No application-level file size limit is enforced — any proxy or infrastructure limits (e.g. nginx) apply instead.</p>
                 */
                public SmallestAIHttpResponse<PostKnowledgebaseIdItemsUploadMediaResponse> uploadAPdfFileToAKnowledgeBase(
                    String id, File media, PostKnowledgebaseIdItemsUploadMediaRequest request) {
                  return uploadAPdfFileToAKnowledgeBase(id,media,request,null);
                }

                /**
                 * Upload a PDF file to a knowledge base. Only PDF files are accepted (validated by MIME type and extension).
                 * <p><strong>400 is returned for billing/entitlement failures before the file is processed:</strong></p>
                 * <ul>
                 * <li><code>&quot;Insufficient credits for KB storage upload.&quot;</code> — account lacks upload credits</li>
                 * <li><code>&quot;KB storage access is not enabled for your account.&quot;</code> — plan doesn't include KB storage</li>
                 * </ul>
                 * <p>No application-level file size limit is enforced — any proxy or infrastructure limits (e.g. nginx) apply instead.</p>
                 */
                public SmallestAIHttpResponse<PostKnowledgebaseIdItemsUploadMediaResponse> uploadAPdfFileToAKnowledgeBase(
                    String id, File media, PostKnowledgebaseIdItemsUploadMediaRequest request,
                    RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                    .addPathSegments("knowledgebase")
                    .addPathSegment(id)
                    .addPathSegments("items")
                    .addPathSegments("upload-media");if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
                    try {
                      String mediaMimeType = Files.probeContentType(media.toPath());
                      MediaType mediaMimeTypeMediaType = mediaMimeType != null ? MediaType.parse(mediaMimeType) : null;
                      multipartBodyBuilder.addFormDataPart("media", media.getName(), RequestBody.create(media, mediaMimeTypeMediaType));
                    }
                    catch(Exception e) {
                      throw new RuntimeException(e);
                    }
                    Request.Builder _requestBuilder = new Request.Builder()
                      .url(httpUrl.build())
                      .method("POST", multipartBodyBuilder.build())
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Accept", "application/json");
                    Request okhttpRequest = _requestBuilder.build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                      okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                    }
                    try (Response response = client.newCall(okhttpRequest).execute()) {
                      ResponseBody responseBody = response.body();
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostKnowledgebaseIdItemsUploadMediaResponse.class), response);
                      }
                      try {
                        switch (response.code()) {
                          case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                          case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                          case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                    }
                    catch (JsonProcessingException e) {
                      throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                    }
                    catch (IOException e) {
                      throw new SmallestAIException("Network error executing HTTP request", e);
                    }
                  }

                  public SmallestAIHttpResponse<PostKnowledgebaseIdItemsUploadMediaResponse> uploadAPdfFileToAKnowledgeBase(
                      String id, InputStream stream, String filename) {
                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                      .addPathSegments("knowledgebase")
                      .addPathSegment(id)
                      .addPathSegments("items")
                      .addPathSegments("upload-media");FileStream fs = new FileStream(stream, filename, null);
                      MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
                      multipartBodyBuilder.addFormDataPart("media", filename, fs.toRequestBody());
                      RequestBody body = multipartBodyBuilder.build();
                      Request.Builder _requestBuilder = new Request.Builder();
                      _requestBuilder.url(httpUrl.build());
                      _requestBuilder.method("POST", body);
                      _requestBuilder.headers(Headers.of(this.clientOptions.headers((RequestOptions) null)));
                      Request okhttpRequest = _requestBuilder.build();
                      OkHttpClient client = clientOptions.httpClient();
                      try (Response response = client.newCall(okhttpRequest).execute()) {
                        ResponseBody responseBody = response.body();
                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                        if (response.isSuccessful()) {
                          return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostKnowledgebaseIdItemsUploadMediaResponse.class), response);
                        }
                        try {
                          switch (response.code()) {
                            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                          }
                        }
                        catch (JsonProcessingException ignored) {
                          // unable to map error response, throwing generic error
                        }
                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                        throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                      }
                      catch (JsonProcessingException e) {
                        throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                      }
                      catch (IOException e) {
                        throw new SmallestAIException("Network error executing HTTP request", e);
                      }
                    }

                    public SmallestAIHttpResponse<PostKnowledgebaseIdItemsUploadMediaResponse> uploadAPdfFileToAKnowledgeBase(
                        String id, InputStream stream, String filename, MediaType mediaType) {
                      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                        .addPathSegments("knowledgebase")
                        .addPathSegment(id)
                        .addPathSegments("items")
                        .addPathSegments("upload-media");FileStream fs = new FileStream(stream, filename, mediaType);
                        MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
                        multipartBodyBuilder.addFormDataPart("media", filename, fs.toRequestBody());
                        RequestBody body = multipartBodyBuilder.build();
                        Request.Builder _requestBuilder = new Request.Builder();
                        _requestBuilder.url(httpUrl.build());
                        _requestBuilder.method("POST", body);
                        _requestBuilder.headers(Headers.of(this.clientOptions.headers((RequestOptions) null)));
                        Request okhttpRequest = _requestBuilder.build();
                        OkHttpClient client = clientOptions.httpClient();
                        try (Response response = client.newCall(okhttpRequest).execute()) {
                          ResponseBody responseBody = response.body();
                          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                          if (response.isSuccessful()) {
                            return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostKnowledgebaseIdItemsUploadMediaResponse.class), response);
                          }
                          try {
                            switch (response.code()) {
                              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                            }
                          }
                          catch (JsonProcessingException ignored) {
                            // unable to map error response, throwing generic error
                          }
                          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                          throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                        }
                        catch (JsonProcessingException e) {
                          throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                        }
                        catch (IOException e) {
                          throw new SmallestAIException("Network error executing HTTP request", e);
                        }
                      }

                      public SmallestAIHttpResponse<PostKnowledgebaseIdItemsUploadMediaResponse> uploadAPdfFileToAKnowledgeBase(
                          String id, InputStream stream, String filename,
                          RequestOptions requestOptions) {
                        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                          .addPathSegments("knowledgebase")
                          .addPathSegment(id)
                          .addPathSegments("items")
                          .addPathSegments("upload-media");if (requestOptions != null) {
                            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                              httpUrl.addQueryParameter(_key, _value);
                            } );
                          }
                          FileStream fs = new FileStream(stream, filename, null);
                          MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
                          multipartBodyBuilder.addFormDataPart("media", filename, fs.toRequestBody());
                          RequestBody body = multipartBodyBuilder.build();
                          Request.Builder _requestBuilder = new Request.Builder();
                          _requestBuilder.url(httpUrl.build());
                          _requestBuilder.method("POST", body);
                          _requestBuilder.headers(Headers.of(this.clientOptions.headers(requestOptions)));
                          Request okhttpRequest = _requestBuilder.build();
                          OkHttpClient client = clientOptions.httpClient();
                          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                            client = clientOptions.httpClientWithTimeout(requestOptions);
                          }
                          if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                            okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                          }
                          try (Response response = client.newCall(okhttpRequest).execute()) {
                            ResponseBody responseBody = response.body();
                            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                            if (response.isSuccessful()) {
                              return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostKnowledgebaseIdItemsUploadMediaResponse.class), response);
                            }
                            try {
                              switch (response.code()) {
                                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                              }
                            }
                            catch (JsonProcessingException ignored) {
                              // unable to map error response, throwing generic error
                            }
                            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                            throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                          }
                          catch (JsonProcessingException e) {
                            throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                          }
                          catch (IOException e) {
                            throw new SmallestAIException("Network error executing HTTP request", e);
                          }
                        }

                        public SmallestAIHttpResponse<PostKnowledgebaseIdItemsUploadMediaResponse> uploadAPdfFileToAKnowledgeBase(
                            String id, InputStream stream, String filename, MediaType mediaType,
                            RequestOptions requestOptions) {
                          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                            .addPathSegments("knowledgebase")
                            .addPathSegment(id)
                            .addPathSegments("items")
                            .addPathSegments("upload-media");if (requestOptions != null) {
                              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                httpUrl.addQueryParameter(_key, _value);
                              } );
                            }
                            FileStream fs = new FileStream(stream, filename, mediaType);
                            MultipartBody.Builder multipartBodyBuilder = new MultipartBody.Builder().setType(MultipartBody.FORM);
                            multipartBodyBuilder.addFormDataPart("media", filename, fs.toRequestBody());
                            RequestBody body = multipartBodyBuilder.build();
                            Request.Builder _requestBuilder = new Request.Builder();
                            _requestBuilder.url(httpUrl.build());
                            _requestBuilder.method("POST", body);
                            _requestBuilder.headers(Headers.of(this.clientOptions.headers(requestOptions)));
                            Request okhttpRequest = _requestBuilder.build();
                            OkHttpClient client = clientOptions.httpClient();
                            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                              client = clientOptions.httpClientWithTimeout(requestOptions);
                            }
                            if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                              okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                            }
                            try (Response response = client.newCall(okhttpRequest).execute()) {
                              ResponseBody responseBody = response.body();
                              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                              if (response.isSuccessful()) {
                                return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostKnowledgebaseIdItemsUploadMediaResponse.class), response);
                              }
                              try {
                                switch (response.code()) {
                                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                  case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                  case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                }
                              }
                              catch (JsonProcessingException ignored) {
                                // unable to map error response, throwing generic error
                              }
                              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                              throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                            }
                            catch (JsonProcessingException e) {
                              throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                            }
                            catch (IOException e) {
                              throw new SmallestAIException("Network error executing HTTP request", e);
                            }
                          }

                          /**
                           * Two-step file upload flow that bypasses Atoms' API for the file bytes themselves — useful when files exceed the multipart upload limit on <code>POST /knowledgebase/{id}/items/upload-media</code> or when you want to upload from the browser without round-tripping through your backend.
                           * <p><strong>Step 1</strong>: Call this endpoint with file metadata. Atoms returns a presigned URL + a storage <code>key</code>.
                           * <strong>Step 2</strong>: <code>PUT</code> the file bytes directly to the presigned URL (set <code>Content-Type</code> to the same value you sent here).
                           * <strong>Step 3</strong>: Call <a href="#operation/completeKnowledgeBaseFileUpload"><code>POST /knowledgebase/compelete-file-upload</code></a> with the same <code>key</code> to commit the upload and start processing.</p>
                           * <p>Same end result as <code>POST /knowledgebase/{id}/items/upload-media</code>, just without the multipart-through-our-API hop.</p>
                           */
                          public SmallestAIHttpResponse<PostKnowledgebaseGetPresignedUrlResponse> getAPresignedS3UrlForDirectFileUpload(
                              PostKnowledgebaseGetPresignedUrlRequest request) {
                            return getAPresignedS3UrlForDirectFileUpload(request,null);
                          }

                          /**
                           * Two-step file upload flow that bypasses Atoms' API for the file bytes themselves — useful when files exceed the multipart upload limit on <code>POST /knowledgebase/{id}/items/upload-media</code> or when you want to upload from the browser without round-tripping through your backend.
                           * <p><strong>Step 1</strong>: Call this endpoint with file metadata. Atoms returns a presigned URL + a storage <code>key</code>.
                           * <strong>Step 2</strong>: <code>PUT</code> the file bytes directly to the presigned URL (set <code>Content-Type</code> to the same value you sent here).
                           * <strong>Step 3</strong>: Call <a href="#operation/completeKnowledgeBaseFileUpload"><code>POST /knowledgebase/compelete-file-upload</code></a> with the same <code>key</code> to commit the upload and start processing.</p>
                           * <p>Same end result as <code>POST /knowledgebase/{id}/items/upload-media</code>, just without the multipart-through-our-API hop.</p>
                           */
                          public SmallestAIHttpResponse<PostKnowledgebaseGetPresignedUrlResponse> getAPresignedS3UrlForDirectFileUpload(
                              PostKnowledgebaseGetPresignedUrlRequest request,
                              RequestOptions requestOptions) {
                            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                              .addPathSegments("knowledgebase/get-presigned-url");if (requestOptions != null) {
                                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                  httpUrl.addQueryParameter(_key, _value);
                                } );
                              }
                              RequestBody body;
                              try {
                                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                              }
                              catch(JsonProcessingException e) {
                                throw new SmallestAIException("Failed to serialize request", e);
                              }
                              Request okhttpRequest = new Request.Builder()
                                .url(httpUrl.build())
                                .method("POST", body)
                                .headers(Headers.of(clientOptions.headers(requestOptions)))
                                .addHeader("Content-Type", "application/json")
                                .addHeader("Accept", "application/json")
                                .build();
                              OkHttpClient client = clientOptions.httpClient();
                              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                                client = clientOptions.httpClientWithTimeout(requestOptions);
                              }
                              if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                                okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                              }
                              try (Response response = client.newCall(okhttpRequest).execute()) {
                                ResponseBody responseBody = response.body();
                                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                if (response.isSuccessful()) {
                                  return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostKnowledgebaseGetPresignedUrlResponse.class), response);
                                }
                                try {
                                  switch (response.code()) {
                                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                    case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                    case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                  }
                                }
                                catch (JsonProcessingException ignored) {
                                  // unable to map error response, throwing generic error
                                }
                                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                              }
                              catch (JsonProcessingException e) {
                                throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                              }
                              catch (IOException e) {
                                throw new SmallestAIException("Network error executing HTTP request", e);
                              }
                            }

                            /**
                             * Step 3 of the presigned-URL upload flow. Commits a file that was uploaded directly to S3 via <code>POST /knowledgebase/get-presigned-url</code>, registers it as a knowledge-base item, and triggers async processing.
                             * <p><strong>Note</strong>: The path includes <code>compelete</code> (sic) — that's the actual route name on the platform. Don't fix the spelling in your client; it's a stable URL.</p>
                             */
                            public SmallestAIHttpResponse<PostKnowledgebaseCompeleteFileUploadResponse> completeAPresignedUrlUploadAndStartProcessing(
                                PostKnowledgebaseCompeleteFileUploadRequest request) {
                              return completeAPresignedUrlUploadAndStartProcessing(request,null);
                            }

                            /**
                             * Step 3 of the presigned-URL upload flow. Commits a file that was uploaded directly to S3 via <code>POST /knowledgebase/get-presigned-url</code>, registers it as a knowledge-base item, and triggers async processing.
                             * <p><strong>Note</strong>: The path includes <code>compelete</code> (sic) — that's the actual route name on the platform. Don't fix the spelling in your client; it's a stable URL.</p>
                             */
                            public SmallestAIHttpResponse<PostKnowledgebaseCompeleteFileUploadResponse> completeAPresignedUrlUploadAndStartProcessing(
                                PostKnowledgebaseCompeleteFileUploadRequest request,
                                RequestOptions requestOptions) {
                              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                                .addPathSegments("knowledgebase/compelete-file-upload");if (requestOptions != null) {
                                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                    httpUrl.addQueryParameter(_key, _value);
                                  } );
                                }
                                RequestBody body;
                                try {
                                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                                }
                                catch(JsonProcessingException e) {
                                  throw new SmallestAIException("Failed to serialize request", e);
                                }
                                Request okhttpRequest = new Request.Builder()
                                  .url(httpUrl.build())
                                  .method("POST", body)
                                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                                  .addHeader("Content-Type", "application/json")
                                  .addHeader("Accept", "application/json")
                                  .build();
                                OkHttpClient client = clientOptions.httpClient();
                                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                                  client = clientOptions.httpClientWithTimeout(requestOptions);
                                }
                                if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                                  okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                                }
                                try (Response response = client.newCall(okhttpRequest).execute()) {
                                  ResponseBody responseBody = response.body();
                                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                  if (response.isSuccessful()) {
                                    return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PostKnowledgebaseCompeleteFileUploadResponse.class), response);
                                  }
                                  try {
                                    switch (response.code()) {
                                      case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                      case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                      case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                    }
                                  }
                                  catch (JsonProcessingException ignored) {
                                    // unable to map error response, throwing generic error
                                  }
                                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                  throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                                }
                                catch (JsonProcessingException e) {
                                  throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                                }
                                catch (IOException e) {
                                  throw new SmallestAIException("Network error executing HTTP request", e);
                                }
                              }

                              /**
                               * Fetches a website's <code>sitemap.xml</code>, parses it, and returns the list of URLs inside. Use this before calling <code>POST /knowledgebase/{id}/scrape-urls</code> to let the customer pick which URLs they actually want indexed.
                               * <p>Returns <code>422</code> if the URL doesn't return a fetchable sitemap or if the XML is malformed.</p>
                               */
                              public SmallestAIHttpResponse<ExtractSitemapUrlsKnowledgeBaseResponse> extractSitemapUrls(
                                  ExtractSitemapUrlsKnowledgeBaseRequest request) {
                                return extractSitemapUrls(request,null);
                              }

                              /**
                               * Fetches a website's <code>sitemap.xml</code>, parses it, and returns the list of URLs inside. Use this before calling <code>POST /knowledgebase/{id}/scrape-urls</code> to let the customer pick which URLs they actually want indexed.
                               * <p>Returns <code>422</code> if the URL doesn't return a fetchable sitemap or if the XML is malformed.</p>
                               */
                              public SmallestAIHttpResponse<ExtractSitemapUrlsKnowledgeBaseResponse> extractSitemapUrls(
                                  ExtractSitemapUrlsKnowledgeBaseRequest request,
                                  RequestOptions requestOptions) {
                                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                                  .addPathSegments("knowledgebase/get-sitemap-urls");if (requestOptions != null) {
                                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                      httpUrl.addQueryParameter(_key, _value);
                                    } );
                                  }
                                  RequestBody body;
                                  try {
                                    body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                                  }
                                  catch(JsonProcessingException e) {
                                    throw new SmallestAIException("Failed to serialize request", e);
                                  }
                                  Request okhttpRequest = new Request.Builder()
                                    .url(httpUrl.build())
                                    .method("POST", body)
                                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                                    .addHeader("Content-Type", "application/json")
                                    .addHeader("Accept", "application/json")
                                    .build();
                                  OkHttpClient client = clientOptions.httpClient();
                                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                                    client = clientOptions.httpClientWithTimeout(requestOptions);
                                  }
                                  if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                                    okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                                  }
                                  try (Response response = client.newCall(okhttpRequest).execute()) {
                                    ResponseBody responseBody = response.body();
                                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                    if (response.isSuccessful()) {
                                      return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ExtractSitemapUrlsKnowledgeBaseResponse.class), response);
                                    }
                                    try {
                                      switch (response.code()) {
                                        case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                        case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                        case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                        case 422:throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                        case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                      }
                                    }
                                    catch (JsonProcessingException ignored) {
                                      // unable to map error response, throwing generic error
                                    }
                                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                    throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                                  }
                                  catch (JsonProcessingException e) {
                                    throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                                  }
                                  catch (IOException e) {
                                    throw new SmallestAIException("Network error executing HTTP request", e);
                                  }
                                }

                                /**
                                 * Adds one or more URLs to a knowledge base by scraping each page's content, chunking it, and indexing for retrieval. Typical flow:
                                 * <ol>
                                 * <li>Discover candidate URLs (<code>POST /knowledgebase/get-sitemap-urls</code> or paste your own list).</li>
                                 * <li>Call this endpoint with the curated list — scraping runs async.</li>
                                 * <li>Poll <code>GET /knowledgebase/{id}/scraped-urls</code> for the per-URL status.</li>
                                 * </ol>
                                 * <p>Returns <code>400</code> if your account's KB billing precheck fails (quota or plan limits). Returns <code>404</code> if the KB doesn't belong to your organization.</p>
                                 */
                                public SmallestAIHttpResponse<ScrapeUrlsKnowledgeBaseResponse> scrapeUrls(
                                    String id, ScrapeUrlsKnowledgeBaseRequest request) {
                                  return scrapeUrls(id,request,null);
                                }

                                /**
                                 * Adds one or more URLs to a knowledge base by scraping each page's content, chunking it, and indexing for retrieval. Typical flow:
                                 * <ol>
                                 * <li>Discover candidate URLs (<code>POST /knowledgebase/get-sitemap-urls</code> or paste your own list).</li>
                                 * <li>Call this endpoint with the curated list — scraping runs async.</li>
                                 * <li>Poll <code>GET /knowledgebase/{id}/scraped-urls</code> for the per-URL status.</li>
                                 * </ol>
                                 * <p>Returns <code>400</code> if your account's KB billing precheck fails (quota or plan limits). Returns <code>404</code> if the KB doesn't belong to your organization.</p>
                                 */
                                public SmallestAIHttpResponse<ScrapeUrlsKnowledgeBaseResponse> scrapeUrls(
                                    String id, ScrapeUrlsKnowledgeBaseRequest request,
                                    RequestOptions requestOptions) {
                                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                                    .addPathSegments("knowledgebase")
                                    .addPathSegment(id)
                                    .addPathSegments("scrape-urls");if (requestOptions != null) {
                                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                        httpUrl.addQueryParameter(_key, _value);
                                      } );
                                    }
                                    RequestBody body;
                                    try {
                                      body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                                    }
                                    catch(JsonProcessingException e) {
                                      throw new SmallestAIException("Failed to serialize request", e);
                                    }
                                    Request okhttpRequest = new Request.Builder()
                                      .url(httpUrl.build())
                                      .method("POST", body)
                                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                                      .addHeader("Content-Type", "application/json")
                                      .addHeader("Accept", "application/json")
                                      .build();
                                    OkHttpClient client = clientOptions.httpClient();
                                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                                      client = clientOptions.httpClientWithTimeout(requestOptions);
                                    }
                                    if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                                      okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                                    }
                                    try (Response response = client.newCall(okhttpRequest).execute()) {
                                      ResponseBody responseBody = response.body();
                                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                      if (response.isSuccessful()) {
                                        return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ScrapeUrlsKnowledgeBaseResponse.class), response);
                                      }
                                      try {
                                        switch (response.code()) {
                                          case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                          case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                          case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                          case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                        }
                                      }
                                      catch (JsonProcessingException ignored) {
                                        // unable to map error response, throwing generic error
                                      }
                                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                      throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                                    }
                                    catch (JsonProcessingException e) {
                                      throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                                    }
                                    catch (IOException e) {
                                      throw new SmallestAIException("Network error executing HTTP request", e);
                                    }
                                  }

                                  /**
                                   * Returns every URL added to the knowledge base via <code>POST /knowledgebase/{id}/scrape-urls</code>, with its current scrape/index status. Poll this after kicking off a scrape job to track progress.
                                   */
                                  public SmallestAIHttpResponse<GetKnowledgebaseIdScrapedUrlsResponse> listScrapedUrLsInAKnowledgeBaseTheirStatus(
                                      String id) {
                                    return listScrapedUrLsInAKnowledgeBaseTheirStatus(id,GetKnowledgebaseIdScrapedUrlsRequest.builder().build());
                                  }

                                  /**
                                   * Returns every URL added to the knowledge base via <code>POST /knowledgebase/{id}/scrape-urls</code>, with its current scrape/index status. Poll this after kicking off a scrape job to track progress.
                                   */
                                  public SmallestAIHttpResponse<GetKnowledgebaseIdScrapedUrlsResponse> listScrapedUrLsInAKnowledgeBaseTheirStatus(
                                      String id, RequestOptions requestOptions) {
                                    return listScrapedUrLsInAKnowledgeBaseTheirStatus(id,GetKnowledgebaseIdScrapedUrlsRequest.builder().build(),requestOptions);
                                  }

                                  /**
                                   * Returns every URL added to the knowledge base via <code>POST /knowledgebase/{id}/scrape-urls</code>, with its current scrape/index status. Poll this after kicking off a scrape job to track progress.
                                   */
                                  public SmallestAIHttpResponse<GetKnowledgebaseIdScrapedUrlsResponse> listScrapedUrLsInAKnowledgeBaseTheirStatus(
                                      String id, GetKnowledgebaseIdScrapedUrlsRequest request) {
                                    return listScrapedUrLsInAKnowledgeBaseTheirStatus(id,request,null);
                                  }

                                  /**
                                   * Returns every URL added to the knowledge base via <code>POST /knowledgebase/{id}/scrape-urls</code>, with its current scrape/index status. Poll this after kicking off a scrape job to track progress.
                                   */
                                  public SmallestAIHttpResponse<GetKnowledgebaseIdScrapedUrlsResponse> listScrapedUrLsInAKnowledgeBaseTheirStatus(
                                      String id, GetKnowledgebaseIdScrapedUrlsRequest request,
                                      RequestOptions requestOptions) {
                                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                                      .addPathSegments("knowledgebase")
                                      .addPathSegment(id)
                                      .addPathSegments("scraped-urls");if (requestOptions != null) {
                                        requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                          httpUrl.addQueryParameter(_key, _value);
                                        } );
                                      }
                                      Request.Builder _requestBuilder = new Request.Builder()
                                        .url(httpUrl.build())
                                        .method("GET", null)
                                        .headers(Headers.of(clientOptions.headers(requestOptions)))
                                        .addHeader("Accept", "application/json");
                                      Request okhttpRequest = _requestBuilder.build();
                                      OkHttpClient client = clientOptions.httpClient();
                                      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                                        client = clientOptions.httpClientWithTimeout(requestOptions);
                                      }
                                      if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                                        okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                                      }
                                      try (Response response = client.newCall(okhttpRequest).execute()) {
                                        ResponseBody responseBody = response.body();
                                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                        if (response.isSuccessful()) {
                                          return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetKnowledgebaseIdScrapedUrlsResponse.class), response);
                                        }
                                        try {
                                          switch (response.code()) {
                                            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                          }
                                        }
                                        catch (JsonProcessingException ignored) {
                                          // unable to map error response, throwing generic error
                                        }
                                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                        throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                                      }
                                      catch (JsonProcessingException e) {
                                        throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                                      }
                                      catch (IOException e) {
                                        throw new SmallestAIException("Network error executing HTTP request", e);
                                      }
                                    }

                                    /**
                                     * Removes a previously-scraped URL (and its indexed content) from the knowledge base. Permanent — there is no undo.
                                     */
                                    public SmallestAIHttpResponse<DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdResponse> deleteAScrapedUrlFromAKnowledgeBase(
                                        String knowledgeBaseId, String knowledgeBaseScrapedUrlsId) {
                                      return deleteAScrapedUrlFromAKnowledgeBase(knowledgeBaseId,knowledgeBaseScrapedUrlsId,DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdRequest.builder().build());
                                    }

                                    /**
                                     * Removes a previously-scraped URL (and its indexed content) from the knowledge base. Permanent — there is no undo.
                                     */
                                    public SmallestAIHttpResponse<DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdResponse> deleteAScrapedUrlFromAKnowledgeBase(
                                        String knowledgeBaseId, String knowledgeBaseScrapedUrlsId,
                                        RequestOptions requestOptions) {
                                      return deleteAScrapedUrlFromAKnowledgeBase(knowledgeBaseId,knowledgeBaseScrapedUrlsId,DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdRequest.builder().build(),requestOptions);
                                    }

                                    /**
                                     * Removes a previously-scraped URL (and its indexed content) from the knowledge base. Permanent — there is no undo.
                                     */
                                    public SmallestAIHttpResponse<DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdResponse> deleteAScrapedUrlFromAKnowledgeBase(
                                        String knowledgeBaseId, String knowledgeBaseScrapedUrlsId,
                                        DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdRequest request) {
                                      return deleteAScrapedUrlFromAKnowledgeBase(knowledgeBaseId,knowledgeBaseScrapedUrlsId,request,null);
                                    }

                                    /**
                                     * Removes a previously-scraped URL (and its indexed content) from the knowledge base. Permanent — there is no undo.
                                     */
                                    public SmallestAIHttpResponse<DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdResponse> deleteAScrapedUrlFromAKnowledgeBase(
                                        String knowledgeBaseId, String knowledgeBaseScrapedUrlsId,
                                        DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdRequest request,
                                        RequestOptions requestOptions) {
                                      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getAtomsURL()).newBuilder()

                                        .addPathSegments("knowledgebase")
                                        .addPathSegment(knowledgeBaseId)
                                        .addPathSegments("scraped-urls")
                                        .addPathSegment(knowledgeBaseScrapedUrlsId);if (requestOptions != null) {
                                          requestOptions.getQueryParameters().forEach((_key, _value) -> {
                                            httpUrl.addQueryParameter(_key, _value);
                                          } );
                                        }
                                        Request.Builder _requestBuilder = new Request.Builder()
                                          .url(httpUrl.build())
                                          .method("DELETE", null)
                                          .headers(Headers.of(clientOptions.headers(requestOptions)))
                                          .addHeader("Accept", "application/json");
                                        Request okhttpRequest = _requestBuilder.build();
                                        OkHttpClient client = clientOptions.httpClient();
                                        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                                          client = clientOptions.httpClientWithTimeout(requestOptions);
                                        }
                                        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
                                          okhttpRequest = okhttpRequest.newBuilder().tag(RetryInterceptor.MaxRetriesOverride.class, new RetryInterceptor.MaxRetriesOverride(requestOptions.getMaxRetries().get())).build();
                                        }
                                        try (Response response = client.newCall(okhttpRequest).execute()) {
                                          ResponseBody responseBody = response.body();
                                          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                                          if (response.isSuccessful()) {
                                            return new SmallestAIHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeleteKnowledgebaseKnowledgeBaseIdScrapedUrlsKnowledgeBaseScrapedUrlsIdResponse.class), response);
                                          }
                                          try {
                                            switch (response.code()) {
                                              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class), response);
                                            }
                                          }
                                          catch (JsonProcessingException ignored) {
                                            // unable to map error response, throwing generic error
                                          }
                                          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                                          throw new SmallestAIApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                                        }
                                        catch (JsonProcessingException e) {
                                          throw new SmallestAIException("Failed to deserialize response: " + e.getMessage(), e);
                                        }
                                        catch (IOException e) {
                                          throw new SmallestAIException("Network error executing HTTP request", e);
                                        }
                                      }
                                    }
