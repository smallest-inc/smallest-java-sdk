/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.waves.speechtospeech.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Object;
import java.lang.String;

public final class SessionConfiguredType {
  public static final SessionConfiguredType SESSION_CONFIGURED = new SessionConfiguredType(Value.SESSION_CONFIGURED, "session.configured");

  private final Value value;

  private final String string;

  SessionConfiguredType(Value value, String string) {
    this.value = value;
    this.string = string;
  }

  public Value getEnumValue() {
    return value;
  }

  @java.lang.Override
  @JsonValue
  public String toString() {
    return this.string;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    return (this == other) 
      || (other instanceof SessionConfiguredType && this.string.equals(((SessionConfiguredType) other).string));
  }

  @java.lang.Override
  public int hashCode() {
    return this.string.hashCode();
  }

  public <T> T visit(Visitor<T> visitor) {
    switch (value) {
      case SESSION_CONFIGURED:
        return visitor.visitSessionConfigured();
      case UNKNOWN:
      default:
        return visitor.visitUnknown(string);
    }
  }

  @JsonCreator(
      mode = JsonCreator.Mode.DELEGATING
  )
  public static SessionConfiguredType valueOf(String value) {
    switch (value) {
      case "session.configured":
        return SESSION_CONFIGURED;
      default:
        return new SessionConfiguredType(Value.UNKNOWN, value);
    }
  }

  public enum Value {
    SESSION_CONFIGURED,

    UNKNOWN
  }

  public interface Visitor<T> {
    T visitSessionConfigured();

    T visitUnknown(String unknownType);
  }
}
