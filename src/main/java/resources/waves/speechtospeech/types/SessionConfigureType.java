/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.waves.speechtospeech.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Object;
import java.lang.String;

public final class SessionConfigureType {
  public static final SessionConfigureType SESSION_CONFIGURE = new SessionConfigureType(Value.SESSION_CONFIGURE, "session.configure");

  private final Value value;

  private final String string;

  SessionConfigureType(Value value, String string) {
    this.value = value;
    this.string = string;
  }

  public Value getEnumValue() {
    return value;
  }

  @java.lang.Override
  @JsonValue
  public String toString() {
    return this.string;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    return (this == other) 
      || (other instanceof SessionConfigureType && this.string.equals(((SessionConfigureType) other).string));
  }

  @java.lang.Override
  public int hashCode() {
    return this.string.hashCode();
  }

  public <T> T visit(Visitor<T> visitor) {
    switch (value) {
      case SESSION_CONFIGURE:
        return visitor.visitSessionConfigure();
      case UNKNOWN:
      default:
        return visitor.visitUnknown(string);
    }
  }

  @JsonCreator(
      mode = JsonCreator.Mode.DELEGATING
  )
  public static SessionConfigureType valueOf(String value) {
    switch (value) {
      case "session.configure":
        return SESSION_CONFIGURE;
      default:
        return new SessionConfigureType(Value.UNKNOWN, value);
    }
  }

  public enum Value {
    SESSION_CONFIGURE,

    UNKNOWN
  }

  public interface Visitor<T> {
    T visitSessionConfigure();

    T visitUnknown(String unknownType);
  }
}
