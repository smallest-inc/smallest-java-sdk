/**
 * This file was auto-generated by Fern from our API Definition.
 */

package ai.smallest.resources.waves.lightningv31tts.types;

import ai.smallest.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Boolean;
import java.lang.Double;
import java.lang.Integer;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = LightningV31TtsRequestMessage.Builder.class
)
public final class LightningV31TtsRequestMessage {
  private final String voiceId;

  private final String text;

  private final Optional<LightningV31TtsRequestMessageModel> model;

  private final Optional<Integer> maxBufferFlushMs;

  private final Optional<Boolean> continue_;

  private final Optional<Boolean> flush;

  private final Optional<Double> completeBackoffMs;

  private final Optional<LightningV31TtsRequestMessageLanguage> language;

  private final Optional<LightningV31TtsRequestMessageNumberPronunciationLanguage> numberPronunciationLanguage;

  private final Optional<Integer> sampleRate;

  private final Optional<Double> speed;

  private final Optional<String> sessionId;

  private final Optional<String> requestId;

  private final Optional<Boolean> wordTimestamps;

  private final Map<String, Object> additionalProperties;

  private LightningV31TtsRequestMessage(String voiceId, String text,
      Optional<LightningV31TtsRequestMessageModel> model, Optional<Integer> maxBufferFlushMs,
      Optional<Boolean> continue_, Optional<Boolean> flush, Optional<Double> completeBackoffMs,
      Optional<LightningV31TtsRequestMessageLanguage> language,
      Optional<LightningV31TtsRequestMessageNumberPronunciationLanguage> numberPronunciationLanguage,
      Optional<Integer> sampleRate, Optional<Double> speed, Optional<String> sessionId,
      Optional<String> requestId, Optional<Boolean> wordTimestamps,
      Map<String, Object> additionalProperties) {
    this.voiceId = voiceId;
    this.text = text;
    this.model = model;
    this.maxBufferFlushMs = maxBufferFlushMs;
    this.continue_ = continue_;
    this.flush = flush;
    this.completeBackoffMs = completeBackoffMs;
    this.language = language;
    this.numberPronunciationLanguage = numberPronunciationLanguage;
    this.sampleRate = sampleRate;
    this.speed = speed;
    this.sessionId = sessionId;
    this.requestId = requestId;
    this.wordTimestamps = wordTimestamps;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return The ID of the voice to use
   */
  @JsonProperty("voice_id")
  public String getVoiceId() {
    return voiceId;
  }

  /**
   * @return The text to convert to speech
   */
  @JsonProperty("text")
  public String getText() {
    return text;
  }

  /**
   * @return TTS model to route the request to.
   * <ul>
   * <li><code>lightning_v3.1</code> (default) — standard Lightning v3.1 pool.</li>
   * <li><code>lightning_v3.1_pro</code> — Lightning v3.1 Pro pool with a curated
   * voice catalog. See the
   * <a href="/waves/model-cards/text-to-speech/lightning-v-3-1-pro">Pro model card</a>.</li>
   * </ul>
   * <p>New integrations should connect to the unified
   * <code>/waves/v1/tts/live</code> WebSocket instead of this endpoint, but the
   * <code>model</code> field is supported here for backwards-compatible Pro
   * opt-in.</p>
   */
  @JsonProperty("model")
  public Optional<LightningV31TtsRequestMessageModel> getModel() {
    return model;
  }

  /**
   * @return The maximum time (in ms) to wait for more input before generating output. It flushes when either this time is reached or enough input is received for optimal output—whichever comes first. This is useful for input streams. Defaults to 0
   */
  @JsonProperty("max_buffer_flush_ms")
  public Optional<Integer> getMaxBufferFlushMs() {
    return maxBufferFlushMs;
  }

  /**
   * @return This setting controls whether the system should buffer and wait for more input after receiving the current one. If not set, it assumes no more input is coming.
   */
  @JsonProperty("continue")
  public Optional<Boolean> getContinue() {
    return continue_;
  }

  /**
   * @return This setting controls whether the system should flush the current buffer.
   */
  @JsonProperty("flush")
  public Optional<Boolean> getFlush() {
    return flush;
  }

  /**
   * @return The time in ms to wait after the last chunk is sent before sending the complete response. Default is 4000ms. Maximum is 10000ms.
   */
  @JsonProperty("complete_backoff_ms")
  public Optional<Double> getCompleteBackoffMs() {
    return completeBackoffMs;
  }

  /**
   * @return Language code for synthesis. Influences pronunciation, number/date
   * normalization, and phoneme selection.
   * <ul>
   * <li>Indian: <code>en</code>, <code>hi</code>, <code>mr</code>, <code>kn</code>, <code>ta</code>, <code>bn</code>, <code>gu</code>, <code>te</code>, <code>ml</code>, <code>pa</code>, <code>or</code></li>
   * <li>European: <code>es</code> (Spanish)</li>
   * </ul>
   */
  @JsonProperty("language")
  public Optional<LightningV31TtsRequestMessageLanguage> getLanguage() {
    return language;
  }

  /**
   * @return Optional. Sets the language used to read out numbers, dates,
   * and other text — the text-normalization language —
   * independently of the synthesis voice.
   * <ul>
   * <li>If you <strong>omit <code>language</code></strong>, this value also becomes the
   * synthesis language: model selection and voice routing
   * follow it.</li>
   * <li>If you <strong>set <code>language</code> explicitly</strong>, <code>language</code> always wins
   * for synthesis and <code>number_pronunciation_language</code> only
   * changes how numbers and text are normalized (for example,
   * read digits in Hindi while an English voice speaks the
   * surrounding words).</li>
   * <li>Omit this field to keep the existing behaviour —
   * normalization follows <code>language</code>.</li>
   * </ul>
   * <p>Accepts the same language codes as <code>language</code>.</p>
   */
  @JsonProperty("number_pronunciation_language")
  public Optional<LightningV31TtsRequestMessageNumberPronunciationLanguage> getNumberPronunciationLanguage(
      ) {
    return numberPronunciationLanguage;
  }

  /**
   * @return Audio sample rate in Hz. Supported values: 8000, 16000, 24000, 44100
   */
  @JsonProperty("sample_rate")
  public Optional<Integer> getSampleRate() {
    return sampleRate;
  }

  /**
   * @return Speaking speed multiplier
   */
  @JsonProperty("speed")
  public Optional<Double> getSpeed() {
    return speed;
  }

  /**
   * @return Optional client-provided session identifier for correlation. Only alphanumeric characters, hyphens, underscores, and dots allowed. Max 128 characters. Echoed back in responses as <code>external_session_id</code>.
   */
  @JsonProperty("session_id")
  public Optional<String> getSessionId() {
    return sessionId;
  }

  /**
   * @return Optional client-provided request identifier for correlation. Only alphanumeric characters, hyphens, underscores, and dots allowed. Max 128 characters. Echoed back in responses as <code>external_request_id</code>.
   */
  @JsonProperty("request_id")
  public Optional<String> getRequestId() {
    return requestId;
  }

  /**
   * @return Opt in to per-word timing events for the synthesized audio. When <code>true</code>, the server interleaves <code>status: &quot;word_timestamp&quot;</code> frames with the audio <code>chunk</code> frames; each carries <code>data: { id, word, start, end }</code> where <code>start</code>/<code>end</code> are floats in seconds relative to the start of the audio stream, and <code>word</code> is verbatim from the input text (un-normalized — <code>&quot;$100&quot;</code> stays <code>&quot;$100&quot;</code>, not <code>&quot;one hundred dollars&quot;</code>). Supported on base-queue English + Hindi voices (<code>meher</code>, <code>devansh</code>, <code>kartik</code>, <code>maithili</code>, <code>liam</code>, <code>avery</code>); other voice families silently emit no word events (audio still works). Defaults to <code>false</code> so existing integrations see no change.
   */
  @JsonProperty("word_timestamps")
  public Optional<Boolean> getWordTimestamps() {
    return wordTimestamps;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof LightningV31TtsRequestMessage && equalTo((LightningV31TtsRequestMessage) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(LightningV31TtsRequestMessage other) {
    return voiceId.equals(other.voiceId) && text.equals(other.text) && model.equals(other.model) && maxBufferFlushMs.equals(other.maxBufferFlushMs) && continue_.equals(other.continue_) && flush.equals(other.flush) && completeBackoffMs.equals(other.completeBackoffMs) && language.equals(other.language) && numberPronunciationLanguage.equals(other.numberPronunciationLanguage) && sampleRate.equals(other.sampleRate) && speed.equals(other.speed) && sessionId.equals(other.sessionId) && requestId.equals(other.requestId) && wordTimestamps.equals(other.wordTimestamps);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.voiceId, this.text, this.model, this.maxBufferFlushMs, this.continue_, this.flush, this.completeBackoffMs, this.language, this.numberPronunciationLanguage, this.sampleRate, this.speed, this.sessionId, this.requestId, this.wordTimestamps);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static VoiceIdStage builder() {
    return new Builder();
  }

  public interface VoiceIdStage {
    /**
     * <p>The ID of the voice to use</p>
     */
    TextStage voiceId(@NotNull String voiceId);

    Builder from(LightningV31TtsRequestMessage other);
  }

  public interface TextStage {
    /**
     * <p>The text to convert to speech</p>
     */
    _FinalStage text(@NotNull String text);
  }

  public interface _FinalStage {
    LightningV31TtsRequestMessage build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>TTS model to route the request to.</p>
     * <ul>
     * <li><code>lightning_v3.1</code> (default) — standard Lightning v3.1 pool.</li>
     * <li><code>lightning_v3.1_pro</code> — Lightning v3.1 Pro pool with a curated
     * voice catalog. See the
     * <a href="/waves/model-cards/text-to-speech/lightning-v-3-1-pro">Pro model card</a>.</li>
     * </ul>
     * <p>New integrations should connect to the unified
     * <code>/waves/v1/tts/live</code> WebSocket instead of this endpoint, but the
     * <code>model</code> field is supported here for backwards-compatible Pro
     * opt-in.</p>
     */
    _FinalStage model(Optional<LightningV31TtsRequestMessageModel> model);

    _FinalStage model(LightningV31TtsRequestMessageModel model);

    /**
     * <p>The maximum time (in ms) to wait for more input before generating output. It flushes when either this time is reached or enough input is received for optimal output—whichever comes first. This is useful for input streams. Defaults to 0</p>
     */
    _FinalStage maxBufferFlushMs(Optional<Integer> maxBufferFlushMs);

    _FinalStage maxBufferFlushMs(Integer maxBufferFlushMs);

    /**
     * <p>This setting controls whether the system should buffer and wait for more input after receiving the current one. If not set, it assumes no more input is coming.</p>
     */
    _FinalStage continue_(Optional<Boolean> continue_);

    _FinalStage continue_(Boolean continue_);

    /**
     * <p>This setting controls whether the system should flush the current buffer.</p>
     */
    _FinalStage flush(Optional<Boolean> flush);

    _FinalStage flush(Boolean flush);

    /**
     * <p>The time in ms to wait after the last chunk is sent before sending the complete response. Default is 4000ms. Maximum is 10000ms.</p>
     */
    _FinalStage completeBackoffMs(Optional<Double> completeBackoffMs);

    _FinalStage completeBackoffMs(Double completeBackoffMs);

    /**
     * <p>Language code for synthesis. Influences pronunciation, number/date
     * normalization, and phoneme selection.</p>
     * <ul>
     * <li>Indian: <code>en</code>, <code>hi</code>, <code>mr</code>, <code>kn</code>, <code>ta</code>, <code>bn</code>, <code>gu</code>, <code>te</code>, <code>ml</code>, <code>pa</code>, <code>or</code></li>
     * <li>European: <code>es</code> (Spanish)</li>
     * </ul>
     */
    _FinalStage language(Optional<LightningV31TtsRequestMessageLanguage> language);

    _FinalStage language(LightningV31TtsRequestMessageLanguage language);

    /**
     * <p>Optional. Sets the language used to read out numbers, dates,
     * and other text — the text-normalization language —
     * independently of the synthesis voice.</p>
     * <ul>
     * <li>If you <strong>omit <code>language</code></strong>, this value also becomes the
     * synthesis language: model selection and voice routing
     * follow it.</li>
     * <li>If you <strong>set <code>language</code> explicitly</strong>, <code>language</code> always wins
     * for synthesis and <code>number_pronunciation_language</code> only
     * changes how numbers and text are normalized (for example,
     * read digits in Hindi while an English voice speaks the
     * surrounding words).</li>
     * <li>Omit this field to keep the existing behaviour —
     * normalization follows <code>language</code>.</li>
     * </ul>
     * <p>Accepts the same language codes as <code>language</code>.</p>
     */
    _FinalStage numberPronunciationLanguage(
        Optional<LightningV31TtsRequestMessageNumberPronunciationLanguage> numberPronunciationLanguage);

    _FinalStage numberPronunciationLanguage(
        LightningV31TtsRequestMessageNumberPronunciationLanguage numberPronunciationLanguage);

    /**
     * <p>Audio sample rate in Hz. Supported values: 8000, 16000, 24000, 44100</p>
     */
    _FinalStage sampleRate(Optional<Integer> sampleRate);

    _FinalStage sampleRate(Integer sampleRate);

    /**
     * <p>Speaking speed multiplier</p>
     */
    _FinalStage speed(Optional<Double> speed);

    _FinalStage speed(Double speed);

    /**
     * <p>Optional client-provided session identifier for correlation. Only alphanumeric characters, hyphens, underscores, and dots allowed. Max 128 characters. Echoed back in responses as <code>external_session_id</code>.</p>
     */
    _FinalStage sessionId(Optional<String> sessionId);

    _FinalStage sessionId(String sessionId);

    /**
     * <p>Optional client-provided request identifier for correlation. Only alphanumeric characters, hyphens, underscores, and dots allowed. Max 128 characters. Echoed back in responses as <code>external_request_id</code>.</p>
     */
    _FinalStage requestId(Optional<String> requestId);

    _FinalStage requestId(String requestId);

    /**
     * <p>Opt in to per-word timing events for the synthesized audio. When <code>true</code>, the server interleaves <code>status: &quot;word_timestamp&quot;</code> frames with the audio <code>chunk</code> frames; each carries <code>data: { id, word, start, end }</code> where <code>start</code>/<code>end</code> are floats in seconds relative to the start of the audio stream, and <code>word</code> is verbatim from the input text (un-normalized — <code>&quot;$100&quot;</code> stays <code>&quot;$100&quot;</code>, not <code>&quot;one hundred dollars&quot;</code>). Supported on base-queue English + Hindi voices (<code>meher</code>, <code>devansh</code>, <code>kartik</code>, <code>maithili</code>, <code>liam</code>, <code>avery</code>); other voice families silently emit no word events (audio still works). Defaults to <code>false</code> so existing integrations see no change.</p>
     */
    _FinalStage wordTimestamps(Optional<Boolean> wordTimestamps);

    _FinalStage wordTimestamps(Boolean wordTimestamps);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements VoiceIdStage, TextStage, _FinalStage {
    private String voiceId;

    private String text;

    private Optional<Boolean> wordTimestamps = Optional.empty();

    private Optional<String> requestId = Optional.empty();

    private Optional<String> sessionId = Optional.empty();

    private Optional<Double> speed = Optional.empty();

    private Optional<Integer> sampleRate = Optional.empty();

    private Optional<LightningV31TtsRequestMessageNumberPronunciationLanguage> numberPronunciationLanguage = Optional.empty();

    private Optional<LightningV31TtsRequestMessageLanguage> language = Optional.empty();

    private Optional<Double> completeBackoffMs = Optional.empty();

    private Optional<Boolean> flush = Optional.empty();

    private Optional<Boolean> continue_ = Optional.empty();

    private Optional<Integer> maxBufferFlushMs = Optional.empty();

    private Optional<LightningV31TtsRequestMessageModel> model = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(LightningV31TtsRequestMessage other) {
      voiceId(other.getVoiceId());
      text(other.getText());
      model(other.getModel());
      maxBufferFlushMs(other.getMaxBufferFlushMs());
      continue_(other.getContinue());
      flush(other.getFlush());
      completeBackoffMs(other.getCompleteBackoffMs());
      language(other.getLanguage());
      numberPronunciationLanguage(other.getNumberPronunciationLanguage());
      sampleRate(other.getSampleRate());
      speed(other.getSpeed());
      sessionId(other.getSessionId());
      requestId(other.getRequestId());
      wordTimestamps(other.getWordTimestamps());
      return this;
    }

    /**
     * <p>The ID of the voice to use</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("voice_id")
    public TextStage voiceId(@NotNull String voiceId) {
      this.voiceId = Objects.requireNonNull(voiceId, "voiceId must not be null");
      return this;
    }

    /**
     * <p>The text to convert to speech</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("text")
    public _FinalStage text(@NotNull String text) {
      this.text = Objects.requireNonNull(text, "text must not be null");
      return this;
    }

    /**
     * <p>Opt in to per-word timing events for the synthesized audio. When <code>true</code>, the server interleaves <code>status: &quot;word_timestamp&quot;</code> frames with the audio <code>chunk</code> frames; each carries <code>data: { id, word, start, end }</code> where <code>start</code>/<code>end</code> are floats in seconds relative to the start of the audio stream, and <code>word</code> is verbatim from the input text (un-normalized — <code>&quot;$100&quot;</code> stays <code>&quot;$100&quot;</code>, not <code>&quot;one hundred dollars&quot;</code>). Supported on base-queue English + Hindi voices (<code>meher</code>, <code>devansh</code>, <code>kartik</code>, <code>maithili</code>, <code>liam</code>, <code>avery</code>); other voice families silently emit no word events (audio still works). Defaults to <code>false</code> so existing integrations see no change.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage wordTimestamps(Boolean wordTimestamps) {
      this.wordTimestamps = Optional.ofNullable(wordTimestamps);
      return this;
    }

    /**
     * <p>Opt in to per-word timing events for the synthesized audio. When <code>true</code>, the server interleaves <code>status: &quot;word_timestamp&quot;</code> frames with the audio <code>chunk</code> frames; each carries <code>data: { id, word, start, end }</code> where <code>start</code>/<code>end</code> are floats in seconds relative to the start of the audio stream, and <code>word</code> is verbatim from the input text (un-normalized — <code>&quot;$100&quot;</code> stays <code>&quot;$100&quot;</code>, not <code>&quot;one hundred dollars&quot;</code>). Supported on base-queue English + Hindi voices (<code>meher</code>, <code>devansh</code>, <code>kartik</code>, <code>maithili</code>, <code>liam</code>, <code>avery</code>); other voice families silently emit no word events (audio still works). Defaults to <code>false</code> so existing integrations see no change.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "word_timestamps",
        nulls = Nulls.SKIP
    )
    public _FinalStage wordTimestamps(Optional<Boolean> wordTimestamps) {
      this.wordTimestamps = wordTimestamps;
      return this;
    }

    /**
     * <p>Optional client-provided request identifier for correlation. Only alphanumeric characters, hyphens, underscores, and dots allowed. Max 128 characters. Echoed back in responses as <code>external_request_id</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage requestId(String requestId) {
      this.requestId = Optional.ofNullable(requestId);
      return this;
    }

    /**
     * <p>Optional client-provided request identifier for correlation. Only alphanumeric characters, hyphens, underscores, and dots allowed. Max 128 characters. Echoed back in responses as <code>external_request_id</code>.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "request_id",
        nulls = Nulls.SKIP
    )
    public _FinalStage requestId(Optional<String> requestId) {
      this.requestId = requestId;
      return this;
    }

    /**
     * <p>Optional client-provided session identifier for correlation. Only alphanumeric characters, hyphens, underscores, and dots allowed. Max 128 characters. Echoed back in responses as <code>external_session_id</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage sessionId(String sessionId) {
      this.sessionId = Optional.ofNullable(sessionId);
      return this;
    }

    /**
     * <p>Optional client-provided session identifier for correlation. Only alphanumeric characters, hyphens, underscores, and dots allowed. Max 128 characters. Echoed back in responses as <code>external_session_id</code>.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "session_id",
        nulls = Nulls.SKIP
    )
    public _FinalStage sessionId(Optional<String> sessionId) {
      this.sessionId = sessionId;
      return this;
    }

    /**
     * <p>Speaking speed multiplier</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage speed(Double speed) {
      this.speed = Optional.ofNullable(speed);
      return this;
    }

    /**
     * <p>Speaking speed multiplier</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "speed",
        nulls = Nulls.SKIP
    )
    public _FinalStage speed(Optional<Double> speed) {
      this.speed = speed;
      return this;
    }

    /**
     * <p>Audio sample rate in Hz. Supported values: 8000, 16000, 24000, 44100</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage sampleRate(Integer sampleRate) {
      this.sampleRate = Optional.ofNullable(sampleRate);
      return this;
    }

    /**
     * <p>Audio sample rate in Hz. Supported values: 8000, 16000, 24000, 44100</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "sample_rate",
        nulls = Nulls.SKIP
    )
    public _FinalStage sampleRate(Optional<Integer> sampleRate) {
      this.sampleRate = sampleRate;
      return this;
    }

    /**
     * <p>Optional. Sets the language used to read out numbers, dates,
     * and other text — the text-normalization language —
     * independently of the synthesis voice.</p>
     * <ul>
     * <li>If you <strong>omit <code>language</code></strong>, this value also becomes the
     * synthesis language: model selection and voice routing
     * follow it.</li>
     * <li>If you <strong>set <code>language</code> explicitly</strong>, <code>language</code> always wins
     * for synthesis and <code>number_pronunciation_language</code> only
     * changes how numbers and text are normalized (for example,
     * read digits in Hindi while an English voice speaks the
     * surrounding words).</li>
     * <li>Omit this field to keep the existing behaviour —
     * normalization follows <code>language</code>.</li>
     * </ul>
     * <p>Accepts the same language codes as <code>language</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage numberPronunciationLanguage(
        LightningV31TtsRequestMessageNumberPronunciationLanguage numberPronunciationLanguage) {
      this.numberPronunciationLanguage = Optional.ofNullable(numberPronunciationLanguage);
      return this;
    }

    /**
     * <p>Optional. Sets the language used to read out numbers, dates,
     * and other text — the text-normalization language —
     * independently of the synthesis voice.</p>
     * <ul>
     * <li>If you <strong>omit <code>language</code></strong>, this value also becomes the
     * synthesis language: model selection and voice routing
     * follow it.</li>
     * <li>If you <strong>set <code>language</code> explicitly</strong>, <code>language</code> always wins
     * for synthesis and <code>number_pronunciation_language</code> only
     * changes how numbers and text are normalized (for example,
     * read digits in Hindi while an English voice speaks the
     * surrounding words).</li>
     * <li>Omit this field to keep the existing behaviour —
     * normalization follows <code>language</code>.</li>
     * </ul>
     * <p>Accepts the same language codes as <code>language</code>.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "number_pronunciation_language",
        nulls = Nulls.SKIP
    )
    public _FinalStage numberPronunciationLanguage(
        Optional<LightningV31TtsRequestMessageNumberPronunciationLanguage> numberPronunciationLanguage) {
      this.numberPronunciationLanguage = numberPronunciationLanguage;
      return this;
    }

    /**
     * <p>Language code for synthesis. Influences pronunciation, number/date
     * normalization, and phoneme selection.</p>
     * <ul>
     * <li>Indian: <code>en</code>, <code>hi</code>, <code>mr</code>, <code>kn</code>, <code>ta</code>, <code>bn</code>, <code>gu</code>, <code>te</code>, <code>ml</code>, <code>pa</code>, <code>or</code></li>
     * <li>European: <code>es</code> (Spanish)</li>
     * </ul>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage language(LightningV31TtsRequestMessageLanguage language) {
      this.language = Optional.ofNullable(language);
      return this;
    }

    /**
     * <p>Language code for synthesis. Influences pronunciation, number/date
     * normalization, and phoneme selection.</p>
     * <ul>
     * <li>Indian: <code>en</code>, <code>hi</code>, <code>mr</code>, <code>kn</code>, <code>ta</code>, <code>bn</code>, <code>gu</code>, <code>te</code>, <code>ml</code>, <code>pa</code>, <code>or</code></li>
     * <li>European: <code>es</code> (Spanish)</li>
     * </ul>
     */
    @java.lang.Override
    @JsonSetter(
        value = "language",
        nulls = Nulls.SKIP
    )
    public _FinalStage language(Optional<LightningV31TtsRequestMessageLanguage> language) {
      this.language = language;
      return this;
    }

    /**
     * <p>The time in ms to wait after the last chunk is sent before sending the complete response. Default is 4000ms. Maximum is 10000ms.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage completeBackoffMs(Double completeBackoffMs) {
      this.completeBackoffMs = Optional.ofNullable(completeBackoffMs);
      return this;
    }

    /**
     * <p>The time in ms to wait after the last chunk is sent before sending the complete response. Default is 4000ms. Maximum is 10000ms.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "complete_backoff_ms",
        nulls = Nulls.SKIP
    )
    public _FinalStage completeBackoffMs(Optional<Double> completeBackoffMs) {
      this.completeBackoffMs = completeBackoffMs;
      return this;
    }

    /**
     * <p>This setting controls whether the system should flush the current buffer.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage flush(Boolean flush) {
      this.flush = Optional.ofNullable(flush);
      return this;
    }

    /**
     * <p>This setting controls whether the system should flush the current buffer.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "flush",
        nulls = Nulls.SKIP
    )
    public _FinalStage flush(Optional<Boolean> flush) {
      this.flush = flush;
      return this;
    }

    /**
     * <p>This setting controls whether the system should buffer and wait for more input after receiving the current one. If not set, it assumes no more input is coming.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage continue_(Boolean continue_) {
      this.continue_ = Optional.ofNullable(continue_);
      return this;
    }

    /**
     * <p>This setting controls whether the system should buffer and wait for more input after receiving the current one. If not set, it assumes no more input is coming.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "continue",
        nulls = Nulls.SKIP
    )
    public _FinalStage continue_(Optional<Boolean> continue_) {
      this.continue_ = continue_;
      return this;
    }

    /**
     * <p>The maximum time (in ms) to wait for more input before generating output. It flushes when either this time is reached or enough input is received for optimal output—whichever comes first. This is useful for input streams. Defaults to 0</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage maxBufferFlushMs(Integer maxBufferFlushMs) {
      this.maxBufferFlushMs = Optional.ofNullable(maxBufferFlushMs);
      return this;
    }

    /**
     * <p>The maximum time (in ms) to wait for more input before generating output. It flushes when either this time is reached or enough input is received for optimal output—whichever comes first. This is useful for input streams. Defaults to 0</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "max_buffer_flush_ms",
        nulls = Nulls.SKIP
    )
    public _FinalStage maxBufferFlushMs(Optional<Integer> maxBufferFlushMs) {
      this.maxBufferFlushMs = maxBufferFlushMs;
      return this;
    }

    /**
     * <p>TTS model to route the request to.</p>
     * <ul>
     * <li><code>lightning_v3.1</code> (default) — standard Lightning v3.1 pool.</li>
     * <li><code>lightning_v3.1_pro</code> — Lightning v3.1 Pro pool with a curated
     * voice catalog. See the
     * <a href="/waves/model-cards/text-to-speech/lightning-v-3-1-pro">Pro model card</a>.</li>
     * </ul>
     * <p>New integrations should connect to the unified
     * <code>/waves/v1/tts/live</code> WebSocket instead of this endpoint, but the
     * <code>model</code> field is supported here for backwards-compatible Pro
     * opt-in.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage model(LightningV31TtsRequestMessageModel model) {
      this.model = Optional.ofNullable(model);
      return this;
    }

    /**
     * <p>TTS model to route the request to.</p>
     * <ul>
     * <li><code>lightning_v3.1</code> (default) — standard Lightning v3.1 pool.</li>
     * <li><code>lightning_v3.1_pro</code> — Lightning v3.1 Pro pool with a curated
     * voice catalog. See the
     * <a href="/waves/model-cards/text-to-speech/lightning-v-3-1-pro">Pro model card</a>.</li>
     * </ul>
     * <p>New integrations should connect to the unified
     * <code>/waves/v1/tts/live</code> WebSocket instead of this endpoint, but the
     * <code>model</code> field is supported here for backwards-compatible Pro
     * opt-in.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "model",
        nulls = Nulls.SKIP
    )
    public _FinalStage model(Optional<LightningV31TtsRequestMessageModel> model) {
      this.model = model;
      return this;
    }

    @java.lang.Override
    public LightningV31TtsRequestMessage build() {
      return new LightningV31TtsRequestMessage(voiceId, text, model, maxBufferFlushMs, continue_, flush, completeBackoffMs, language, numberPronunciationLanguage, sampleRate, speed, sessionId, requestId, wordTimestamps, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
